# Round 3 影响分析

> 日期：2026-07-14
> 本轮范围：FR-5 / FR-6 / FR-9 / FR-12 / FR-13

---

## 变更影响链

### 1. MediaVisualComponents.kt — YingShiMediaFrame 新增可选参数

| 路径 | 影响 |
|------|------|
| `MediaVisualComponents.kt::YingShiMediaFrame` 新增 `selectedBorderColor: Color? = null` | 直接影响 |
| → `SystemMediaGrid.kt::SystemMediaCard` 传入 viewerAccent | 间接影响（目标变更） |
| → `PhotoFeedScreen.kt` 调用 YingShiMediaFrame（不传新参数） | 无影响（默认 null 保持原行为） |
| → `AlbumPostCard.kt` 调用 YingShiMediaFrame（不传新参数） | 无影响 |
| → `SmallAlbumMediaGrid.kt` 调用 YingShiMediaFrame（不传新参数） | 无影响 |

**IA-1**: 验证 PhotoFeedScreen / AlbumPostCard / SmallAlbumMediaGrid 的 YingShiMediaFrame 选中态视觉不变

### 2. SystemMediaGrid.kt — SystemMediaBadge token 替换

| 路径 | 影响 |
|------|------|
| `SystemMediaGrid.kt::SystemMediaBadge` color 从 raisedSurface → memoryContainer | 直接影响 |
| → 所有调用 SystemMediaBadge("已导入") 的位置 | 视觉变化（目标） |
| → SystemMediaBadge 的其他调用方 | 无（仅 SystemMediaCard 调用） |

**IA-2**: 验证"已导入"badge 视觉效果（memoryContainer 浅底 + onMemoryContainer 文字）

### 3. SystemMediaModels.kt — 新增 SystemMediaAlbum + UiState 字段

| 路径 | 影响 |
|------|------|
| `SystemMediaModels.kt` 新增 SystemMediaAlbum data class | 直接影响 |
| → `SystemMediaRepository.kt` 使用 SystemMediaAlbum | 间接影响 |
| → `SystemMediaViewModel.kt` 使用 SystemMediaAlbum | 间接影响 |
| → `SystemMediaComponents.kt` SystemMediaAlbumSheet 使用 SystemMediaAlbum | 间接影响 |
| `SystemMediaUiState` 新增 selectedAlbum + albums 字段 | 直接影响 |
| → `SystemMediaScreen.kt` collectAsState 读取新字段 | 间接影响 |
| → `SystemMediaViewModel.kt` publishState 需设置新字段 | 间接影响 |

**IA-3**: 验证 SystemMediaUiState 新字段在所有 publishState 调用处正确初始化

### 4. SystemMediaRepository.kt — 新增 queryAlbums/loadAlbums

| 路径 | 影响 |
|------|------|
| `SystemMediaDataSource` 接口新增 queryAlbums() | 直接影响 |
| → `MediaStoreSystemMediaDataSource` 必须实现 queryAlbums() | 间接影响 |
| → 任何其他 SystemMediaDataSource 实现类 | 需检查（当前仅 MediaStoreSystemMediaDataSource） |
| `SystemMediaRepository` 接口新增 loadAlbums() | 直接影响 |
| → `LocalSystemMediaRepository` 必须实现 loadAlbums() | 间接影响 |
| → 测试桩/Fake Repository | 需检查（如有） |

**IA-4**: 验证 SystemMediaDataSource 接口变更不破坏现有实现（确认无其他实现类）
**IA-5**: 验证 loadAlbums() 在无权限/空媒体时不崩溃

### 5. SystemMediaViewModel.kt — applyFilter 签名变更 + album 逻辑

| 路径 | 影响 |
|------|------|
| `applyFilter(filter)` → `applyFilter(filter, album)` | 直接影响 |
| → `publishState` 调用 applyFilter 的所有位置 | 需传入 album |
| → `init` / `refresh` / `startBackgroundRefresh` / `onFilterSelected` / `refreshLocalState` / `handleBridgeMutation` | 均调用 publishState |
| 新增 `onAlbumSelected` / `loadAlbums` | 直接影响 |
| → `SystemMediaScreen.kt` 调用新方法 | 间接影响 |

**IA-6**: 验证所有 publishState 调用路径正确传入 selectedAlbum

### 6. SystemMediaComponents.kt — TopBar 新增参数 + 副标题文案变更

| 路径 | 影响 |
|------|------|
| `SystemMediaTopBar` 新增参数（onOpenAlbums, selectedAlbum, totalCount） | 直接影响 |
| → `SystemMediaScreen.kt` 调用 TopBar 需传入新参数 | 间接影响 |
| 副标题文案从"当前筛选：xx" → "共 N 项" | 直接影响 |
| → 用户体验变化 | 目标变更 |

**IA-7**: 验证 TopBar 在无 album 数据时相册按钮正常显示
**IA-8**: 验证副标题在不同 filter/album 组合下显示正确

### 7. SystemMediaViewerOverlayChrome.kt — 新建文件

| 路径 | 影响 |
|------|------|
| 新建 SystemMediaViewerTopScrim / SystemMediaViewerBottomScrim | 直接影响 |
| → `SystemMediaViewerScreen.kt` 使用新组件 | 间接影响 |
| → 07 模块 ViewerOverlayChrome.kt | 无影响（独立文件，不复用） |

**IA-9**: 验证 Viewer overlay 在沉浸模式切换时正确显隐

### 8. SystemMediaViewerScreen.kt — overlay 动画参数 + Scrim 包裹

| 路径 | 影响 |
|------|------|
| AnimatedVisibility fadeIn/fadeOut 加 tween + slide | 直接影响 |
| → 沉浸模式切换体验 | 视觉变化（目标） |
| 新增 SystemMediaViewerTopScrim/BottomScrim 叠加层 | 直接影响 |
| → overlay 层级 z-order | 需确保 Scrim 在 TopBar/InfoCard 下方 |

**IA-10**: 验证 Scrim 不遮挡 TopBar/InfoCard 的点击事件

### 9. SystemMediaViewerControls.kt — InfoCard/CircleButton/VideoControls/MenuAction

| 路径 | 影响 |
|------|------|
| InfoCard 添加 drawBehind 渐变 + viewerOverlayBorder + shadowElevation | 直接影响 |
| CircleButton 添加 drawBehind 渐变 + viewerOverlayBorder + size 46dp | 直接影响 |
| → CircleButton size 从 44dp → 46dp | 布局微调 |
| VideoControls border 改为 viewerOverlayBorder | 直接影响 |
| MenuAction danger 色从 MaterialTheme.colorScheme.error → colors.destructive | 直接影响 |

**IA-11**: 验证 CircleButton size 变化不影响 TopBar Row 布局
**IA-12**: 验证 MenuAction danger 项视觉正确（destructive token）

### 10. SystemMediaGrid.kt — Ghost Overlay 动画参数

| 路径 | 影响 |
|------|------|
| SystemMediaDensityGhostOverlay alpha 用 motion token | 直接影响 |
| scale 从固定 0.998f → Animatable(1f → 0.96f) | 直接影响 |
| → 密度切换体验 | 视觉变化（目标） |

**IA-13**: 验证 16列↔3列大跨度切换流畅
**IA-14**: 验证 Ghost Overlay 消失后 onFinished 正确回调

### 11. SystemMediaScreen.kt — 主 grid 淡入动画

| 路径 | 影响 |
|------|------|
| 新增 gridEnterAlpha 状态 | 直接影响 |
| → density 切换时主 grid 淡入 | 视觉变化（目标） |
| → 滚动/交互不受影响 | 需验证 |

**IA-15**: 验证 gridEnterAlpha 不阻塞滚动和点击
**IA-16**: 验证 density 切换后 grid 正常交互

---

## 必须测试的影响路径清单

| 编号 | 影响路径 | 验证方法 |
|------|---------|---------|
| IA-1 | PhotoFeedScreen/AlbumPostCard/SmallAlbumMediaGrid 的 YingShiMediaFrame 选中态不变 | 编译通过 + 视觉回归 |
| IA-2 | "已导入"badge 使用 memoryContainer 视觉 | 真机/Preview |
| IA-3 | SystemMediaUiState 新字段在 publishState 正确初始化 | 编译通过 + 逻辑验证 |
| IA-4 | SystemMediaDataSource 接口不破坏现有实现 | 编译通过 |
| IA-5 | loadAlbums() 无权限/空媒体不崩溃 | 边界测试 |
| IA-6 | 所有 publishState 路径传入 selectedAlbum | 编译通过 + 逻辑验证 |
| IA-7 | TopBar 相册按钮在无 album 数据时正常 | 边界测试 |
| IA-8 | 副标题在不同 filter/album 组合下正确 | 逻辑验证 |
| IA-9 | Viewer overlay 沉浸模式切换正确显隐 | 真机/Preview |
| IA-10 | Scrim 不遮挡 TopBar/InfoCard 点击 | 真机 |
| IA-11 | CircleButton size 变化不影响 TopBar 布局 | 编译 + 视觉 |
| IA-12 | MenuAction danger 项使用 destructive token | 视觉验证 |
| IA-13 | 16列↔3列大跨度切换流畅 | 真机 |
| IA-14 | Ghost Overlay onFinished 正确回调 | 逻辑验证 |
| IA-15 | gridEnterAlpha 不阻塞滚动和点击 | 真机 |
| IA-16 | density 切换后 grid 正常交互 | 真机 |
