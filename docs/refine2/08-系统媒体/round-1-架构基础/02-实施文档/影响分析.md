# Round 1 影响分析 — 架构基础（拆分重构 + 代码清理）

> 基于: Round 1 实施计划 2026-07-13
> 范围: FR-1 + FR-2 + FR-3 + FR-4
> 端: Android

---

## 一、变更文件影响链

### 文件1: SystemMediaPalette.kt（新建）

**变更**: 提取 palette 函数到公共位置

**影响链**:
```
SystemMediaPalette.kt (新建)
├── SystemMediaRepository.kt (删除 paletteForSystemMediaId, 改引用)
│   ├── 行 250 调用点 → paletteForSystemMediaId(mediaStoreId)
│   └── 行 387 调用点 → paletteForSystemMediaId(mediaStoreId)
└── LocalSystemMediaBridgeRepository.kt (删除 pickedPaletteFor, 改引用)
    └── 行 2744 调用点 → paletteForPickedMedia(index, type)
```

**影响说明**: paletteForSystemMediaId 原 4 组 → 改用 6 组。前 4 组颜色不变，新增 2 组轮换。视觉差异极小。

### 文件2: ViewerZoomState.kt（修改）

**变更**: 追加 systemViewerZoomGesture Modifier + helper

**影响链**:
```
ViewerZoomState.kt (修改, 155→~260 行)
├── SystemMediaViewerScreen.kt (删除 SystemViewerZoomState 类 + 私有常量 + systemViewerZoomGesture, 改用共享版)
│   ├── 行 404 实例化 → ViewerZoomState()
│   ├── 行 738, 743 参数类型 → ViewerZoomState?
│   └── 行 884 调用 → systemViewerZoomGesture(同 package 引用)
└── 07 模块 PhotoViewerScreen.kt (不改动, 仅共享 ViewerZoomState 类)
```

**影响说明**: systemViewerZoomGesture 迁出到 ViewerZoomState.kt 后，07 模块的 viewerZoomGesture（如有）不受影响。仅 SystemMediaViewerScreen.kt 改用共享 ViewerZoomState。

### 文件3: OperationBus.kt（新建）

**变更**: 提取操作结果事件总线

**影响链**:
```
OperationBus.kt (新建)
├── LocalSystemMediaBridgeRepository.kt (薄代理)
│   ├── operationResults getter → OperationBus.operationResults
│   └── dismissOperationResult(eventId) → OperationBus.dismissOperationResult(eventId)
├── UploadManager.kt (调用 publishOperationSummaryIfReady 等)
│   ├── enqueueRealUploadTask → publishOperationSummaryIfReady
│   ├── pauseUploadTask → publishOperationSummaryIfReady
│   └── retryUploadTask → publishOperationResult
└── 外部调用方 (YingShiApp.kt)
    └── operationResults → LocalSystemMediaBridgeRepository.operationResults → OperationBus.operationResults
```

**影响说明**: OperationBus 通过 provider 接口注入 uploadTasksState 和 operationRequestsById 的只读访问。外部调用方零改动（通过 LocalSystemMediaBridgeRepository 间接访问）。

### 文件4: ImportOverlayStore.kt（新建）

**变更**: 提取导入状态覆盖层

**影响链**:
```
ImportOverlayStore.kt (新建)
├── LocalSystemMediaBridgeRepository.kt (薄代理)
│   ├── applyOverlay → ImportOverlayStore.applyOverlay
│   ├── rememberImportStatus → ImportOverlayStore.rememberImportStatus
│   ├── forgetImportStatus* → ImportOverlayStore.forgetImportStatus*
│   ├── markMovedToSystemTrash → ImportOverlayStore.markMovedToSystemTrash
│   ├── latestMutationEvent getter → ImportOverlayStore.latestMutationEvent
│   └── mutationVersion getter → ImportOverlayStore.mutationVersion
├── UploadManager.kt (调用 rememberAppMediaIdForSource / linkMediaToPost / publishMutation)
│   ├── 上传成功后 → rememberAppMediaIdForSource
│   ├── 收尾时 → linkMediaToPost
│   └── 状态变更通知 → publishMutation
└── 外部调用方
    ├── SystemMediaViewModel.kt → applyOverlay (通过 LocalSystemMediaBridgeRepository)
    ├── SystemMediaRepository.kt → warmPersistentImportOverlay / rememberImportStatus / forgetImportStatus
    ├── RealTrashViewModels.kt → forgetImportStatusByAppMediaId(s)
    └── RealPhotoFeedViewModel.kt → forgetImportStatusByAppMediaId
```

### 文件5: UploadManager.kt + SystemMediaUploadReal.kt + SystemMediaUploadSupport.kt（新建）

**变更**: 提取上传调度 + Real 实现 + 支持工具

**影响链**:
```
UploadManager.kt (新建, 入口+调度+生命周期)
├── LocalSystemMediaBridgeRepository.kt (薄代理)
│   ├── enqueue* → UploadManager.enqueue*
│   ├── pause/cancel/dismiss/retry → UploadManager.*
│   └── uploadTasks getter → UploadManager.uploadTasks
├── SystemMediaUploadReal.kt (Real 实现+收尾)
│   └── 被 UploadManager 调用
├── SystemMediaUploadSupport.kt (持久化+远程历史+picked+API工具+Fake)
│   ├── 被 UploadManager 调用
│   └── 被 SystemMediaUploadReal 调用
└── 外部调用方
    ├── TransferCenterScreen.kt → uploadTasks / cancel / pause / retry (通过 LocalSystemMediaBridgeRepository)
    ├── YingShiApp.kt → enqueueImportToAppUpload / operationResults
    ├── SystemMediaScreen.kt → enqueue* / buildImportToAppPreview
    ├── SystemMediaViewerScreen.kt → enqueue* / markMovedToSystemTrash
    └── PostComposerViewModel.kt → enqueueCreatePostUpload / createPostFromSystemMediaDraft
```

### 文件6: LocalSystemMediaBridgeRepository.kt（修改，薄代理）

**变更**: 3273 行 → ~200 行薄代理

**影响链**:
```
LocalSystemMediaBridgeRepository.kt (薄代理)
├── 保留 object 单例外壳
├── 保留 4 个公共类型 (MutationKind / OperationType / MutationEvent / OperationResultEvent)
├── 所有 public 入口函数委托给子模块
└── 所有外部调用方 (11 个文件, 70 处引用) 零改动
```

### 文件7-11: SystemMediaScreen 拆分（States / Selection / Grid / Scrubber / Components）

**影响链**:
```
SystemMediaStates.kt (新建)
├── SystemMediaScreen.kt (主容器调用状态 Composable)
└── 无外部影响 (private 改 internal, 同 module)

SystemMediaSelection.kt (新建)
├── SystemMediaScreen.kt (调用 SelectionBar / SelectionBadge / SelectionNumberFlashOverlay)
├── SystemMediaGrid.kt (SystemMediaCard 调用 SelectionBadge)
└── SystemMediaRowMapping (Selection + Grid 共用)

SystemMediaGrid.kt (新建)
├── SystemMediaScreen.kt (调用 SystemMediaCard / DensityGhostOverlay / buildSystemMediaGridBlocks)
├── SystemMediaGridBlock (Grid + Scrubber + 主容器共用)
└── SystemMediaDensityGhost (Grid + 主容器共用)

SystemMediaScrubber.kt (新建)
├── SystemMediaScreen.kt (调用滑条 helper)
└── 无外部影响

SystemMediaComponents.kt (新建)
├── SystemMediaScreen.kt (调用 TopBar / AtmosphereLayer / IconButton)
├── SystemMediaStates.kt (调用 ActionChip)
└── SystemMediaSelection.kt (调用 ActionChip)
```

### 文件12-13: SystemMediaViewerScreen 拆分（Canvas / Controls）

**影响链**:
```
SystemMediaViewerCanvas.kt (新建)
├── SystemMediaViewerScreen.kt (主容器调用 Canvas)
└── 无外部影响

SystemMediaViewerControls.kt (新建)
├── SystemMediaViewerScreen.kt (主容器调用 TopBar / InfoCard / VideoControls / MenuSheet)
└── 无外部影响
```

---

## 二、必须测试的影响路径清单

| IA# | 影响路径 | 测试要点 | 端 | 优先级 |
|-----|---------|---------|-----|--------|
| IA-1 | MediaStore 查询 → paletteForSystemMediaId | 缩略图占位色正确（6 组轮换） | Android | P1 |
| IA-2 | Picked 媒体 → paletteForPickedMedia | picked 媒体占位色正确（6 组轮换，视频 offset+1） | Android | P1 |
| IA-3 | SystemMediaViewerScreen → ViewerZoomState | 缩放/双击/弹性回弹正常 | Android | P0 |
| IA-4 | SystemMediaViewerScreen → systemViewerZoomGesture | 双指缩放手势正常 | Android | P0 |
| IA-5 | ImportOverlay → applyOverlay | 导入状态正确合并到网格 | Android | P0 |
| IA-6 | ImportOverlay → rememberImportStatus | 导入后状态立即更新 | Android | P0 |
| IA-7 | ImportOverlay → forgetImportStatus | 删除后状态正确清除 | Android | P0 |
| IA-8 | ImportOverlay → markMovedToSystemTrash | 系统回收站标记正确 | Android | P0 |
| IA-9 | OperationBus → publishOperationSummaryIfReady | 操作结果事件正确发布 | Android | P0 |
| IA-10 | OperationBus → dismissOperationResult | 事件可正确移除 | Android | P1 |
| IA-11 | UploadManager → enqueueImportToAppUpload | 导入照片流上传正常 | Android | P0 |
| IA-12 | UploadManager → enqueueCreatePostUpload | 新建小相册上传正常 | Android | P0 |
| IA-13 | UploadManager → enqueueAddToExistingPostUpload | 加入小相册上传正常 | Android | P0 |
| IA-14 | UploadManager → pause/cancel/dismiss/retry | 上传任务生命周期管理正常 | Android | P0 |
| IA-15 | UploadManager → 持久化（warmPersistentUploadTasks） | 重启后任务恢复 | Android | P0 |
| IA-16 | UploadManager → 远程历史合并 | 远端历史正确合并 | Android | P1 |
| IA-17 | LocalSystemMediaBridgeRepository → 外部调用方（11 个文件） | 所有外部调用零改动 | Android | P0 |
| IA-18 | SystemMediaScreen → States | 空/加载/错误/权限态正常 | Android | P0 |
| IA-19 | SystemMediaScreen → Selection | 多选/滑选/底部操作栏正常 | Android | P0 |
| IA-20 | SystemMediaScreen → Grid | 网格/卡片/密度切换/Ghost Overlay 正常 | Android | P0 |
| IA-21 | SystemMediaScreen → Scrubber | 时间滑条 helper 正常 | Android | P0 |
| IA-22 | SystemMediaScreen → Components | TopBar / AtmosphereLayer 正常 | Android | P1 |
| IA-23 | SystemMediaScreen → 16 列模式 | 16 列特殊处理保持 | Android | P0 |
| IA-24 | SystemMediaViewerScreen → Canvas | 图片/视频/长图渲染正常 | Android | P0 |
| IA-25 | SystemMediaViewerScreen → Controls | 视频控制/信息卡/菜单正常 | Android | P0 |
| IA-26 | SystemMediaViewerScreen → 沉浸模式 | 单击切换沉浸正常 | Android | P1 |
| IA-27 | SystemMediaViewerScreen → HorizontalPager | 翻页正常 | Android | P0 |
| IA-28 | ViewerZoomState → 07 模块 | 07 模块 PhotoViewerScreen 不受影响 | Android | P0 |
| IA-29 | paletteForPickedMedia → 视觉变化 | picked 媒体颜色分布变化可接受 | Android | P1 |
| IA-30 | 死代码 SystemMediaFilterChip / SystemMediaTimeScrubber | 拆分后仍存在（不删除） | Android | P2 |

---

## 三、后端影响链

**无**。本轮纯 Android 变更，服务端无任何改动。所有 API 契约不变。

---

## 四、跨模块影响

### 4.1 直接影响（A 类：使用 LocalSystemMediaBridgeRepository 的模块）

| 模块 | 文件 | 影响 | 测试要点 |
|------|------|------|---------|
| 传输中心 | TransferCenterScreen.kt | 通过薄代理访问 uploadTasks / cancel / pause / retry | 上传任务列表/暂停/取消/重试正常 |
| 照片流 | RealPhotoFeedViewModel.kt | forgetImportStatusByAppMediaId | 删除 App 媒体后系统媒体标记清除 |
| 回收站 | RealTrashViewModels.kt | forgetImportStatusByAppMediaId(s) | 回收站恢复后系统媒体标记更新 |
| Post 编辑 | PostComposerViewModel.kt | enqueueCreatePostUpload / createPostFromSystemMediaDraft | 创建小相册上传正常 |
| 路由 | YingShiApp.kt + YingShiAppHelpers.kt | operationResults / enqueueImportToAppUpload / dismissOperationResult | 操作结果通知正常 |

### 4.2 间接影响（B 类：使用共享 ViewerZoomState 的模块）

| 模块 | 文件 | 影响 | 测试要点 |
|------|------|------|---------|
| 照片查看器 | PhotoViewerScreen.kt（07 模块） | ViewerZoomState.kt 追加 systemViewerZoomGesture，不影响 viewerZoomGesture | 07 模块缩放/双击/弹性回弹正常 |

### 4.3 共享组件影响

| 共享组件 | 本轮是否改动 | 影响 |
|---------|-------------|------|
| ViewerZoomState | ✅ 追加 systemViewerZoomGesture | 仅新增内容，不改动现有 ViewerZoomState 类和 viewerZoomGesture |
| PhotoFeedTimeScrubber | ❌ 不改动 | — |
| PhotoFeedDensity | ❌ 不改动 | — |
| MultiSelectSwipeGesture | ❌ 不改动 | — |
| DiscreteZoomGesture | ❌ 不改动 | — |

---

## 五、风险与缓解

| # | 风险 | 严重度 | 影响路径 | 缓解措施 |
|---|------|--------|---------|---------|
| 1 | OperationBus provider 初始化时机 | 中 | IA-9, IA-10 | 在 LocalSystemMediaBridgeRepository 静态初始化块中设置 provider |
| 2 | UploadManager 内部引用 ImportOverlayStore / OperationBus 方法名冲突 | 中 | IA-11~13 | 使用 fully qualified 引用 |
| 3 | paletteForPickedMedia 4→6 组视觉变化 | 低 | IA-29 | 前 4 组不变，新增 2 组轮换 |
| 4 | systemViewerZoomGesture 迁出后与 07 viewerZoomGesture 共存 | 低 | IA-28 | 保留不同名称 |
| 5 | 主函数胶水代码提取后行为变化 | 中 | IA-19~21 | remember helper 保持原有闭包语义 |
| 6 | 死代码处理（不删除） | 低 | IA-30 | 标注 TODO，不影响功能 |
