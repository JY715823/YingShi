# Round 4 影响分析

> 日期：2026-07-14
> 本轮 FR：FR-7 测试骨架搭建

---

## 一、变更文件影响分析

### 文件 1: SystemMediaViewModel.kt

**变更**: L256 `private` → `internal`

**直接影响**:
- 同 module 内的所有代码现在可以访问 `applyFilter`
- 但 `applyFilter` 是顶层扩展函数，仅在 `SystemMediaViewModel.kt` 文件内被 `publishState` 调用
- 测试文件 `SystemMediaViewModelTest.kt` 将新增调用

**间接影响**:
- 无。internal 在 module 内可见，但其他生产代码不会主动调用此函数（它是筛选工具函数，不是公共 API）

**影响路径清单**:
| IA# | 路径 | 影响 | 测试 |
|-----|------|------|------|
| IA-1 | publishState 调用 applyFilter | 无行为变更，仅可见性扩大 | 编译验证 + 现有功能回归 |
| IA-2 | 测试文件调用 applyFilter | 新增调用，预期行为 | SystemMediaViewModelTest 18 个用例 |

---

### 文件 2: SystemMediaTestFixtures.kt (新建)

**直接影响**:
- 新增文件，无对现有代码的影响
- 提供 internal 工具函数，仅供测试使用

**间接影响**:
- 无。该文件位于 test sourceSet，不参与生产编译

---

### 文件 3: SystemMediaViewModelTest.kt (新建)

**直接影响**:
- 新增测试文件，不修改任何生产代码
- 测试 applyFilter 纯函数

**间接影响**:
- 无。纯新增测试

---

### 文件 4: SystemMediaImportPreviewTest.kt (修改)

**变更**:
- 删除 private `sampleSystemMediaItem` 函数（L150-182）
- 改为引用 `SystemMediaTestFixtures.sampleSystemMediaItem`
- 新增 7 个测试用例

**直接影响**:
- 现有 5 个用例的 `sampleSystemMediaItem` 调用现在解析到 TestFixtures 中的函数
- 参数签名兼容（TestFixtures 版本参数是原版本的超集，所有默认值一致）

**间接影响**:
- 无。测试文件修改不影响生产代码

**影响路径清单**:
| IA# | 路径 | 影响 | 测试 |
|-----|------|------|------|
| IA-3 | 现有 5 个用例引用 sampleSystemMediaItem | 函数引用重定向到 TestFixtures，参数兼容 | 重新运行现有 5 个用例确认 PASS |
| IA-4 | 新增 7 个用例 | 新增测试预期 | 运行新增用例确认 PASS |

---

## 二、必须测试的影响路径清单

| IA# | 影响路径 | 测试方式 | 优先级 |
|-----|---------|---------|--------|
| IA-1 | applyFilter 可见性变更不影响 publishState 行为 | 编译验证 + testDebugUnitTest 全量 | P0 |
| IA-2 | applyFilter 在测试中被调用 | SystemMediaViewModelTest 18 个用例 | P0 |
| IA-3 | sampleSystemMediaItem 引用重定向 | SystemMediaImportPreviewTest 现有 5 个用例重跑 | P0 |
| IA-4 | 新增 ImportPreview 测试用例 | SystemMediaImportPreviewTest 新增 7 个用例 | P0 |

---

## 三、跨模块影响

**无跨模块影响**。所有变更仅涉及:
- 1 个生产文件（仅改可见性关键字）
- 3 个测试文件（新建 2 + 修改 1）

不涉及任何其他模块的接口、数据模型、或共享组件。
