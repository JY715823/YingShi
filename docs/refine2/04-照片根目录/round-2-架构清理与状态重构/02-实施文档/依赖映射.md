# Round 2 依赖映射

> 日期: 2026-07-09
> 本轮主题: 架构清理与状态重构

---

## 本模块依赖的外部模块/服务

| 依赖模块 | 本轮改动接口 | 说明 |
|---------|-------------|------|
| TrashService (Server) | **是** — 删除 systemDeleteMediaAllowingEmptySmallAlbums | 方法删除，调用方改为 systemDeleteMedia |
| LifeConsoleService (Server) | **是** — 修改调用方 | 方法名变更，行为不变 |
| PhotoFeedInteractionModels (Android) | **是** — 新增 3 个 data class | 纯新增，向后兼容 |
| RepositoryProvider (Android) | 否 | 仅读取 currentMode 判断 FAKE/REAL |
| FakeAlbumRepository (Android) | 否 | FAKE 模式下继续使用 |
| FakePhotoFeedRepository (Android) | 否 | FAKE 模式下继续使用 |
| FakeTrashRepository (Android) | 否 | FAKE 模式下继续使用 |
| RealPhotoFeedPage (Android) | 否 | REAL 模式下删除委托给它 |

---

## 依赖本模块的外部调用方

| 调用方 | 本轮是否影响 | 说明 |
|--------|-------------|------|
| YingShiApp.kt | **是** — 参数重组 | PhotosRootScreen 调用处需同步修改 |
| LifeConsoleService.java | **是** — 方法名变更 | systemDeleteMediaAllowingEmptySmallAlbums → systemDeleteMedia |

---

## 前后端共享组件依赖

| 组件 | 是否受影响 | 说明 |
|------|-----------|------|
| YingShiSurfaces | 否 | 不修改 |
| AppShell | 否 | 不修改 |
| YingShiMotion | 否 | 不修改 |
| YingShiAuroraBackdrop | 否 | 不修改 |
| YingShiNoticeHost | 否 | 不修改 |

---

## 数据流图

```
┌─────────────────────────────────────────────────┐
│                YingShiApp.kt                     │
│  ┌───────────────────────────────────────────┐  │
│  │           PhotosRootScreen(...)            │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │         FR-6: dialogState           │  │  │
│  │  │  ┌───────────────────────────────┐  │  │  │
│  │  │  │  FR-4: isFakeMode ?           │  │  │  │
│  │  │  │  ├─ FAKE: FakeAlbumRepo       │  │  │  │
│  │  │  │  │  · delete / addToPost      │  │  │  │
│  │  │  │  └─ REAL: RealPhotoFeedPage   │  │  │  │
│  │  │  │     · delete / addToPost      │  │  │  │
│  │  │  └───────────────────────────────┘  │  │  │
│  │  │  FR-7: trashParams / selectionParams │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              Server (FR-5)                       │
│  TrashService.systemDeleteMedia(mediaId, user)  │
│    └─> LifeConsoleService (调用方改为该方法)     │
│  [删除] systemDeleteMediaAllowingEmptySmallAlbums│
└─────────────────────────────────────────────────┘
```

---

## 回归测试范围

### 前端回归测试

| 测试项 | 优先级 | 说明 |
|--------|--------|------|
| 删除确认弹窗打开/关闭 | P0 | dialogState 重构后弹窗状态正确 |
| 删除操作（FAKE 模式） | P0 | 删除功能正常，回收站写入正常 |
| 加帖弹窗打开/关闭 | P0 | 加帖功能正常 |
| 新建大相册弹窗 | P0 | 创建功能正常 |
| 确认/销毁弹窗 | P0 | 弹窗打开/关闭正常 |
| 编译通过 | P0 | Android + Server 编译 |
| 三个 tab 切换 | P1 | 切换正常 |
| 多选态 | P1 | 多选进入/退出正常 |
| REAL 模式数据加载 | P1 | 不加载 Fake 数据 |
| FAKE 模式数据加载 | P1 | 正常加载 Fake 数据 |

### 后端回归测试

| 测试项 | 优先级 | 说明 |
|--------|--------|------|
| Server 编译通过 | P0 | mvnw.cmd compile |
| LifeConsole 删除功能 | P1 | 媒体删除正常 |

---

## 跨模块回归测试

| 测试项 | 涉及模块 | 优先级 | 说明 |
|--------|---------|--------|------|
| YingShiApp 壳层正常工作 | 15-外壳 | P0 | 参数传递正确 |
| 照片流多选删除 | 03-照片流 | P0 | REAL 模式下删除委托正常 |
| 回收站 Tab 正常 | 09-回收站 | P1 | trashParams 透传正确 |
| LifeConsole 功能 | 12-生活首页 | P1 | 删除功能正常 |