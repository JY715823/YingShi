# Round 4 测试计划

> 轮次: Round 4 — 服务端性能修复
> 日期: 2026-07-09
> 测试范围: FR-12 (N+1 查询修复)、FR-13 (真分页)

---

## 一、编译/基础设施测试

### TC-01: Server 编译通过
- 对应: FR-12/AC-5, FR-13/AC-5
- 端: Server
- 前置条件: 代码已修改
- 步骤: `./mvnw.cmd compile`
- 预期结果: BUILD SUCCESS，exit code 0
- 通过标准: 零编译错误

### TC-02: Android 编译通过
- 对应: NFR-3
- 端: Android
- 前置条件: 代码无变更
- 步骤: `./gradlew.bat assembleDebug`
- 预期结果: BUILD SUCCESSFUL，exit code 0
- 通过标准: 零编译错误

### TC-03: Docker 容器健康
- 对应: NFR-3
- 端: Server
- 前置条件: Docker Desktop 运行中
- 步骤: `docker compose up -d`，等待启动后 `curl localhost:8080/actuator/health`
- 预期结果: 所有容器 healthy，health endpoint 返回 UP
- 通过标准: 容器状态 healthy

---

## 二、FR-12 功能测试

### TC-10: resolveFirstVisibleMediaId — 批量查询逻辑正确性
- 对应: FR-12/AC-1
- 端: Server
- 前置条件: 代码已修改，编译通过
- 步骤: 代码审查确认：`resolveFirstVisibleMediaId` 方法中调用了 `findByLibraryIdAndIdInAndDeletedAtIsNull` 批量查询，而非 for-loop 逐条查询
- 预期结果: 方法体包含批量查询调用，不存在 for-loop 内 `findByIdAndLibraryIdAndDeletedAtIsNull` 的逐条调用
- 通过标准: 代码审查确认批量查询替代逐条查询

### TC-11: resolveFirstVisibleMediaId — excludedMediaId 过滤
- 对应: FR-12/AC-1, IA-2
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：`excludedMediaId` 在 mediaIds 收集和遍历 relations 时均被正确过滤
- 预期结果: `excludedMediaId` 被排除在 mediaIds 收集之外，且在遍历时被 continue 跳过
- 通过标准: 代码审查确认两个过滤点均存在

### TC-12: resolveFirstVisibleMediaId — 空 relations 边界
- 对应: FR-12/AC-1
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：当 relations 过滤后 mediaIds 为空时，直接返回 `Optional.empty()`
- 预期结果: 不执行批量查询，直接返回 empty
- 通过标准: 代码审查确认空集合早期返回

### TC-13: purgeMediaRemovedItemsForSmallAlbum — SQL 层过滤
- 对应: FR-12/AC-2
- 端: Server
- 前置条件: 代码已修改，编译通过
- 步骤: 代码审查确认：`purgeMediaRemovedItemsForSmallAlbum` 调用了 `findByLibraryIdAndStateAndItemTypeAndSourcePostId`，而非 `findByLibraryIdAndStateAndItemType` + Java filter
- 预期结果: 方法体调用新 Repository 方法，不存在 `stream().filter()` 的 Java 层过滤
- 通过标准: 代码审查确认 SQL 层过滤替代 Java 层过滤

### TC-14: purgeMediaRemovedItemsForSmallAlbum — 空结果边界
- 对应: FR-12/AC-2, IA-3
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：查询结果为空时跳过 `deleteAll`
- 预期结果: 存在 `if (!mediaRemovedItems.isEmpty())` 判断
- 通过标准: 代码审查确认空结果保护

### TC-15: TrashItemRepository — 新增方法存在
- 对应: FR-12/AC-3
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `TrashItemRepository` 接口中存在 `findByLibraryIdAndStateAndItemTypeAndSourcePostId` 方法
- 预期结果: 方法签名正确：4 个参数（libraryId, state, itemType, sourcePostId），返回 `List<TrashItemEntity>`
- 通过标准: 代码审查确认方法定义存在且签名正确

### TC-16: MediaRepository — 批量查询方法可用
- 对应: FR-12/AC-4
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `resolveFirstVisibleMediaId` 中调用了 `mediaRepository.findByLibraryIdAndIdInAndDeletedAtIsNull`
- 预期结果: 调用的方法与 MediaRepository 中已存在的方法签名匹配
- 通过标准: 编译通过确认方法存在

---

## 三、FR-13 功能测试

### TC-20: UploadController — cursor 参数存在
- 对应: FR-13/AC-1
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `listUploads` 方法签名包含 `@RequestParam(required = false) String cursor` 参数
- 预期结果: cursor 参数存在且为 optional
- 通过标准: 代码审查确认参数定义

### TC-21: listUploadHistory — 分页大小控制
- 对应: FR-13/AC-1, IA-4
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：`PageRequest.of(0, pageSize + 1)` 查询 pageSize+1 条记录
- 预期结果: 查询 pageSize+1 条，若返回 > pageSize 条则截断到 pageSize 条
- 通过标准: 代码审查确认分页逻辑正确

### TC-22: listUploadHistory — hasMore 判断
- 对应: FR-13/AC-2, IA-4
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：`hasMore = entities.size() > pageSize`
- 预期结果: 当查询结果条数 > pageSize 时 hasMore=true，否则 false
- 通过标准: 代码审查确认 hasMore 逻辑

### TC-23: listUploadHistory — nextCursor 生成
- 对应: FR-13/AC-2, IA-5
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：nextCursor = `last.getUpdatedAt().toEpochMilli() + ":" + last.getId()`
- 预期结果: nextCursor 格式为 `{epochMillis}:{id}`；hasMore=false 时 nextCursor=null
- 通过标准: 代码审查确认 cursor 生成逻辑

### TC-24: listUploadHistory — 不传 cursor 向后兼容
- 对应: FR-13/AC-3, IA-6, IA-7
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：cursor 参数在方法签名中接收但不参与过滤逻辑，不传 cursor 时行为与之前一致
- 预期结果: cursor 参数不影响查询结果，仅用于 API 接口保留
- 通过标准: 代码审查确认向后兼容

### TC-25: UploadController — PageInfo 正确填充
- 对应: FR-13/AC-2, IA-5
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认：`new PageInfo(1, result.items().size(), result.nextCursor(), result.hasMore())`
- 预期结果: PageInfo 的 pageSize 为实际返回条数，nextCursor 和 hasMore 来自 Service 返回值
- 通过标准: 代码审查确认不再使用硬编码 `null, false`

### TC-26: UploadTaskRepository — findVisibleHistoryPage 方法存在
- 对应: FR-13/AC-1
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `UploadTaskRepository` 接口中存在 `findVisibleHistoryPage` 方法，接受 `Pageable` 参数
- 预期结果: 方法签名包含 `Pageable pageable` 参数
- 通过标准: 代码审查确认方法定义存在

### TC-27: UploadService — UploadHistoryResult record 存在
- 对应: FR-13/AC-2
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `UploadService` 中存在 `UploadHistoryResult` record，包含 items、nextCursor、hasMore 字段
- 预期结果: record 定义正确
- 通过标准: 代码审查确认 record 存在

---

## 四、影响范围测试

### TC-30: dismissUploadBatch 未受影响
- 对应: IA-9
- 端: Server
- 前置条件: 代码已修改
- 步骤: 代码审查确认 `dismissUploadBatch` 仍调用 `findVisibleHistory`（旧方法），而非 `findVisibleHistoryPage`
- 预期结果: dismissUploadBatch 逻辑不变
- 通过标准: 代码审查确认旧调用方未受影响

### TC-31: Android UploadApi 兼容性
- 对应: IA-9
- 端: Android
- 前置条件: 后端代码已修改
- 步骤: 代码审查确认 `UploadApi.getUploadHistory` 不传 cursor 参数，返回 `ApiEnvelopeDto<List<UploadTaskDto>>`
- 预期结果: Android 端无需修改，编译通过
- 通过标准: `gradlew.bat assembleDebug` 编译通过

---

## 五、跨模块回归测试

### TC-40: TrashController 所有端点行为不变
- 对应: 依赖映射-回归范围
- 端: Server
- 前置条件: 编译通过
- 步骤: 代码审查确认 TrashService 的 public 方法签名（restoreMedia、restoreMediaToPost、systemDeleteMedia 等）未变更
- 预期结果: 所有 public 方法签名与改动前一致
- 通过标准: 代码审查确认 API 契约不变

### TC-41: UploadController 其他端点行为不变
- 对应: 依赖映射-回归范围
- 端: Server
- 前置条件: 编译通过
- 步骤: 代码审查确认除 listUploads 外的其他端点（createUploadToken、uploadFile、getUploadTask、confirmUpload、cancelUpload、dismissUpload、dismissUploadBatch）未变更
- 预期结果: 所有其他端点方法体不变
- 通过标准: 代码审查确认无意外变更

---

## 测试用例汇总

| # | 测试名 | 端 | 类型 | 关联 |
|---|--------|-----|------|------|
| TC-01 | Server 编译通过 | Server | 基础设施 | FR-12/AC-5, FR-13/AC-5 |
| TC-02 | Android 编译通过 | Android | 基础设施 | NFR-3 |
| TC-03 | Docker 容器健康 | Server | 基础设施 | NFR-3 |
| TC-10 | resolveFirstVisibleMediaId 批量查询 | Server | 功能 | FR-12/AC-1 |
| TC-11 | excludedMediaId 过滤 | Server | 功能 | IA-2 |
| TC-12 | 空 relations 边界 | Server | 功能 | FR-12/AC-1 |
| TC-13 | purgeMediaRemoved SQL 过滤 | Server | 功能 | FR-12/AC-2 |
| TC-14 | 空结果边界 | Server | 功能 | IA-3 |
| TC-15 | TrashItemRepository 新增方法 | Server | 功能 | FR-12/AC-3 |
| TC-16 | MediaRepository 批量查询可用 | Server | 功能 | FR-12/AC-4 |
| TC-20 | cursor 参数存在 | Server | 功能 | FR-13/AC-1 |
| TC-21 | 分页大小控制 | Server | 功能 | IA-4 |
| TC-22 | hasMore 判断 | Server | 功能 | FR-13/AC-2 |
| TC-23 | nextCursor 生成 | Server | 功能 | IA-5 |
| TC-24 | 不传 cursor 向后兼容 | Server | 功能 | FR-13/AC-3, IA-6 |
| TC-25 | PageInfo 正确填充 | Server | 功能 | FR-13/AC-2 |
| TC-26 | findVisibleHistoryPage 方法存在 | Server | 功能 | FR-13/AC-1 |
| TC-27 | UploadHistoryResult record 存在 | Server | 功能 | FR-13/AC-2 |
| TC-30 | dismissUploadBatch 未受影响 | Server | 影响范围 | IA-9 |
| TC-31 | Android UploadApi 兼容性 | Android | 影响范围 | IA-9 |
| TC-40 | TrashController 行为不变 | Server | 回归 | 依赖映射 |
| TC-41 | UploadController 其他端点不变 | Server | 回归 | 依赖映射 |

总用例: 21