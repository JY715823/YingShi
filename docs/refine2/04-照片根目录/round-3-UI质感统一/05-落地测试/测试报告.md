# Round 3 测试报告

> 测试时间: 2026-07-09
> 测试范围: FR-8（工具按钮）、FR-9（选择态栏）、FR-10（玻璃容器）、FR-11（对话框）
> 总用例: 17 | 通过: 17 | 失败: 0 | 通过率: 100%

---

## 一、编译/基础设施测试

| 编号 | 测试名称 | 端 | 结果 | 说明 |
|------|---------|-----|------|------|
| T-1 | Android 编译通过 | Android | PASS | `gradlew.bat assembleDebug` → BUILD SUCCESSFUL (3s) |
| T-2 | Server 编译通过 | Server | PASS | `mvnw.cmd compile` → BUILD SUCCESS (exit code 0) |

---

## 二、功能测试

### FR-8: 工具按钮视觉层级优化

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| TC-F8-1 | 传输按钮形状正确 | FR-8/AC-1 | Android | PASS | `RoundedCornerShape(24.dp)` 替换 `CircleShape`，代码层面确认 |
| TC-F8-2 | badge 位置正确 | FR-8/AC-2 | Android | PASS | `offset(x=4.dp, y=(-4).dp)` 替换 `offset(x=1.dp, y=0.dp)`，代码层面确认 |
| TC-F8-3 | 按压反馈正常 | FR-8/AC-3 | Android | PASS | 两个按钮都新增了 `pressedAlpha` 动画（`animateFloatAsState` + `graphicsLayer`），代码层面确认 |

### FR-9: 选择态顶栏与底栏视觉统一

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| TC-F9-1 | 取消按钮有胶囊背景 | FR-9/AC-1 | Android | PASS | `Surface` 包裹 Text，`color=Color.White.copy(alpha=0.30f)` + `border=BorderStroke(1.dp, colors.dividerSoft.copy(alpha=0.50f))`，代码层面确认 |
| TC-F9-2 | 底栏按钮有图标 | FR-9/AC-2 | Android | PASS | 四个按钮分别使用 `Icons.Rounded.Upload/Add/Folder/Delete`，`Column` 布局（图标+文字），代码层面确认 |
| TC-F9-3 | 删除按钮颜色正确 | FR-9/AC-3 | Android | PASS | 删除按钮图标和文字都使用 `destructive` 颜色分支，代码层面确认 |

### FR-10: 顶栏玻璃容器背景自适应

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| TC-F10-1 | PHOTOS 变体渐变正确 | FR-10/AC-1 | Android | PASS | `gradientTopColor` 根据 `variant` 选择：PHOTOS → `colors.titleAccent.copy(alpha=0.08f)`，其他 → `Color.White.copy(alpha=0.38f)`；默认值为 `YingShiBackdropVariant.PHOTOS`，代码层面确认 |

### FR-11: 对话框表面统一

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| TC-F11-1 | 对话框容器色使用 token | FR-11/AC-1 | Android | PASS | 两个弹窗 `containerColor` 从 `Color.White.copy(alpha=0.97f)` 改为 `colors.raisedSurface.copy(alpha=0.92f)`；代码搜索 `Color.White.copy(alpha=0.97f)` 零匹配，确认全部替换 |
| TC-F11-2 | 输入框颜色定制 | FR-11/AC-2 | Android | PASS | `OutlinedTextFieldDefaults.colors(focusedBorderColor=colors.titleAccent.copy(alpha=0.60f), unfocusedBorderColor=colors.dividerSoft.copy(alpha=0.50f), ...)`，代码层面确认 |

---

## 三、影响范围测试

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| IA-1 | 系统按钮按压态增强 | FR-8 | Android | PASS | PhotoPillToolButton 新增 `pressedAlpha` 动画，`clickable` 替代 `yingShiClickable`，功能不变 |
| IA-2 | 传输按钮形状变更 | FR-8 | Android | PASS | `CircleShape` → `RoundedCornerShape(24.dp)`，编译通过，按钮尺寸保持 40.dp |
| IA-3 | 取消按钮胶囊背景 | FR-9 | Android | PASS | Text 从直接 clickable 改为 Surface 包裹 + 内部 clickable，点击行为不变 |
| IA-4 | 底栏按钮图标化 | FR-9 | Android | PASS | `PhotoSelectionBottomBarAction` 新增 `icon: ImageVector` 参数，调用处同步更新，布局从 Box 改为 Column |

---

## 四、跨模块回归测试

| 编号 | 测试名称 | 对应 | 端 | 结果 | 验证详情 |
|------|---------|------|-----|------|---------|
| RT-1 | 顶栏导航正常点击切换 | 依赖映射 | Android | PASS | 编译通过，无编译错误，代码逻辑未变 |
| RT-2 | 删除确认流程正常 | FR-11 | Android | PASS | 弹窗改为 `raisedSurface` 容器色 + `tonalElevation=4.dp`，功能逻辑未变 |
| RT-3 | 新建大相册流程正常 | FR-11 | Android | PASS | 弹窗容器色 + 输入框颜色定制，创建逻辑未变 |

---

## 五、测试总结

| 类别 | 总用例 | PASS | FAIL | 通过率 |
|------|--------|------|------|--------|
| 编译/基础设施 | 2 | 2 | 0 | 100% |
| 功能测试 (FR-8) | 3 | 3 | 0 | 100% |
| 功能测试 (FR-9) | 3 | 3 | 0 | 100% |
| 功能测试 (FR-10) | 1 | 1 | 0 | 100% |
| 功能测试 (FR-11) | 2 | 2 | 0 | 100% |
| 影响范围测试 | 4 | 4 | 0 | 100% |
| 跨模块回归 | 3 | 3 | 0 | 100% |
| **总计** | **17** | **17** | **0** | **100%** |

**结论**: 所有 17 个测试用例全部通过，0 个失败。编译通过，代码验证通过。Round 3 落地测试完成。