# Round 3 依赖映射

> 日期: 2026-07-09
> 基于实施计划 v1.0 + 影响分析 v1.0

---

## 一、本模块依赖的外部模块/服务

| 依赖模块 | 依赖方向 | 依赖内容 | 本轮是否改动接口 |
|---------|---------|---------|---------------|
| 15-外壳与基础设备 | 本模块→15 | YingShiThemeTokens（colors, spacing, motion） | 否（仅读取 token） |
| 15-外壳与基础设备 | 本模块→15 | yingShiClickable Modifier | 否（仅调用） |
| — | — | YingShiAuroraBackdrop variant 枚举 | 否（仅读取） |

注：本轮不涉及服务端改动。

---

## 二、依赖本模块的外部调用方

| 调用方 | 依赖内容 | 本轮是否影响 |
|--------|---------|------------|
| YingShiApp.kt | PhotosRootScreen 函数调用 | 否（函数签名不变） |

---

## 三、前后端共享组件依赖

| 共享组件 | 是否受影响 | 说明 |
|---------|----------|------|
| YingShiThemeTokens.colors | 否 | 仅读取已有 token，不修改 |
| YingShiThemeTokens.motion | 否 | 仅读取 tapMillis |
| yingShiClickable | 否 | 仅调用，不修改 |
| YingShiAuroraBackdrop | 否 | 仅读取 variant 枚举值 |
| Material3 AlertDialog | 是 | 修改弹窗参数（containerColor/border/tonalElevation） |
| Material3 OutlinedTextField | 是 | 定制输入框颜色 |
| SelectionActionChip | 否 | 仅调用，不修改 |
| Icons.Rounded.* | 否 | 新增引用 Upload/Add/Folder/Delete 图标 |

---

## 四、数据流图

```
PhotosRootScreen (FR-8~11 全部在本文件内)
  │
  ├─ PhotoTopBar
  │   ├─ [正常模式] PhotoTopGlassSurface(variant=PHOTOS)  ← FR-10
  │   │   ├─ PhotoPillToolButton("系统")                   ← FR-8
  │   │   └─ PhotoCircleToolButton("传输")                 ← FR-8
  │   └─ [选择模式] "取消"按钮 + "已选 N 项"               ← FR-9
  │
  ├─ 选择态底栏
  │   └─ PhotoSelectionBottomBarAction × 4                 ← FR-9
  │       (分享/新建/加入/删除)
  │
  ├─ AlertDialog: 删除确认弹窗                             ← FR-11
  └─ AlertDialog: 新建大相册弹窗                            ← FR-11
```

---

## 五、回归测试范围

由于所有变更仅在 PhotosRootScreen.kt 单文件内，且均为纯视觉改动，回归测试范围：

| 测试范围 | 说明 |
|---------|------|
| Android 编译 | `gradlew.bat assembleDebug` 通过 |
| 正常模式顶栏 | 系统按钮 + 传输按钮正常显示和点击 |
| 选择态顶栏 | 取消按钮正常显示和点击，已选 N 项文字正常 |
| 选择态底栏 | 四个按钮（分享/新建/加入/删除）正常显示和点击 |
| 玻璃容器 | 顶栏玻璃容器外观正常 |
| 删除确认弹窗 | 打开/关闭/确认操作正常 |
| 新建大相册弹窗 | 打开/关闭/输入/创建操作正常 |
| 传输按钮 badge | 传输中/传输失败 badge 正常显示 |