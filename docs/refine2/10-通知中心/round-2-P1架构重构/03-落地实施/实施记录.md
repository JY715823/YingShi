# Round 2 实施记录

> 实施时间: 2026-07-16
> 包含 FR: FR-4 (模型提取) → FR-2 (ViewModel 提取) → FR-3 (文件拆分) → FR-7 (附带清理)

## 变更摘要

| # | 文件 | 端 | 变更类型 | 说明 |
|---|------|-----|---------|------|
| 1 | `NotificationModels.kt` | Android | **新建** | 公共模型 + 枚举 + 扩展函数（~180行） |
| 2 | `NotificationCenterViewModel.kt` | Android | **新建** | ViewModel + UiState + 业务逻辑（~556行） |
| 3 | `NotificationCenterComponents.kt` | Android | **新建** | 19 个 Composable UI 组件（~600行） |
| 4 | `NotificationCenterPresentation.kt` | Android | **新建** | 27 个视觉解析函数（~500行） |
| 5 | `FakeNotificationRepository.kt` | Android | 修改 | 移除模型 → 只保留 LocalStore + FakeRepo（~370行） |
| 6 | `NotificationCenterScreen.kt` | Android | 修改 | 使用 ViewModel，移除状态管理（2080→~600行） |
| 7 | `NotificationModels.kt` (FR-7) | Android | 修改 | 硬编码 ID 替换 + FakeAlbumRepository 解耦 |

## 详细变更

### FR-4: 通知模型文件重构

**新建**: `NotificationModels.kt`
- 从 `FakeNotificationRepository.kt` 提取 7 个数据模型/枚举 + 7 个扩展函数
- 内容: `NotificationCenterRoute`, `NotificationDetailRoute`, `NotificationCenterItemUiModel`, `NotificationCenterMediaItemUiModel`, `NotificationCenterItemType`, `NotificationCenterFilter`, `NotificationCategoryFilter`
- 扩展函数: `isLifeLedgerTarget`, `isLifeConsoleTarget`, `isLifeChatTarget`, `hasNotificationTargetAction`, `notificationTargetLabel`, `notificationActionLabel`, `notificationTargetDescription`

**修改**: `FakeNotificationRepository.kt` (543行 → 370行)
- 移除已提取的模型和扩展函数
- 保留: `NotificationCenterLocalStore`, `FakeNotificationRepository`, 转换函数

### FR-2: 提取 NotificationCenterViewModel

**新建**: `NotificationCenterViewModel.kt` (~556行)
- `NotificationCenterUiState` data class (18 字段)
- `NotificationResolvedPresentation` data class
- `NotificationVisual` sealed interface
- `NotificationCenterViewModel` class:
  - StateFlow: `uiState`, `selectedFilter`, `selectedCategory`, `includeSelfActor`, `includePartnerActor`
  - SharedFlow: `noticeEvent`
  - Companion: `NotificationCenterMemoryCache`
  - 方法: `init()`, `refresh()`, `markRead()`, `markAllRead()`, `clearList()`, `setFilter()`, `setCategory()`, `toggleSelfActor()`, `togglePartnerActor()`, `updateNotificationItem()`, `showClearListConfirm()`, `dismissClearListConfirm()`

**修改**: `NotificationCenterScreen.kt` (2080行 → ~600行)
- 移除: 状态管理（uiState/selectedFilterName/selectedCategoryName 等）
- 移除: 业务逻辑（refresh/markRead/markAllRead/clearList 等）
- 移除: 数据类定义（NotificationCenterUiState/NotificationResolvedPresentation/NotificationVisual/NotificationCenterMemoryCache）
- 新增: `val viewModel = viewModel<NotificationCenterViewModel>()`
- 新增: `LaunchedEffect` 收集 `viewModel.noticeEvent`
- 保留: 所有 Composable 函数（UI 渲染）

### FR-3: NotificationCenterScreen.kt 文件拆分

**新建**: `NotificationCenterComponents.kt` (~600行)
- 19 个 Composable 函数（全部 `internal`）
- 包括: NotificationCenterItemRow, NotificationMediaStrip, NotificationTypeChip, NotificationStatusBadge, NotificationCenterLoadingState, NotificationCenterEmptyState, NotificationDateSectionHeader, NotificationActionPill, NotificationFilterChip, NotificationActorChip 等

**新建**: `NotificationCenterPresentation.kt` (~500行)
- 27 个函数/数据类（全部 `internal`）
- 包括: 视觉呈现解析、日期格式化、筛选/分组、友好文案构建

**修改**: `NotificationCenterScreen.kt`
- 移除已提取到 Components.kt 和 Presentation.kt 的函数
- 只保留: `NotificationCenterScreen` Composable + `NotificationCenterTopBar` + `showNotice` + `ReconnectRefreshEffect`

### FR-7: 附带清理

**修改**: `NotificationModels.kt`
- `hasNotificationTargetAction()`: `id == "notice-cache-1"` → `targetSummary == "设置"`
- `notificationTargetLabel()`: `id == "notice-cache-1"` → `targetSummary == "设置"`
- `notificationActionLabel()`: `id == "notice-cache-1"` → `targetSummary == "设置"`
- `notificationTargetDescription()`: `id == "notice-cache-1"` → `targetSummary == "设置"`

**修改**: `FakeNotificationRepository.kt`
- `NotificationCenterLocalStore.pushPostCommentNotification()`: 新增 `postTitle: String = "当前小相册"` 参数，移除 `FakeAlbumRepository.getPost()` 调用
- `NotificationCenterLocalStore.pushMediaCommentNotification()`: 新增 `postTitle: String = "当前媒体"` 参数，移除 `FakeAlbumRepository` 调用
- `FakeNotificationRepository.pushPostCommentNotification()`: 同上
- `FakeNotificationRepository.pushMediaCommentNotification()`: 同上

## 编译验证

| 步骤 | 结果 |
|------|------|
| FR-4 编译 | BUILD SUCCESSFUL (56s) |
| FR-2 编译 | BUILD SUCCESSFUL |
| FR-3 编译 | BUILD SUCCESSFUL |
| FR-7 编译 | BUILD SUCCESSFUL (26s) |

## 完整性自检

| # | 实施计划项 | 状态 |
|---|-----------|------|
| 1 | NotificationModels.kt 新建 | ✅ |
| 2 | FakeNotificationRepository.kt 移除模型 | ✅ |
| 3 | NotificationCenterViewModel.kt 新建 | ✅ |
| 4 | NotificationCenterScreen.kt 使用 ViewModel | ✅ |
| 5 | NotificationCenterComponents.kt 新建 | ✅ |
| 6 | NotificationCenterPresentation.kt 新建 | ✅ |
| 7 | NotificationCenterScreen.kt 移除已提取函数 | ✅ |
| 8 | hasNotificationTargetAction 硬编码 ID 移除 | ✅ |
| 9 | notificationTargetLabel 硬编码 ID 移除 | ✅ |
| 10 | notificationActionLabel 硬编码 ID 移除 | ✅ |
| 11 | notificationTargetDescription 硬编码 ID 移除 | ✅ |
| 12 | pushPostCommentNotification FakeAlbumRepository 解耦 | ✅ |
| 13 | pushMediaCommentNotification FakeAlbumRepository 解耦 | ✅ |
| 14 | 全部编译通过 | ✅ |

**完整性**: 14/14 ✅