# Round 2 影响分析

> 基于实施计划: Round 2 — P1 架构重构

---

## 变更影响链

### FR-4: 通知模型文件重构

| 变更文件 | 直接影响 | 间接影响 |
|---------|---------|---------|
| **新建** NotificationModels.kt | 无（新文件） | 所有引用模型的 import 路径更新 |
| FakeNotificationRepository.kt | 删除已移出的模型（L1-L101, L470-L543） | 文件从 543 行减至约 300 行 |
| YingShiApp.kt | import 更新 | 无逻辑变更 |
| NotificationCenterScreen.kt | import 更新 | 无逻辑变更 |
| NotificationDetailScreen.kt | import 更新 | 无逻辑变更 |
| HomeScreen.kt | import 更新 | 无逻辑变更 |

**影响路径清单**:
- IA-1: FakeNotificationRepository.kt 编译通过（不依赖已移出的模型）
- IA-2: YingShiApp.kt 编译通过（import 更新正确）
- IA-3: NotificationCenterScreen.kt 编译通过（import 更新正确）
- IA-4: NotificationDetailScreen.kt 编译通过（import 更新正确）
- IA-5: HomeScreen.kt 编译通过（import 更新正确）

### FR-2: 提取 NotificationCenterViewModel

| 变更文件 | 直接影响 | 间接影响 |
|---------|---------|---------|
| **新建** NotificationCenterViewModel.kt | 无（新文件） | 后续 FR-3 拆分时使用 |
| NotificationCenterScreen.kt | 状态管理移至 ViewModel | Composable 通过 `viewModel.uiState.collectAsState()` 获取状态 |

**影响路径清单**:
- IA-6: NotificationCenterScreen.kt 编译通过（ViewModel 集成正确）
- IA-7: 状态管理正确（refresh/markRead/markAllRead/filter 切换功能正常）

### FR-3: 文件拆分

| 变更文件 | 直接影响 | 间接影响 |
|---------|---------|---------|
| NotificationCenterScreen.kt | 删除已移出的 Composable 和函数 | 文件从 ~1100 行减至约 600 行 |
| **新建** NotificationCenterComponents.kt | 无（新文件） | 包含约 20 个 Composable |
| **新建** NotificationCenterPresentation.kt | 无（新文件） | 包含约 30 个函数 |

**影响路径清单**:
- IA-8: NotificationCenterScreen.kt 编译通过（引用拆分的文件正确）
- IA-9: NotificationCenterComponents.kt 编译通过
- IA-10: NotificationCenterPresentation.kt 编译通过

### FR-7: 附带清理

| 变更文件 | 直接影响 | 间接影响 |
|---------|---------|---------|
| NotificationModels.kt | hasNotificationTargetAction 等4个函数逻辑变更 | NotificationDetailScreen.kt 判断逻辑受影响 |
| FakeNotificationRepository.kt | pushPostCommentNotification/pushMediaCommentNotification 参数变更 | 调用方需更新 |

**影响路径清单**:
- IA-11: hasNotificationTargetAction 不再依赖硬编码 ID
- IA-12: pushPostCommentNotification 不再耦合 FakeAlbumRepository
- IA-13: pushMediaCommentNotification 不再耦合 FakeAlbumRepository

---

## 前端影响链

### Composable 重构影响

```
NotificationCenterScreen.kt (主 Composable)
  ├── ViewModel.uiState.collectAsState()  (FR-2 变更)
  ├── NotificationCenterTopBar            (保留在 Screen)
  ├── NotificationCenterItemRow           (→ Components.kt)
  │   ├── NotificationMediaStrip          (→ Components.kt)
  │   ├── NotificationVisualPane          (→ Components.kt)
  │   ├── NotificationTypeChip            (→ Components.kt)
  │   └── NotificationStatusBadge         (→ Components.kt)
  ├── NotificationCenterLoadingState      (→ Components.kt)
  ├── NotificationCenterEmptyState        (→ Components.kt)
  └── 视觉解析函数                         (→ Presentation.kt)
```

### 共享组件依赖

| 共享组件 | 是否受影响 | 说明 |
|---------|-----------|------|
| AppShell | 否 | 路由不变 |
| YingShiThemeTokens | 否 | 无 UI 视觉变更 |
| YingShiNotice | 否 | 通知提示不变 |

---

## 回归风险

| 风险 | 严重度 | 缓解措施 |
|------|--------|---------|
| FR-4 import 更新遗漏 | 中 | 全局搜索确认所有引用文件 |
| FR-2 ViewModel 状态迁移遗漏 | 中 | 逐状态变量对照，编译验证 |
| FR-3 Composable 拆分后 private 可见性 | 低 | 同包内访问，private 改为 internal |
| FR-7 硬编码 ID 移除影响通知详情页 | 低 | 确认 NotificationDetailScreen 判断逻辑 |