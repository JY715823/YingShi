# Round 3 测试计划

> 基于: 需求文档 FR-6/FR-7/FR-8, 实施计划
> 日期: 2026-07-16

---

## 测试用例总览

| 类型 | 用例数 | 说明 |
|------|--------|------|
| 编译/基础设施 | 2 | T-1~T-2 |
| 功能测试 (FR-8) | 5 | T-3~T-7 |
| 功能测试 (FR-7) | 5 | T-8~T-12 |
| 功能测试 (FR-6) | 6 | T-13~T-18 |
| 影响范围测试 | 6 | T-19~T-24 |
| 跨模块回归测试 | 4 | T-25~T-28 |
| **总计** | **28** | |

---

## 一、编译/基础设施测试

### T-1: Android 编译通过
- 对应: NFR-1
- 端: Android
- 步骤: `gradlew.bat :app:assembleDebug`
- 预期: BUILD SUCCESSFUL
- 通过标准: 无编译错误

### T-2: Android 零弃用警告
- 对应: NFR-1
- 端: Android
- 步骤: 检查编译输出中无 `rememberSaveable(key=...)` 弃用警告
- 预期: 无弃用警告
- 通过标准: 零弃用警告

---

## 二、功能测试 — FR-8: CommentInputBar 统一为 TextFieldValue

### T-3: CommentInputBar 签名变更
- 对应: FR-8 AC-1, AC-2
- 步骤: 检查 CommentInputBar 函数签名 `value: TextFieldValue` 和 `onValueChange: (TextFieldValue) -> Unit`
- 通过标准: 参数类型为 TextFieldValue，且保留默认值以保持向后兼容

### T-4: 发送评论时取 textFieldValue.text
- 对应: FR-8 AC-3
- 步骤: 代码审查 CommentInputBar 内部 `onSend` 调用时使用 `value.text.trim()`
- 通过标准: 发送逻辑正确

### T-5: 发送后清空输入框
- 对应: FR-8 AC-4
- 步骤: 代码审查 `onSend` 回调中调用 `onValueChange(TextFieldValue(""))` 清空
- 通过标准: 发送后输入框清空

### T-6: 4 个调用方已更新
- 对应: FR-8 AC-5
- 步骤: 确认 RealCommentThreadContent / FakeSmallAlbumCommentSheet / RealSmallAlbumCommentSheet / PhotoViewerCommentSheet 均传递 `value` 和 `onValueChange`
- 通过标准: 4 个调用方全部更新

### T-7: 编译通过
- 对应: FR-8 AC-5
- 步骤: `gradlew.bat :app:assembleDebug`
- 通过标准: BUILD SUCCESSFUL

---

## 三、功能测试 — FR-7: Popup 操作菜单动画

### T-8: AnimatedVisibility 包装
- 对应: FR-7 AC-1, AC-2
- 步骤: 检查 CommentInlineActionMenu 外部有 `AnimatedVisibility` 包装，使用 `visibleState` 参数
- 通过标准: AnimatedVisibility 存在，enter/exit 动画已定义

### T-9: 进入动画正确
- 对应: FR-7 AC-1
- 步骤: 检查 enter 动画为 `fadeIn() + scaleIn()`
- 通过标准: 进入动画包含 fadeIn + scaleIn

### T-10: 退出动画正确
- 对应: FR-7 AC-2
- 步骤: 检查 exit 动画为 `fadeOut() + scaleOut()`
- 通过标准: 退出动画包含 fadeOut + scaleOut

### T-11: MutableTransitionState 使用
- 对应: FR-7 AC-1, AC-2
- 步骤: 检查使用 `MutableTransitionState` 管理动画状态，确保退出动画能完整播放
- 通过标准: MutableTransitionState 正确使用

### T-12: 编译通过
- 对应: FR-7 AC-5
- 步骤: `gradlew.bat :app:assembleDebug`
- 通过标准: BUILD SUCCESSFUL

---

## 四、功能测试 — FR-6: 评论列表分页

### T-13: RealCommentThreadUiState 新增字段
- 对应: FR-6 AC-3
- 步骤: 检查 `hasMore: Boolean = false` 和 `currentPage: Int = 1` 字段存在
- 通过标准: 两个字段存在且默认值正确

### T-14: CommentListState 新增字段
- 对应: FR-6 AC-3
- 步骤: 检查 `hasMore` 和 `currentPage` 字段存在，`toCommentListState` 正确映射
- 通过标准: 字段正确映射

### T-15: loadMoreMediaComments 方法
- 对应: FR-6 AC-1, AC-4
- 步骤: 检查 CommentThreadManager.loadMoreMediaComments 存在，逻辑正确（去重、追加）
- 通过标准: 方法存在，去重逻辑正确

### T-16: ViewModel 暴露 loadMore
- 对应: FR-6 AC-1
- 步骤: 检查 RealViewerCommentViewModel 和 PostDetailRealViewModel 暴露 loadMoreMediaComments
- 通过标准: 两个 ViewModel 均暴露方法

### T-17: UI 加载更多按钮
- 对应: FR-6 AC-2
- 步骤: 检查 RealCommentThreadContent 在 hasMore=true 时显示"加载更多评论"按钮，加载中显示 loading 指示器
- 通过标准: UI 正确显示

### T-18: 编译通过
- 对应: FR-6 AC-5
- 步骤: `gradlew.bat :app:assembleDebug`
- 通过标准: BUILD SUCCESSFUL

---

## 五、影响范围测试

### T-19: FakeSmallAlbumCommentSheet 影响
- 步骤: 代码审查确认 FAKE 模式小相册评论输入框使用 TextFieldValue 后行为不变
- 通过标准: 功能完整

### T-20: RealSmallAlbumCommentSheet 影响
- 步骤: 代码审查确认 REAL 模式小相册评论输入框行为不变
- 通过标准: 功能完整

### T-21: PhotoViewerCommentSheet 影响
- 步骤: 代码审查确认 Viewer 媒体评论输入框行为不变（darkMode 等参数保留）
- 通过标准: 功能完整

### T-22: RealCommentThreadContent 影响
- 步骤: 代码审查确认输入框 + 分页加载 UI 正确集成
- 通过标准: 功能完整

### T-23: PostDetailScreen 影响
- 步骤: 代码审查确认 `onLoadMore` 回调正确传递到 RealMediaCommentSheet
- 通过标准: 回调正确

### T-24: CommentListItem 影响
- 步骤: 代码审查确认操作菜单动画不影响原有交互（编辑/复制/删除）
- 通过标准: 原有功能不变

---

## 六、跨模块回归测试

### T-25: FAKE 模式评论功能
- 步骤: 代码审查确认 FAKE 模式下所有评论功能（小相册、媒体）不受影响
- 通过标准: 功能完整

### T-26: REAL 模式评论功能
- 步骤: 代码审查确认 REAL 模式下帖子评论、媒体评论、Viewer 评论不受影响
- 通过标准: 功能完整

### T-27: 评论编辑/删除/复制功能
- 步骤: 代码审查确认 CommentListItem 的编辑/删除/复制功能不受 FR-7 动画影响
- 通过标准: 功能完整

### T-28: 搜索/通知/同步模块
- 步骤: 确认改动不涉及搜索、通知、同步模块
- 通过标准: 无影响