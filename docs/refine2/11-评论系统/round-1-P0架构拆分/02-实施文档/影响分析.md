# Round 1 影响分析

> 基于: 实施计划
> 日期: 2026-07-16

---

## 变更影响链

### 变更1: 新建 CommentInteractionState.kt

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | — | 新文件，无直接影响 |
| 无间接影响 | — | 新增 data class + Composable 函数，不修改现有代码 |

---

### 变更2: 修改 RealCommentThreadContent 内联状态

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | `RealCommentThreadContent` 内部 | 6 个分散状态变量 → 1 个 `CommentInteractionState` |
| 直接 | `RealCommentThreadContent` 的调用方 | 函数签名不变（回调参数不变），调用方无需修改 |
| 间接 | `RealMediaCommentSheet` | 通过 `RealCommentThreadContent` 间接使用，行为不变 |
| 间接 | `RealSmallAlbumCommentSheet` | 同上 |
| 间接 | `RealCommentThreadCard` | 同上 |

**影响路径**: IA-1: `RealCommentThreadContent` 的交互行为（编辑/删除/复制/选择/展开收起）不变

---

### 变更3: 修改 FakeSmallAlbumCommentSheet 内联状态

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | `FakeSmallAlbumCommentSheet` 内部 | 6 个分散状态变量 → 1 个 `CommentInteractionState` |
| 间接 | `PostDetailScreen.kt` L145 | 调用方，函数签名不变 |

**影响路径**: IA-2: Fake 模式的交互行为不变

---

### 变更4: 修改 PhotoViewerCommentSheet 内联状态

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | `PhotoViewerCommentSheet` 内部 | 7 个分散状态变量 → 1 个 `CommentInteractionState` |
| 间接 | `RealViewerCommentViewModel` | 回调参数不变 |
| 间接 | `PhotoViewerScreen.kt` | 调用方，函数签名不变 |

**影响路径**: IA-3: Viewer 评论弹窗的交互行为（编辑/删除/复制/选择/定位高亮）不变

---

### 变更5: 删除 SmallAlbumCommentSheets.kt + 新建 3 个文件

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | `PostDetailScreen.kt` | 引用 `FakeSmallAlbumCommentSheet`(L145) / `MediaCommentPlaceholderSheet`(L168) / `RealSmallAlbumCommentSheet`(L379) / `RealMediaCommentSheet`(L401) |
| 直接 | `RealSmallAlbumCommentSheet` 内部 | 引用 `RealCommentThreadContent`（已迁移到 CommentComponents.kt） |
| 直接 | `RealMediaCommentSheet` 内部 | 引用 `RealCommentThreadContent`（同上） |
| 间接 | 无其他外部引用 | 所有函数为 `internal` 同 package 引用 |

**影响路径**: IA-4: 所有引用方无需改 import（同 package `internal` 函数）
**影响路径**: IA-5: `RealCommentThreadContent` 迁移后 `RealSmallAlbumCommentSheet` 和 `RealMediaCommentSheet` 的引用自动解析

---

### 变更6: 迁移 RealCommentThreadContent 到 CommentComponents.kt

| 影响层级 | 影响对象 | 影响说明 |
|---------|---------|---------|
| 直接 | `CommentComponents.kt` | 文件从 616 行增至 ~850 行 |
| 直接 | `SmallAlbumCommentSheet.kt`（新） | 引用 `RealCommentThreadContent` |
| 直接 | `MediaCommentSheet.kt`（新） | 引用 `RealCommentThreadContent` |
| 间接 | 无新增外部引用 | 函数仍为 `internal` |

---

## 必须测试的影响路径

| # | 路径 | 说明 |
|---|------|------|
| IA-1 | RealCommentThreadContent 交互行为 | 编辑/删除/复制/选择/展开收起 5 种行为不变 |
| IA-2 | FakeSmallAlbumCommentSheet 交互行为 | 同上 |
| IA-3 | PhotoViewerCommentSheet 交互行为 | 同上 + 定位高亮（2.4s 自动消失） |
| IA-4 | PostDetailScreen 引用方 | 4 处引用无需改 import |
| IA-5 | RealCommentThreadContent 迁移 | SmallAlbumCommentSheet + MediaCommentSheet 引用正常 |

---

## 不发生影响的代码区域（排除清单）

| 区域 | 原因 |
|------|------|
| `CommentGateway.kt` | 不修改 |
| `RealViewerCommentViewModel.kt` | 不修改 |
| `RealPhotoViewModels.kt` | 不修改 |
| `CommentModels.kt` | 不修改 |
| `CommentInputBar` | 不修改（状态管理在 CommentInteractionState 外层） |
| `CommentListItem` | 不修改 |
| `PhotoViewerScreen.kt` | 不修改（调用方签名不变） |
| Server 端所有文件 | 本轮无服务端变更 |
| 通知/同步/推送模块 | 不涉及 |