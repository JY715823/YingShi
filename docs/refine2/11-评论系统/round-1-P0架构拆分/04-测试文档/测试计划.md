# Round 1 测试计划

> 基于: 实施计划, 影响分析, 依赖映射, 需求文档 FR-1/FR-2
> 日期: 2026-07-16
> 本轮 FR: FR-1 (评论交互状态去重) + FR-2 (SmallAlbumCommentSheets.kt 拆分)

---

## 测试用例总览

| 类型 | 用例数 | 说明 |
|------|--------|------|
| 编译/基础设施 | 3 | T-1~T-3 |
| 功能测试 (FR-1) | 6 | T-4~T-9，对应 AC-1~AC-6 |
| 功能测试 (FR-2) | 5 | T-10~T-14，对应 AC-1~AC-5 |
| 影响范围测试 | 5 | T-15~T-19，对应 IA-1~IA-5 |
| 跨模块回归测试 | 7 | T-20~T-26，对应 A-1~A-7 |
| **总计** | **26** | |

---

## 一、编译/基础设施测试

### T-1: Android 编译通过
- 对应: NFR-1 AC-1
- 端: Android
- 前置条件: 所有代码变更已完成
- 步骤: `gradlew.bat :app:compileDebugKotlin`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 无编译错误

### T-2: Server 编译通过
- 对应: NFR-1
- 端: Server
- 前置条件: 无（本轮无 Server 变更）
- 步骤: `mvnw.cmd compile`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 无编译错误

### T-3: Docker 容器健康
- 对应: NFR-1
- 端: Server
- 前置条件: Docker Desktop 运行中
- 步骤: `docker compose up -d` → `curl localhost:8080/actuator/health`
- 预期结果: 所有容器 healthy，health endpoint 返回 UP
- 通过标准: 返回 `{"status":"UP"}`

---

## 二、功能测试 — FR-1: 评论交互状态去重

### T-4: CommentInteractionState.kt 文件存在且结构正确
- 对应: FR-1 AC-1
- 端: Android
- 前置条件: 无
- 步骤:
  1. 检查文件 `CommentInteractionState.kt` 存在
  2. 检查 `CommentInteractionState` data class 包含 7 个字段: `actionCommentId`, `editingCommentId`, `editingDraft`, `selectedCommentId`, `pendingDeleteCommentId`, `selectedCommentValue`, `showSelectedCommentNotice`
  3. 检查 `rememberCommentInteractionState()` Composable 存在，接受 `stateKey: String` 和 `initialSelectedCommentId: String?` 参数
  4. 检查返回类型为 `MutableState<CommentInteractionState>`
- 预期结果: 文件存在，data class 字段完整，Composable 签名正确
- 通过标准: 全部满足

### T-5: RealCommentThreadContent 使用 CommentInteractionState
- 对应: FR-1 AC-2
- 端: Android
- 前置条件: CommentComponents.kt 已修改
- 步骤:
  1. 读取 `CommentComponents.kt` 中 `RealCommentThreadContent` 函数
  2. 确认不再有 6 个独立的 `rememberSaveable` 状态变量
  3. 确认使用 `var interactionState by rememberCommentInteractionState(stateKeyPrefix)`
  4. 确认所有状态读写使用 `interactionState.xxx` / `interactionState.copy(xxx=...)`
- 预期结果: 状态管理统一为 `CommentInteractionState`
- 通过标准: 无独立状态变量，全部使用 `interactionState`

### T-6: SmallAlbumCommentSheet 使用 CommentInteractionState
- 对应: FR-1 AC-3
- 端: Android
- 前置条件: SmallAlbumCommentSheet.kt 已创建
- 步骤:
  1. 读取 `SmallAlbumCommentSheet.kt` 中 `FakeSmallAlbumCommentSheet` 和 `RealSmallAlbumCommentSheet` 函数
  2. 确认 `FakeSmallAlbumCommentSheet` 使用 `var interactionState by rememberCommentInteractionState(postId)`
  3. 确认 `RealSmallAlbumCommentSheet` 委托给 `RealCommentThreadContent`（内部使用 CommentInteractionState）
- 预期结果: 两个函数都使用 CommentInteractionState
- 通过标准: Fake 使用 `rememberCommentInteractionState()`，Real 通过 `RealCommentThreadContent` 间接使用

### T-7: PhotoViewerCommentSheet 使用 CommentInteractionState
- 对应: FR-1 AC-4
- 端: Android
- 前置条件: ViewerCommentSheet.kt 已修改
- 步骤:
  1. 读取 `ViewerCommentSheet.kt` 中 `PhotoViewerCommentSheet` 函数
  2. 确认不再有 7 个独立的 `rememberSaveable` 状态变量
  3. 确认使用 `var interactionState by rememberCommentInteractionState(mediaId, selectedCommentId)`
  4. 确认 `selectedCommentId` 参数传入 `rememberCommentInteractionState`
  5. 确认 `LaunchedEffect` 中定位高亮 2.4s 自动消失逻辑保留
- 预期结果: 状态管理统一为 `CommentInteractionState`
- 通过标准: 无独立状态变量，全部使用 `interactionState`

### T-8: 三处弹窗交互行为不变（代码审查）
- 对应: FR-1 AC-5
- 端: Android
- 前置条件: 所有 FR-1 变更已完成
- 步骤:
  1. 对比三处弹窗的编辑/删除/复制/选择回调逻辑，确认与拆分前一致
  2. 确认 `interactionState.copy()` 调用的字段正确
  3. 确认 BackHandler 逻辑不变
  4. 确认 `CommentInputBar` 的 `onSend` 回调不变
- 预期结果: 所有回调逻辑与原代码一致
- 通过标准: 代码审查无差异

### T-9: FR-1 编译通过
- 对应: FR-1 AC-6
- 端: Android
- 前置条件: FR-1 变更已完成
- 步骤: `gradlew.bat :app:compileDebugKotlin`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 无编译错误

---

## 三、功能测试 — FR-2: SmallAlbumCommentSheets.kt 拆分

### T-10: 拆分后文件结构正确
- 对应: FR-2 AC-1
- 端: Android
- 前置条件: 拆分已完成
- 步骤:
  1. 确认 `SmallAlbumCommentSheets.kt` 已删除
  2. 确认 `SmallAlbumCommentSheet.kt` 存在，包含 `RealSmallAlbumCommentSheet` 和 `FakeSmallAlbumCommentSheet`
  3. 确认 `MediaCommentSheet.kt` 存在，包含 `RealMediaCommentSheet` 和 `MediaCommentPlaceholderSheet`
  4. 确认 `CommentSummaryCard.kt` 存在，包含 `SmallAlbumCommentSummaryCard`
- 预期结果: 4 个文件（3 新建 + 1 删除），所有函数结构正确
- 通过标准: 所有文件存在且函数完整

### T-11: RealCommentThreadContent 迁移正确
- 对应: FR-2 AC-2
- 端: Android
- 前置条件: 拆分已完成
- 步骤:
  1. 读取 `CommentComponents.kt`，确认 `RealCommentThreadCard` 和 `RealCommentThreadContent` 在文件末尾
  2. 确认函数签名不变
  3. 确认使用 `rememberCommentInteractionState()`（FR-1 修改后版本）
- 预期结果: 两个函数在 CommentComponents.kt 中，签名正确
- 通过标准: 函数存在且签名不变

### T-12: 每个文件不超过 400 行
- 对应: FR-2 AC-3
- 端: Android
- 前置条件: 拆分已完成
- 步骤:
  1. 检查 `SmallAlbumCommentSheet.kt` 行数
  2. 检查 `MediaCommentSheet.kt` 行数
  3. 检查 `CommentSummaryCard.kt` 行数
  4. 检查 `CommentComponents.kt` 行数（含迁移后内容）
- 预期结果: 每个文件 ≤400 行（CommentComponents.kt 可略超，因为含共享组件）
- 通过标准: `SmallAlbumCommentSheet.kt` ≤400, `MediaCommentSheet.kt` ≤400, `CommentSummaryCard.kt` ≤400

### T-13: 所有 Composable 使用 YingShiThemeTokens
- 对应: FR-2 AC-4
- 端: Android
- 前置条件: 拆分已完成
- 步骤:
  1. grep 检查 4 个新文件是否存在 `MaterialTheme.colorScheme` 直接引用（排除 `MaterialTheme.typography`）
  2. 确认颜色引用使用 `YingShiThemeTokens.colors.*` 或 `colors` 变量（`val colors = YingShiThemeTokens.colors`）
- 预期结果: 所有颜色 token 使用 YingShiThemeTokens
- 通过标准: 无 `MaterialTheme.colorScheme` 直接颜色引用

### T-14: FR-2 编译通过
- 对应: FR-2 AC-5
- 端: Android
- 前置条件: 拆分已完成
- 步骤: `gradlew.bat :app:compileDebugKotlin`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 无编译错误

---

## 四、影响范围测试

### T-15: RealCommentThreadContent 交互行为（IA-1）
- 对应: IA-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查确认编辑/删除/复制/选择/展开收起 5 种回调逻辑与拆分前一致
  2. 确认 `interactionState.copy()` 字段传递正确
  3. 确认 `copyComment` 回调调用正确
- 预期结果: 所有交互逻辑不变
- 通过标准: 代码审查无差异

### T-16: FakeSmallAlbumCommentSheet 交互行为（IA-2）
- 对应: IA-2
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 读取 `SmallAlbumCommentSheet.kt` 中 `FakeSmallAlbumCommentSheet`
  2. 对比拆分前 L357-567 的交互逻辑
  3. 确认 `CommentGateway` 调用不变
  4. 确认 `onShowNotice` 回调不变
- 预期结果: 所有交互逻辑不变
- 通过标准: 代码审查无差异

### T-17: PhotoViewerCommentSheet 交互行为（IA-3）
- 对应: IA-3
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 读取 `ViewerCommentSheet.kt` 中 `PhotoViewerCommentSheet`
  2. 确认编辑/删除/复制/选择/定位高亮 5 种交互的回调正确
  3. 确认 `LaunchedEffect` 中 2.4s 自动消失逻辑保留
  4. 确认 darkMode=true 参数传递正确
- 预期结果: 所有交互逻辑不变，定位高亮功能正常
- 通过标准: 代码审查无差异

### T-18: PostDetailScreen 引用方 check（IA-4）
- 对应: IA-4
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 读取 `PostDetailScreen.kt`
  2. 确认 `FakeSmallAlbumCommentSheet`(L145)、`MediaCommentPlaceholderSheet`(L168)、`RealSmallAlbumCommentSheet`(L379)、`RealMediaCommentSheet`(L401) 引用正常
  3. 确认无需新增 import（同 package `internal` 函数）
- 预期结果: 4 处引用无需改 import，编译通过
- 通过标准: 编译通过，引用正常

### T-19: RealCommentThreadContent 迁移后引用正常（IA-5）
- 对应: IA-5
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 确认 `SmallAlbumCommentSheet.kt` 中 `RealSmallAlbumCommentSheet` 引用 `RealCommentThreadContent`
  2. 确认 `MediaCommentSheet.kt` 中 `RealMediaCommentSheet` 引用 `RealCommentThreadContent`
  3. 确认同 package 自动解析（无需 import）
- 预期结果: 两处引用正常
- 通过标准: 编译通过，引用正常

---

## 五、跨模块回归测试

### T-20: 小相册评论弹窗 REAL 模式（A-1）
- 对应: A-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `RealSmallAlbumCommentSheet` 的 Surface/背景/标题/关闭按钮结构
  2. 确认 `RealCommentThreadContent` 传递参数正确
  3. 确认 `CommentInputBar` 绑定正确
- 预期结果: 弹窗结构完整，组件引用正确
- 通过标准: 代码审查通过

### T-21: 小相册评论弹窗 FAKE 模式（A-2）
- 对应: A-2
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `FakeSmallAlbumCommentSheet` 的完整交互逻辑
  2. 确认 `CommentGateway` 调用（getPostComments/addPostComment/updatePostComment/deletePostComment）
  3. 确认 `CommentListItem` 回调传递正确
  4. 确认 `CommentInputBar` 绑定正确
- 预期结果: 弹窗结构完整，交互逻辑正确
- 通过标准: 代码审查通过

### T-22: Viewer 评论弹窗（A-3）
- 对应: A-3
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `PhotoViewerCommentSheet` 的完整交互逻辑
  2. 确认 darkMode=true 的主题参数传递
  3. 确认 `CommentListItem` 回调传递正确
  4. 确认 `CommentInputBar` 绑定正确（darkMode=true）
  5. 确认定位高亮逻辑（showSelectedCommentNotice）
- 预期结果: 弹窗结构完整，定位高亮功能正常
- 通过标准: 代码审查通过

### T-23: 媒体评论弹窗 REAL 模式（A-4）
- 对应: A-4
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `RealMediaCommentSheet` 的 Surface/标题/关闭按钮结构
  2. 确认 `RealCommentThreadContent` 传递参数正确
- 预期结果: 弹窗结构完整，组件引用正确
- 通过标准: 代码审查通过

### T-24: 媒体评论弹窗 FAKE 模式（A-5）
- 对应: A-5
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `MediaCommentPlaceholderSheet` 的完整结构
  2. 确认 `CommentGateway.getMediaComments` 调用
  3. 确认评论列表渲染正确
- 预期结果: 弹窗结构完整，交互逻辑正确
- 通过标准: 代码审查通过

### T-25: 评论摘要卡片（A-6）
- 对应: A-6
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 代码审查 `SmallAlbumCommentSummaryCard` 的参数传递
  2. 确认 `label`/`emptyText`/`commentCount` 正确显示
  3. 确认 `onOpenComments`/`onRetry` 回调正常
  4. 确认 `isLoading`/`errorMessage` 状态显示正确
- 预期结果: 卡片显示正确，回调完整
- 通过标准: 代码审查通过

### T-26: 编译验证（A-7）
- 对应: A-7
- 端: Android
- 前置条件: 所有变更已完成
- 步骤: `gradlew.bat :app:compileDebugKotlin`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 无编译错误

---

## 六、测试执行顺序

```
T-4 → T-5 → T-6 → T-7 → T-8 → T-9  (FR-1 功能测试)
T-10 → T-11 → T-12 → T-13 → T-14    (FR-2 功能测试)
T-15 → T-16 → T-17 → T-18 → T-19    (影响范围测试)
T-20 → T-21 → T-22 → T-23 → T-24 → T-25 → T-26  (跨模块回归)
T-1 → T-2 → T-3                      (编译/基础设施，最后执行)
```