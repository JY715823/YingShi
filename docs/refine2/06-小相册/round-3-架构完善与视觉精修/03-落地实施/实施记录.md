# 06-小相册 Round 3 实施记录

> 实施时间: 2026-07-11
> 实施者: Trae
> 范围: FR-8（创建页 ViewModel 引入）+ FR-9（三端视觉精修）

---

## 一、FR-8: 创建页 ViewModel 引入

### 1.1 新建文件

**文件**: `feature/photos/PostComposerViewModel.kt`

- 新建 `PostComposerUiState` 数据类，包含 22+ 状态字段
- 计算属性: `selectedAppMediaIds`, `selectedSystemMediaIds`, `selectedMediaIds`, `normalizedSelectedParticipantUserIds`, `resolvedCoverMediaId`, `postMediaListItems`, `selectedAlbumTitles`, `hasUnsavedChanges`, `publishButtonText`
- 新建 `PostComposerViewModel` 类，包含:
  - `MutableStateFlow<PostComposerUiState>` + `asStateFlow()`
  - `refresh()`: 加载 collaboratorDirectory + seedState + availableAppMediaItems
  - `hydrateInitialAppMediaIds()`: 处理 route.initialAppMediaIds 注入
  - `normalizeParticipants()`: 归一化参与者集合
  - 状态更新方法: `updateTitle()`, `updateSummary()`, `toggleAlbum()`, `updateParticipantUserIds()`, `updateDisplayTime()`, `updateMediaDraft()`, `addAppMediaItems()`, `removeMedia()`, `clearAllMedia()`
  - 6 个 setShowXxx() 方法
  - `setLocalMessage()`, `clearLocalMessage()`
  - `submitDraft()`: 保留全部 6 分支逻辑（REAL/FAKE × system-media/app-media/no-media）
  - `companion object { fun factory(route: CreatePostRoute): ViewModelProvider.Factory }`

### 1.2 修改文件

**文件**: `feature/photos/PostComposerScreen.kt`

- `loadCreatePostUiState` 从 `private` 改为 `internal`
- `loadCreatePostAppMediaItems` 从 `private` 改为 `internal`
- `CreatePostScreen` 函数完全重写:
  - 删除 22 个 rememberSaveable/remember/produceState 声明
  - 删除 submitDraft(), toggleAlbum(), handleClose() 局部函数
  - 改为 `viewModel()` + `collectAsState()` 模式
  - 所有 UI 回调改为调用 viewModel 方法
  - 保留 2 个 LaunchedEffect（hydration + participant normalization）
  - 清理未使用 import: Toast, CreatePostPayload, kotlinx.coroutines.launch, mutableStateOf, produceState, remember, rememberCoroutineScope, rememberSaveable, setValue

### 1.3 编译验证

- `gradlew.bat assembleDebug` 通过 (BUILD SUCCESSFUL)

---

## 二、FR-9: 三端视觉精修

### 2.1 AC-2: 标题加大加粗

**文件**: `SmallAlbumDetailHeader.kt` L282

- 从: `MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.SemiBold)`
- 改为: `MaterialTheme.typography.titleLarge.copy(fontSize = 24.sp, fontWeight = FontWeight.Bold)`

### 2.2 AC-4: 媒体卡片阴影/圆角优化

**文件**: `SmallAlbumMediaGrid.kt` L1492-1615

- 外层 Box 改为 Surface 包裹
- shape = `RoundedCornerShape(YingShiThemeTokens.radius.sm)` (12dp)
- color = `colors.glassSurfaceBase.copy(alpha = 0.55f)`
- border = `BorderStroke(1.dp, colors.glassStroke.copy(alpha = 0.52f))`
- shadowElevation = `2.dp`
- 内层保留 Box 处理 combinedClickable 和内容定位
- 移除 `clipToBounds()` 和 `background(Color.Transparent)`（由 Surface 统一处理）

### 2.3 AC-5: 密度缩放 overlay 美化

**文件**: `SmallAlbumMediaGrid.kt` L1579-1590

- 从: `colors.viewerBackground.copy(alpha = if (selected) 0.18f else 0.06f)` 实色覆盖
- 改为: `Brush.radialGradient(colors.glassStroke → Color.Transparent)` 光晕效果

### 2.4 AC-7: 创建页媒体预览玻璃容器

**文件**: `PostComposerScreen.kt` L687-733, L735-758

- 媒体预览瓦片: Box → Surface（glassSurfaceBase 0.55 + glassStroke border + shadowElevation 2dp）
- "全部"按钮: Box → Surface（onClick 版本 + glassSurfaceBase 0.48 + glassStroke border + shadowElevation 2dp）
- 内层包裹 Box 恢复 BoxScope.align() 支持

### 2.5 AC-9: Color.Black/White 替换

**文件**: `PostComposerScreen.kt`

| 位置 | 原 | 新 |
|------|---|---|
| 移出按钮 containerColor | `Color.Black.copy(alpha=0.58f)` | `colors.viewerBackground.copy(alpha=0.72f)` |
| 移出按钮 contentColor | `Color.White` | `colors.viewerText` |
| 封面标签 color | `Color.White` | `colors.onPrimaryContainer` |

**文件**: `GearEditScreen.kt`

| 位置 | 原 | 新 |
|------|---|---|
| 封面标签 color | `Color.White` | `colors.onPrimaryContainer` |

### 2.6 AC-10: 保存/删除按钮间距收紧

**文件**: `GearEditScreen.kt` L374, L681

- 从: `Arrangement.spacedBy(6.dp)`
- 改为: `Arrangement.spacedBy(4.dp)`（两处均改）

### 2.7 AC-11: 编辑页媒体预览统一

**文件**: `GearEditScreen.kt` L915-948

- Box → Surface（glassSurfaceBase 0.55 + glassStroke border + shadowElevation 2dp）
- 内层包裹 Box 恢复 BoxScope.align() 支持
- Color.White → colors.onPrimaryContainer

### 2.8 AC-12: SelectableAlbumChip 升级

**文件**: `PostComposerScreen.kt` L516-582

- 形状: capsule → `RoundedCornerShape(radius.md)` (18dp)
- 选中容器色: `primaryContainer 0.72` → `glassSurfaceBase`
- 未选中容器色: `sectionBackground 0.72` → `raisedSurface 0.96`
- 边框: 固定 1dp → 选中 1.5dp / 未选中 1dp
- shadowElevation: 0 → 选中 2dp / 未选中 0dp
- 新增 Canvas 光晕（选中时 radialGradient: titleAuraWarmGlow 0.32 → titleAuraCoolGlow 0.18 → Transparent）
- 新增 accent 竖条（3dp×18dp linearGradient: titleAuraWarmGlow → titleAuraCoolGlow）
- 文字: `labelLarge` → `titleSmall.copy(fontSize = 14.sp, lineHeight = 17.sp, fontWeight = SemiBold/Medium)`
- 文字色: `titleAccent` / `textSecondary` → `titleAccent` / `textPrimary`

### 2.9 编译验证

- `gradlew.bat assembleDebug` 通过 (BUILD SUCCESSFUL in 28s)

---

## 三、变更文件统计

| # | 文件 | 端 | 变更类型 | 行数变化 |
|---|------|-----|---------|---------|
| 1 | PostComposerViewModel.kt | Android | 新建 | +487 |
| 2 | PostComposerScreen.kt | Android | 修改 | -100+（重构） |
| 3 | SmallAlbumDetailHeader.kt | Android | 修改 | ±1 |
| 4 | SmallAlbumMediaGrid.kt | Android | 修改 | +12 |
| 5 | GearEditScreen.kt | Android | 修改 | +8 |

---

## 四、完整性自检

| 实施计划项 | 状态 | 备注 |
|-----------|------|------|
| FR-8 PostComposerViewModel.kt 新建 | ✅ | 含全部状态+6分支submitDraft |
| FR-8 PostComposerScreen.kt 改造 | ✅ | viewModel()+collectAsState() |
| FR-8 编译验证 | ✅ | BUILD SUCCESSFUL |
| FR-9 AC-2 标题加大 | ✅ | 24sp Bold |
| FR-9 AC-4 媒体卡片阴影 | ✅ | Surface+border+shadow |
| FR-9 AC-5 密度overlay美化 | ✅ | radialGradient glow |
| FR-9 AC-7 创建页玻璃容器 | ✅ | Surface glass container |
| FR-9 AC-9 Color硬编码替换 | ✅ | 4处全替换 |
| FR-9 AC-10 按钮间距收紧 | ✅ | 6dp→4dp 两处 |
| FR-9 AC-11 编辑页玻璃容器 | ✅ | Surface glass container |
| FR-9 AC-12 Chip升级 | ✅ | AlbumSwitchChip风格 |
| FR-9 AC-14 最终编译验证 | ✅ | BUILD SUCCESSFUL |
