# 06-小相册 Round 2 实施计划

> 基于 Round 2 调研报告
> 日期：2026-07-11
> 范围：FR-4（颜色 token 化）+ FR-5（批量删除优化）+ FR-6（文档同步）+ FR-7（合并逻辑统一）

---

## 一、FR-6: post-api.md 文档同步

### 目标
补充 post-api.md 中缺失的 participantUserIds 字段文档。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `E:/Study/App/YingShi/docs/contracts/post-api.md` | Docs | 修改 | L108-118 POST 请求体补充 participantUserIds；L129-136 PATCH 请求体补充 participantUserIds |

### 详细计划

#### 文件1: post-api.md
- **L108-118（POST /api/posts 请求体）**：在 `"contributorLabel": "Demo A and Demo B",` 之后插入 `"participantUserIds": ["user_001", "user_002"],`
- **L129-136（PATCH /api/posts/{postId} 请求体）**：在 `"contributorLabel": "Demo A and Demo B",` 之后插入 `"participantUserIds": ["user_001", "user_002"],`

**格式要求**：保持文档现有纯 JSON 示例风格，字段顺序参照 PostDto.kt 中 DTO 字段顺序（contributorLabel 之后、displayTimeMillis 之前）。

---

## 二、FR-5: 编辑保存批量删除优化（服务端）

### 目标
服务端新增批量删除端点，一次请求删除多个媒体，@Transactional 保证原子性。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `E:/Study/App/YingShi-Server/src/main/java/com/yingshi/server/dto/content/UpdatePostMediaBatchRequest.java` | Server | 新建 | DTO record，含 removeMediaIds 字段 |
| 2 | `E:/Study/App/YingShi-Server/src/main/java/com/yingshi/server/service/content/PostService.java` | Server | 修改 | 新增 batchRemovePostMedia 方法（@Transactional） |
| 3 | `E:/Study/App/YingShi-Server/src/main/java/com/yingshi/server/controller/PostController.java` | Server | 修改 | 新增 batchRemoveSmallAlbumMedia 端点（PATCH /{smallAlbumId}/media-batch） |

### 详细计划

#### 文件1: UpdatePostMediaBatchRequest.java（新建）

```java
package com.yingshi.server.dto.content;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotEmpty;
import java.util.List;

public record UpdatePostMediaBatchRequest(
    @NotEmpty(message = "removeMediaIds is required.")
    List<@NotBlank(message = "removeMediaId is required.") String> removeMediaIds
) {}
```

**注解模式**：参考 AddPostMediaRequest，@NotEmpty + @NotBlank type-use。

#### 文件2: PostService.java（新增方法）

在 updatePostMediaOrder 方法（L249）之后新增：

```java
@Transactional
public PostDetailDto batchRemovePostMedia(String postId, UpdatePostMediaBatchRequest request, AuthenticatedUser currentUser) {
    String libraryId = currentUser.libraryId();
    PostEntity post = requirePost(postId, libraryId);
    List<String> removeMediaIds = request.removeMediaIds();
    validateDistinctIds(removeMediaIds, ErrorCode.SMALL_ALBUM_MEDIA_ORDER_INVALID, "removeMediaIds contains duplicates.");

    List<PostMediaEntity> relations = postMediaRepository.findByLibraryIdAndPostIdOrderBySortOrderAsc(libraryId, postId);
    Set<String> existingMediaIds = relations.stream().map(PostMediaEntity::getMediaId).collect(Collectors.toSet());
    for (String mediaId : removeMediaIds) {
        if (!existingMediaIds.contains(mediaId)) {
            throw new ApiException(HttpStatus.BAD_REQUEST, ErrorCode.MEDIA_NOT_FOUND,
                "One or more removeMediaIds do not belong to the small album.");
        }
    }

    if (relations.size() - removeMediaIds.size() <= 0) {
        throw new ApiException(HttpStatus.CONFLICT, ErrorCode.DELETE_CONFLICT,
            "Batch removal would leave the small album empty; delete the whole small album instead.");
    }

    List<PostMediaEntity> toRemove = relations.stream()
        .filter(r -> removeMediaIds.contains(r.getMediaId()))
        .toList();
    postMediaRepository.deleteAll(toRemove);
    postMediaRepository.flush();

    List<PostMediaEntity> remaining = postMediaRepository.findByLibraryIdAndPostIdOrderBySortOrderAsc(libraryId, postId);
    for (int i = 0; i < remaining.size(); i++) {
        remaining.get(i).setSortOrder(i + 1);
    }
    postMediaRepository.saveAll(remaining);

    if (removeMediaIds.contains(post.getCoverMediaId())) {
        post.setCoverMediaId(remaining.isEmpty() ? null : remaining.get(0).getMediaId());
    }
    post.setLastModifiedByUserId(currentUser.userId());
    postRepository.save(post);

    PostDetailDto dto = buildPostDetail(post);
    notifyContentUpdated(currentUser, post, "对方从小相册批量移除了" + removeMediaIds.size() + "项媒体。");
    return dto;
}
```

**逻辑要点**：
- requirePost：library 隔离 + 软删除校验
- validateDistinctIds：去重校验（复用现有私有方法）
- 加载全部关系 + 逐个校验 removeMediaIds 属于本小相册（MEDIA_NOT_FOUND）
- 防清空校验（DELETE_CONFLICT）
- deleteAll + flush（避免唯一约束冲突，参考 addMediaToPost 模式）
- 重排剩余关系 sortOrder（参考 TrashService.resequenceSmallAlbumMedia）
- 封面补偿：若封面被删，回退到剩余首个媒体
- notifyContentUpdated：事务提交后异步推送通知

**边界条件**：
- removeMediaIds 为空：@NotEmpty 自动拦截 → 400 VALIDATION_ERROR
- removeMediaIds 含重复：validateDistinctIds → 400 SMALL_ALBUM_MEDIA_ORDER_INVALID
- mediaId 不属于本小相册：→ 400 MEDIA_NOT_FOUND
- 批量删除会清空：→ 409 DELETE_CONFLICT
- 小相册不存在/已软删：requirePost → 404 SMALL_ALBUM_NOT_FOUND

**异常处理**：全部通过 ApiException 抛出，由全局异常处理器统一处理。

#### 文件3: PostController.java（新增端点）

在 updateSmallAlbumMediaOrder（L114）之后插入：

```java
@Operation(summary = "Batch remove media from small album", security = @SecurityRequirement(name = "bearerAuth"))
@PatchMapping("/{smallAlbumId}/media-batch")
public ApiResponse<PostDetailDto> batchRemoveSmallAlbumMedia(
        @PathVariable String smallAlbumId,
        @Valid @RequestBody UpdatePostMediaBatchRequest request,
        @CurrentUser AuthenticatedUser currentUser,
        HttpServletRequest httpRequest
) {
    return ApiResponse.success(requestId(httpRequest),
        postService.batchRemovePostMedia(smallAlbumId, request, currentUser));
}
```

**注解模式**：完全对齐 updateSmallAlbumMediaOrder（@Operation + @SecurityRequirement + @PatchMapping + @PathVariable + @Valid @RequestBody + @CurrentUser + HttpServletRequest）。

---

## 三、FR-5: 编辑保存批量删除优化（Android 端）

### 目标
Android 端新增批量删除 API/Repository/ViewModel 改造，将 N 次单删除改为 1 次批量请求。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/data/remote/api/SmallAlbumApi.kt` | Android | 修改 | 新增 updatePostMediaBatch 方法 |
| 2 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/data/remote/api/dto/PostDto.kt` | Android | 修改 | 新增 UpdatePostMediaBatchRequestDto |
| 3 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/data/repository/Contracts.kt` | Android | 修改 | PostRepository 接口新增 updatePostMediaBatch 抽象方法 |
| 4 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/data/repository/RealRepositories.kt` | Android | 修改 | RealPostRepository 新增 updatePostMediaBatch 实现 |
| 5 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/data/repository/FakeRepositories.kt` | Android | 修改 | FakePostRepositoryShell 新增 updatePostMediaBatch FAKE 实现 |
| 6 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/RealPostEditingViewModels.kt` | Android | 修改 | save() 第 316-334 行 forEach 循环改为一次批量请求 |

### 详细计划

#### 文件2: PostDto.kt（新增 DTO）

在 UpdatePostMediaOrderRequestDto 之后新增：

```kotlin
@Serializable
data class UpdatePostMediaBatchRequestDto(
    val removeMediaIds: List<String>
)
```

#### 文件1: SmallAlbumApi.kt（新增 API 方法）

在 updatePostMediaOrder（L49）之后新增：

```kotlin
@PATCH("api/small-albums/{smallAlbumId}/media-batch")
suspend fun updatePostMediaBatch(
    @Path("smallAlbumId") smallAlbumId: String,
    @Body request: UpdatePostMediaBatchRequestDto,
): ApiEnvelopeDto<SmallAlbumDetailDto>
```

**路径**：`api/small-albums/{smallAlbumId}/media-batch`（与服务端一致）
**返回类型**：`ApiEnvelopeDto<SmallAlbumDetailDto>`（与 addMediaToPost/updatePostMediaOrder 一致）

#### 文件3: Contracts.kt（接口扩展）

在 PostRepository 接口的 updatePostMediaOrder 之后新增：

```kotlin
suspend fun updatePostMediaBatch(
    postId: String,
    removeMediaIds: List<String>,
): ApiResult<RemotePostDetail>
```

#### 文件4: RealRepositories.kt（实现）

在 RealPostRepository 的 updatePostMediaOrder 之后新增：

```kotlin
override suspend fun updatePostMediaBatch(
    postId: String,
    removeMediaIds: List<String>,
): ApiResult<RemotePostDetail> {
    return runCatching {
        postApi.updatePostMediaBatch(
            smallAlbumId = postId,
            request = UpdatePostMediaBatchRequestDto(removeMediaIds = removeMediaIds),
        ).data.toRemoteDetail()
    }.fold(
        onSuccess = { ApiResult.Success(it) },
        onFailure = {
            ApiResult.Error(
                code = "POST_MEDIA_BATCH_REQUEST_FAILED",
                message = backendRequestErrorMessage(it, "批量移除媒体失败，请稍后重试。"),
                throwable = it,
            )
        },
    )
}
```

**模式**：完全对齐现有 updatePostMediaOrder 实现（runCatching + fold + ApiResult）。

#### 文件5: FakeRepositories.kt（FAKE 同步）

在 FakePostRepositoryShell 的 updatePostMediaOrder（L316）之后新增：

```kotlin
override suspend fun updatePostMediaBatch(
    postId: String,
    removeMediaIds: List<String>,
): ApiResult<RemotePostDetail> = ApiResult.Error(
    code = "NOT_IMPLEMENTED",
    message = "FAKE updatePostMediaBatch 尚未实现。",
    throwable = NotImplementedError(),
)
```

**模式**：与现有 addMediaToPost（NOT_IMPLEMENTED）、deleteSmallAlbum（NOT_IMPLEMENTED）一致。

#### 文件6: RealPostEditingViewModels.kt（save() 改造）

**当前逻辑（L316-334）**：
```kotlin
val removedIds = initialMediaIds.filterNot { finalMediaIds.contains(it) }
removedIds.forEach { mediaId ->
    // 逐个调用 RepositoryProvider.mediaRepository.deleteMediaFromPost
    // 成功更新 committedMediaIds，失败记录 firstFailure（不中断）
}
```

**改造为**：
```kotlin
val removedIds = initialMediaIds.filterNot { finalMediaIds.contains(it) }
if (removedIds.isNotEmpty()) {
    when (val batchResult = postRepository.updatePostMediaBatch(route.postId, removedIds)) {
        is ApiResult.Success -> {
            mediaChanged = true
            committedMediaIds = committedMediaIds.filterNot { removedIds.contains(it) }
        }
        is ApiResult.Error -> firstFailure = batchResult.toBackendUiMessage("批量移除媒体失败。")
        ApiResult.Loading -> Unit
    }
}
```

**语义变化**：
- 原：逐个删除，首个失败记录但不中断（容错）
- 新：一次批量请求，全部成功或全部失败（@Transactional 原子性）
- 符合需求文档风险清单第 2 项"@Transactional 保证原子性"

**注意事项**：
- `toBackendUiMessage` 是现有扩展函数，需确认是否已 import
- 后续 updatePostMediaOrder 调用（L336-348）依赖 finalMediaIds，批量删除成功后顺序更新逻辑不变

---

## 四、FR-4: 硬编码颜色 token 化

### 目标
17 处 MaterialTheme.colorScheme 替换为 YingShiThemeTokens token。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/PostDetailScreen.kt` | Android | 修改 | L121, L307: background → colors.appBackground |
| 2 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/SmallAlbumDetailHeader.kt` | Android | 修改 | L198: background → colors.appBackground |
| 3 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/GearEditScreen.kt` | Android | 修改 | L346,437,657,1200,1212,1241,1248,1264: error → colors.destructive |
| 4 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/PostMediaListScreen.kt` | Android | 修改 | L1139,1140,1146,1199,1212,1218: error/errorContainer/onErrorContainer → colors.destructive/destructiveContainer/onDestructiveContainer |

### 详细计划

#### 替换映射表

| 原值 | 新值 | 处数 |
|------|------|------|
| `MaterialTheme.colorScheme.background` | `colors.appBackground` | 3 |
| `MaterialTheme.colorScheme.error` | `colors.destructive` | 9 |
| `MaterialTheme.colorScheme.error.copy(alpha = Xf)` | `colors.destructive.copy(alpha = Xf)` | （包含在 error 9 处内） |
| `MaterialTheme.colorScheme.errorContainer.copy(alpha = Xf)` | `colors.destructiveContainer.copy(alpha = Xf)` | 2 |
| `MaterialTheme.colorScheme.onErrorContainer` | `colors.onDestructiveContainer` | 2 |
| `BorderStroke(1.dp, MaterialTheme.colorScheme.error.copy(...))` | `BorderStroke(1.dp, colors.destructive.copy(...))` | 1（包含在 error 内） |

#### 前置条件验证

4 个文件均已有 `val colors = YingShiThemeTokens.colors`（调研确认），无需新增变量声明或 import。

#### Token 使用合规性

所有替换使用已存在的 YingShiThemeTokens token（appBackground、destructive、destructiveContainer、onDestructiveContainer），**不新增/修改 token 定义**，符合"颜色系统受保护"设计约束。

---

## 五、FR-7: 重复合并逻辑统一

### 目标
提取共享合并函数，消除两处重复逻辑。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/PostMediaMergeUtils.kt` | Android | 新建 | 共享泛型合并函数 |
| 2 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/GearEditScreen.kt` | Android | 修改 | 删除 mergeDraftPostMediaWithRepoAdditions（L871-890），L146 调用改为共享函数 |
| 3 | `E:/Study/App/YingShi/app/src/main/java/com/example/yingshi/feature/photos/RealPostEditingViewModels.kt` | Android | 修改 | 删除 mergeMediaDraftWithRemoteAdditions（L737-757），L226 调用改为共享函数 |

### 详细计划

#### 文件1: PostMediaMergeUtils.kt（新建）

```kotlin
package com.example.yingshi.feature.photos

/**
 * 合并本地草稿媒体与远端新增媒体的共享逻辑。
 *
 * 算法：
 * 1. 用 latest 覆盖 current 中同 id 的项（通过 transformLatest 转换类型）
 * 2. 新增 latest 中不在 currentIds 且不在 previousBaselineIds 的项（防止重新加入已删除的基线项）
 */
internal fun <T, U> mergeMediaDraftWithRemoteAdditions(
    currentItems: List<T>,
    latestItems: List<U>,
    previousBaselineIds: Collection<String>,
    currentIdSelector: (T) -> String,
    latestIdSelector: (U) -> String,
    transformLatest: (U) -> T,
): List<T> {
    val currentIds = currentItems.mapTo(linkedSetOf()) { currentIdSelector(it) }
    val latestById = latestItems.associateBy { latestIdSelector(it) }
    return buildList {
        currentItems.forEach { item ->
            val latest = latestById[currentIdSelector(item)]
            if (latest != null) add(transformLatest(latest)) else add(item)
        }
        latestItems.forEach { media ->
            val id = latestIdSelector(media)
            if (id !in currentIds && id !in previousBaselineIds) {
                add(transformLatest(media))
            }
        }
    }
}
```

#### 文件2: GearEditScreen.kt（改造）

**删除**：L871-890 的 `private fun mergeDraftPostMediaWithRepoAdditions(...)`

**L146 调用改为**：
```kotlin
val merged = mergeMediaDraftWithRemoteAdditions(
    currentItems = mediaItems,
    latestItems = repoMediaItems,
    previousBaselineIds = initialMediaItems.map { it.id },
    currentIdSelector = { it.id },
    latestIdSelector = { it.id },
    transformLatest = { it.toPostMediaListItem() },
)
```

#### 文件3: RealPostEditingViewModels.kt（改造）

**删除**：L737-757 的 `private fun mergeMediaDraftWithRemoteAdditions(...)`

**L226 调用改为**：
```kotlin
val merged = mergeMediaDraftWithRemoteAdditions(
    currentItems = snapshot.mediaItems,
    latestItems = latestMediaItems,
    previousBaselineIds = initialMediaIds,
    currentIdSelector = { it.id },
    latestIdSelector = { it.id },
    transformLatest = { it.copy(isCover = false) },
)
```

**注意**：B 调用方保留 `copy(isCover = false)` 语义（封面标记清零，由调用方另行指派）。

---

## 六、实施顺序

按依赖关系排序：

1. **FR-6 文档同步**（无依赖，最简单）
   - post-api.md 补充 participantUserIds

2. **FR-5 服务端**（Android 端依赖服务端端点）
   - UpdatePostMediaBatchRequest.java（新建 DTO）
   - PostService.batchRemovePostMedia（新增方法）
   - PostController.batchRemoveSmallAlbumMedia（新增端点）
   - mvnw.cmd compile 验证

3. **FR-5 服务端测试**（服务端端点完成后）
   - PostIntegrationTest 新增 5 个测试方法
   - mvnw.cmd test 验证

4. **FR-5 Android 端**（依赖服务端端点）
   - PostDto.kt 新增 DTO
   - SmallAlbumApi 新增方法
   - Contracts.kt 接口扩展
   - RealRepositories.kt 实现
   - FakeRepositories.kt FAKE 同步
   - RealPostEditingViewModels.kt save() 改造

5. **FR-4 颜色 token 化**（独立，可与 FR-5 Android 并行）
   - PostDetailScreen.kt（2 处）
   - SmallAlbumDetailHeader.kt（1 处）
   - GearEditScreen.kt（8 处）
   - PostMediaListScreen.kt（6 处）

6. **FR-7 合并逻辑统一**（独立，可与 FR-4 并行）
   - PostMediaMergeUtils.kt 新建
   - GearEditScreen.kt 改造
   - RealPostEditingViewModels.kt 改造

7. **编译验证**
   - gradlew.bat assembleDebug
   - mvnw.cmd compile

---

## 七、Flyway 迁移

**本轮无 Flyway 迁移**。FR-5 批量删除复用现有 small_album_media 表结构，无需新增字段或索引。

---

## 八、回滚方案

| FR | 回滚方式 |
|----|---------|
| FR-6 | git revert post-api.md |
| FR-5 服务端 | git revert 新增的 3 个文件/方法 |
| FR-5 Android | git revert 新增的 API/Repository/ViewModel 改造 |
| FR-4 | git revert 4 个文件的颜色替换 |
| FR-7 | git revert PostMediaMergeUtils.kt + 2 个调用点改造 |

所有变更均为新增或替换，不涉及破坏性删除，回滚安全。
