# Round 2 实施计划 — 视觉提升

> 基于: Round 2 调研报告 2026-07-13
> 范围: FR-4（Overlay 材质光影升级）+ FR-5（视频错误状态 UI 优化）
> 端: Android（服务端本轮无变更）
> 基线: Round 1 已完成（9 个 Viewer 文件 + 主容器 759 行）

---

## 总体策略

**实施顺序**（每步编译验证）:
1. 新增 Viewer 材质 token（Color.kt + Tokens.kt）
2. 新建 ViewerGlassModifier.kt — 毛玻璃辅助 Modifier
3. 修改 ViewerOverlayChrome.kt — 升级 9 个 Composable + DropdownMenu
4. 扩展 VideoUi.kt — VideoGlyphState 新增 ERROR
5. 修改 ViewerVideoPlayer.kt — 新增 ViewerVideoErrorState + AnimatedContent

**设计约束**:
- 新增 token 必须派生自现有 viewer* / glass* token（copy + alpha），不引入新色相
- 新增 Composable 必须使用 `YingShiThemeTokens.colors.*`
- RenderEffect 仅 API 31+ 使用，API 24-30 fallback 为半透明深色
- 浮层薄、轻、克制，材质不能反客为主

**行数控制**:
- ViewerOverlayChrome.kt 当前 668 行，升级后预估 ~780 行（< 800）
- ViewerVideoPlayer.kt 当前 591 行，升级后预估 ~680 行（< 800）
- 新建 ViewerGlassModifier.kt 预估 ~120 行

---

## FR-4: Overlay 材质光影升级

### 目标
将 Viewer overlay 控制栏从"纯色+简单 alpha"升级为符合 design-plan 的材质光影风格：毛玻璃效果 + 边缘微光线 + 渐变时间胶囊 + DropdownMenu 阴影层次。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `ui/theme/Color.kt` | Android | 修改 | 新增 2 个 viewer overlay 颜色 token |
| 2 | `ui/theme/Tokens.kt` | Android | 修改 | YingShiColors 新增 viewerOverlayEdgeGlow/viewerOverlayBorder 字段；YingShiMotion 新增 viewerScrimBlur/viewerCapsuleBlur 字段 |
| 3 | `feature/photos/ViewerGlassModifier.kt` | Android | 新建 | 毛玻璃辅助 Modifier（rememberViewerBlurEffect + Modifier.viewerGlassSurface） |
| 4 | `feature/photos/ViewerOverlayChrome.kt` | Android | 修改 | 升级 9 个 Composable + DropdownMenu 深色化 |

### 详细计划

#### 文件1: Color.kt（修改）

**新增 2 个 token**（在现有 viewer token 之后，约 32 行）:
```kotlin
// Viewer Overlay 材质 token（派生自 viewerAccent，不引入新色相）
val YingShiViewerOverlayEdgeGlow = YingShiViewerAccent.copy(alpha = 0.15f)  // 边缘微光线
val YingShiViewerOverlayBorder = YingShiViewerAccent.copy(alpha = 0.22f)   // 金属边线
```

**import 补充**: 无需新增（YingShiViewerAccent 已在同文件）

#### 文件2: Tokens.kt（修改）

**YingShiColors 数据类新增 2 字段**（约 60 行之后）:
```kotlin
val viewerOverlayEdgeGlow: Color = YingShiViewerOverlayEdgeGlow,
val viewerOverlayBorder: Color = YingShiViewerOverlayBorder,
```

**YingShiMotion 数据类新增 2 字段**（约 92 行之后）:
```kotlin
val viewerScrimBlur: Dp = 18.dp,
val viewerCapsuleBlur: Dp = 12.dp,
```

**import 补充**: `androidx.compose.ui.graphics.Color`、`androidx.compose.ui.unit.Dp`（如未存在）

**defaultYingShiColors / defaultYingShiMotion 实例补充**: 对应字段加入默认值

#### 文件3: ViewerGlassModifier.kt（新建，~120 行）

**目标**: 封装毛玻璃 RenderEffect 逻辑，供 ViewerOverlayChrome.kt 调用。

**内容**:

```kotlin
package com.example.yingshi.feature.photos

import android.graphics.RenderEffect
import android.graphics.Shader
import android.os.Build
import androidx.compose.runtime.Composable
import androidx.compose.runtime.remember
import androidx.compose.ui.Modifier
import androidx.compose.ui.composed
import androidx.compose.ui.graphics.CompositingStrategy
import androidx.compose.ui.graphics.graphicsLayer
import androidx.compose.ui.platform.LocalDensity
import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp
import com.example.yingshi.ui.theme.YingShiThemeTokens

/**
 * 记住 RenderEffect 实例（API 31+），低版本返回 null。
 * 使用 OffscreenLayer 合成策略确保 blur 生效。
 */
@Composable
internal fun rememberViewerBlurEffect(blurRadius: Dp): RenderEffect? {
    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) return null
    val radiusPx = with(androidx.compose.ui.platform.LocalDensity.current) { blurRadius.toPx() }
    return remember(blurRadius) {
        RenderEffect.createBlurEffect(
            radiusPx,
            radiusPx,
            Shader.TileMode.DECAL,
        )
    }
}

/**
 * Viewer 玻璃表面 Modifier。
 * API 31+: 应用 RenderEffect 真实模糊
 * API 24-30: 仅设置 CompositingStrategy.Offscreen + 透明度（fallback）
 *
 * 配合 Surface(color = viewerSurface.copy(alpha = 0.x)) 使用。
 */
internal fun Modifier.viewerGlassSurface(
    blurRadius: Dp,
    fallbackAlpha: Float = 0.78f,
): Modifier = composed {
    val renderEffect = rememberViewerBlurEffect(blurRadius)
    if (renderEffect != null) {
        this.graphicsLayer {
            this.renderEffect = renderEffect
            this.compositingStrategy = CompositingStrategy.Offscreen
        }
    } else {
        // fallback: 不做 blur，alpha 由调用方 Surface 控制
        this
    }
}
```

**import**: RenderEffect, Shader, Build, Composable, remember, Modifier, composed, CompositingStrategy, graphicsLayer, LocalDensity, Dp, dp, YingShiThemeTokens

**预估行数**: ~100 行

#### 文件4: ViewerOverlayChrome.kt（修改，668 → ~780 行）

**修改清单**:

| # | Composable | 行号 | 改造内容 |
|---|-----------|------|---------|
| 1 | ViewerTopScrim | 107-120 | drawBehind 顶部加 1dp drawLine 微光线（viewerOverlayEdgeGlow） |
| 2 | ViewerBottomScrim | 122-135 | drawBehind 底部加 1dp drawLine 微光线（viewerOverlayEdgeGlow） |
| 3 | ViewerNoticeHost | 137-192 | Surface 加 viewerGlassSurface(blurRadius=capsuleBlur)；BorderStroke 换 viewerOverlayBorder |
| 4 | ViewerTimeBadge | 340-359 | 背景 linearGradient（viewerSurface → viewerSurface.copy(alpha=0.55f)）+ viewerGlassSurface(capsuleBlur) + BorderStroke 换 viewerOverlayBorder + 保留 shadowElevation 4.dp |
| 5 | ViewerIconCircle | 361-385 | 背景 linearGradient + viewerGlassSurface(capsuleBlur) + BorderStroke 换 viewerOverlayBorder |
| 6 | ViewerCapsule | 477-518 | viewerGlassSurface(capsuleBlur) + BorderStroke 换 viewerOverlayBorder |
| 7 | ViewerCommentEntry | 434-475 | viewerGlassSurface(capsuleBlur) + BorderStroke 换 viewerOverlayBorder |
| 8 | ViewerCommentPreviewLayer | 520-601 | viewerGlassSurface(scrimBlur)（已有 radialGradient 保留） |
| 9 | DropdownMenu | 308-334 | 自定义 containerColor = viewerSurface.copy(alpha=0.92f) + shadowElevation 8.dp |

**详细改造方案**:

##### 4.1 ViewerTopScrim（107-120 行）

旧:
```kotlin
Modifier.background(
    brush = Brush.verticalGradient(
        0f to ViewerNightBottom.copy(alpha = 0.62f),
        0.5f to ViewerNightBottom.copy(alpha = 0.24f),
        1f to Color.Transparent,
    ),
)
```

新:
```kotlin
Modifier
    .background(
        brush = Brush.verticalGradient(
            0f to ViewerNightBottom.copy(alpha = 0.62f),
            0.5f to ViewerNightBottom.copy(alpha = 0.24f),
            1f to Color.Transparent,
        ),
    )
    .drawBehind {
        // 顶部边缘微光线
        drawLine(
            color = YingShiThemeTokens.colors.viewerOverlayEdgeGlow,
            start = Offset(0f, 0f),
            end = Offset(size.width, 0f),
            strokeWidth = 1.dp.toPx(),
        )
    }
```

**import 补充**: drawBehind, Offset, drawLine

##### 4.2 ViewerBottomScrim（122-135 行）

同上，在底部边缘加 drawLine（y = size.height）。

##### 4.3 ViewerTimeBadge（340-359 行）

旧:
```kotlin
Surface(
    color = ViewerNightTop.copy(alpha = 0.68f),
    border = BorderStroke(1.dp, ViewerAccent.copy(alpha = 0.24f)),
    shadowElevation = 4.dp,
)
```

新:
```kotlin
Surface(
    modifier = modifier.viewerGlassSurface(
        blurRadius = YingShiThemeTokens.motion.viewerCapsuleBlur,
    ),
    shape = RoundedCornerShape(YingShiThemeTokens.radius.capsule),
    color = Color.Transparent,  // 背景由 drawBehind 渐变绘制
    border = BorderStroke(1.dp, YingShiThemeTokens.colors.viewerOverlayBorder),
    shadowElevation = 4.dp,
)
.drawBehind {
    // 渐变背景
    drawRect(
        brush = Brush.linearGradient(
            colors = listOf(
                ViewerNightTop.copy(alpha = 0.82f),
                ViewerNightTop.copy(alpha = 0.55f),
            ),
        ),
    )
}
```

**注意**: color=Transparent + drawBehind 渐变，确保 blur 能透过背景看到内容（毛玻璃效果需要透明背景才能模糊后方内容）。

##### 4.4 ViewerIconCircle（361-385 行）

同 ViewerTimeBadge 模式：viewerGlassSurface + linearGradient 背景 + viewerOverlayBorder 边线。

##### 4.5 ViewerNoticeHost（137-192 行）

Surface 加 viewerGlassSurface(capsuleBlur)，BorderStroke 换 viewerOverlayBorder。

##### 4.6 ViewerCapsule / ViewerCommentEntry / ViewerCommentPreviewLayer

同上模式，加 viewerGlassSurface + BorderStroke 换 token。

##### 4.7 DropdownMenu（308-334 行）

旧:
```kotlin
DropdownMenu(
    expanded = menuExpanded,
    onDismissRequest = { menuExpanded = false },
    modifier = Modifier.wrapContentSize(Alignment.TopEnd),
)
```

新:
```kotlin
DropdownMenu(
    expanded = menuExpanded,
    onDismissRequest = { menuExpanded = false },
    modifier = Modifier.wrapContentSize(Alignment.TopEnd),
    containerColor = ViewerNightTop.copy(alpha = 0.92f),
    shadowElevation = 8.dp,
    tonalElevation = 0.dp,
)
```

**import 补充**: viewerGlassSurface（从 ViewerGlassModifier.kt）

---

## FR-5: 视频播放器错误状态 UI 优化

### 目标
将视频加载失败时的"仅重试按钮"升级为"错误图标+提示文字+重试按钮"的优雅视觉呈现，使用 Viewer 深色 token，带过渡动画。

### 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `feature/photos/VideoUi.kt` | Android | 修改 | VideoGlyphState 枚举新增 ERROR + when 分支补充错误图标 |
| 2 | `feature/photos/ViewerVideoPlayer.kt` | Android | 修改 | 新增 ViewerVideoErrorState Composable + ViewerVideoCanvas 用 AnimatedContent 包裹状态切换 |

### 详细计划

#### 文件1: VideoUi.kt（修改，32 行 → ~55 行）

**枚举扩展**（29 行）:
```kotlin
enum class VideoGlyphState { PLAY, PAUSE, ERROR }
```

**when 分支补充**（约 150 行，DrawVideoGlyph 函数内）:
```kotlin
VideoGlyphState.ERROR -> {
    // 绘制错误图标：圆圈 + 叹号
    drawCircle(
        color = color,
        radius = size.minDimension / 2.2f,
        style = Stroke(width = strokeWidth),
    )
    // 竖线
    drawLine(
        color = color,
        start = Offset(center.x, center.y - size.minDimension / 5f),
        end = Offset(center.x, center.y - size.minDimension / 16f),
        strokeWidth = strokeWidth,
    )
    // 点
    drawCircle(
        color = color,
        radius = strokeWidth / 2,
        center = Offset(center.x, center.y + size.minDimension / 6f),
    )
}
```

**import 补充**: Offset, Stroke（如未存在）

#### 文件2: ViewerVideoPlayer.kt（修改，591 → ~680 行）

**新增 ViewerVideoErrorState Composable**（插入在 ViewerVideoPosterFallback 之后，约 109 行）:

```kotlin
@Composable
internal fun ViewerVideoErrorState(
    errorMessage: String,
    onRetry: () -> Unit,
    modifier: Modifier = Modifier,
) {
    val colors = YingShiThemeTokens.colors
    val spacing = YingShiThemeTokens.spacing
    val radius = YingShiThemeTokens.radius

    Column(
        modifier = modifier
            .fillMaxSize()
            .padding(spacing.xl),
        horizontalAlignment = Alignment.CenterHorizontally,
        verticalArrangement = Arrangement.Center,
    ) {
        // 错误图标（viewerAccent 色）
        Box(
            modifier = Modifier
                .size(48.dp)
                .clip(CircleShape)
                .background(colors.viewerSurface.copy(alpha = 0.54f))
                .border(
                    BorderStroke(1.dp, colors.viewerOverlayBorder),
                    CircleShape,
                ),
            contentAlignment = Alignment.Center,
        ) {
            VideoGlyph(
                state = VideoGlyphState.ERROR,
                modifier = Modifier.size(28.dp),
                color = colors.viewerAccent,
            )
        }

        Spacer(modifier = Modifier.height(spacing.md))

        // 错误提示文案（viewerText 色）
        Text(
            text = errorMessage,
            style = MaterialTheme.typography.bodyMedium,
            color = colors.viewerText.copy(alpha = 0.90f),
            textAlign = TextAlign.Center,
            maxLines = 2,
        )

        Spacer(modifier = Modifier.height(spacing.lg))

        // 重试按钮（viewerAccent 色调，按压态反馈）
        Surface(
            shape = RoundedCornerShape(radius.capsule),
            color = colors.viewerAccent.copy(alpha = 0.18f),
            border = BorderStroke(1.dp, colors.viewerOverlayBorder),
            modifier = Modifier.yingShiClickable(
                pressedScale = 0.94f,
                onClick = onRetry,
            ),
        ) {
            Row(
                modifier = Modifier.padding(horizontal = spacing.md, vertical = spacing.sm),
                verticalAlignment = Alignment.CenterVertically,
                horizontalArrangement = Arrangement.spacedBy(spacing.xs),
            ) {
                Icon(
                    imageVector = Icons.Rounded.Refresh,
                    contentDescription = null,
                    tint = colors.viewerAccent,
                    modifier = Modifier.size(18.dp),
                )
                Text(
                    text = "重试",
                    style = MaterialTheme.typography.labelLarge.copy(
                        fontWeight = FontWeight.SemiBold,
                    ),
                    color = colors.viewerAccent,
                )
            }
        }
    }
}
```

**修改 ViewerVideoCanvas**（455-476 行替换）:

旧:
```kotlin
if (playbackState.errorMessage != null) {
    Surface(...) {
        Text(text = "重试", ...)
    }
}
```

新:
```kotlin
// 用 AnimatedContent 包裹状态切换
AnimatedContent(
    targetState = videoUiState,  // 枚举: PLAYING / LOADING / ERROR / COMPLETED
    transitionSpec = {
        fadeIn(tween(motion.stateMillis, easing = motion.easing)) togetherWith
            fadeOut(tween(motion.stateMillis, easing = motion.easing))
    },
    label = "videoState",
) { state ->
    when (state) {
        VideoUiState.PLAYING -> { /* PlayerView */ }
        VideoUiState.LOADING -> { /* CircularProgressIndicator */ }
        VideoUiState.ERROR -> {
            ViewerVideoErrorState(
                errorMessage = playbackState.errorMessage ?: "视频加载失败",
                onRetry = {
                    retryVersion += 1
                    isPrepared = false
                    updatePlaybackState { it.retryState() }
                },
            )
        }
        VideoUiState.COMPLETED -> { /* 最后一帧 */ }
    }
}
```

**新增 VideoUiState 枚举**（文件顶部或 ViewerVideoErrorState 之前）:
```kotlin
private enum class VideoUiState { PLAYING, LOADING, ERROR, COMPLETED }
```

**状态映射逻辑**:
```kotlin
val videoUiState = when {
    playbackState.errorMessage != null -> VideoUiState.ERROR
    !isPrepared || playbackState.isLoading -> VideoUiState.LOADING
    playbackState.isPlaying -> VideoUiState.PLAYING
    playbackState.isCompleted -> VideoUiState.COMPLETED
    else -> VideoUiState.LOADING  // 默认加载态
}
```

**import 补充**:
- `androidx.compose.animation.AnimatedContent`
- `androidx.compose.animation.fadeIn`
- `androidx.compose.animation.fadeOut`
- `androidx.compose.animation.togetherWith`
- `androidx.compose.animation.core.tween`
- `androidx.compose.foundation.layout.Arrangement`
- `androidx.compose.foundation.layout.Column`
- `androidx.compose.foundation.layout.Row`
- `androidx.compose.foundation.layout.Spacer`
- `androidx.compose.foundation.layout.height`
- `androidx.compose.foundation.layout.size`
- `androidx.compose.foundation.border`
- `androidx.compose.foundation.clip`
- `androidx.compose.foundation.background`
- `androidx.compose.material3.Icon`
- `androidx.compose.material3.MaterialTheme`
- `androidx.compose.material3.Text`
- `androidx.compose.ui.draw.clip`
- `androidx.compose.ui.text.font.FontWeight`
- `androidx.compose.ui.text.style.TextAlign`
- `androidx.compose.ui.unit.dp`
- `androidx.compose.material.icons.Icons`
- `androidx.compose.material.icons.rounded.Refresh`
- `com.example.yingshi.ui.components.yingShiClickable`
- `com.example.yingshi.ui.theme.YingShiThemeTokens`

**ViewerVideoControls 错误态处理**:
- 错误态下隐藏 ViewerVideoControls（通过 `if (videoUiState != VideoUiState.ERROR)` 包裹）
- 或保持当前行为（仅禁用 Slider），由 AnimatedContent 的 ERROR 分支覆盖整个 Canvas

**选择方案**: 用 AnimatedContent 包裹整个内容区，ERROR 分支全屏覆盖（包含 ViewerVideoControls 区域），这样错误态下控件自然隐藏。

---

## Flyway 迁移

**无**。本轮不涉及数据库变更。

---

## 实施顺序与编译验证节点

| 步骤 | 操作 | 编译验证 |
|------|------|---------|
| 1 | Color.kt 新增 2 个 token | `gradlew.bat assembleDebug` |
| 2 | Tokens.kt 新增 4 个字段 + 默认值 | `gradlew.bat assembleDebug` |
| 3 | 新建 ViewerGlassModifier.kt | `gradlew.bat assembleDebug` |
| 4 | ViewerOverlayChrome.kt 升级 9 个 Composable + DropdownMenu | `gradlew.bat assembleDebug` |
| 5 | VideoUi.kt 扩展 ERROR 枚举 | `gradlew.bat assembleDebug` |
| 6 | ViewerVideoPlayer.kt 新增 ViewerVideoErrorState + AnimatedContent | `gradlew.bat assembleDebug` |
| 7 | 全量编译 + 单元测试 | `gradlew.bat assembleDebug` + `gradlew.bat :app:testDebugUnitTest --tests "*PhotoViewerBehaviorTest*"` |

---

## AC 验收标准对照

| AC | 描述 | 验证方式 |
|----|------|---------|
| FR-4 AC-1 | 顶部/底部 overlay 有 drawBehind 毛玻璃效果 | 代码检查 + API 31+ 真机验证 |
| FR-4 AC-2 | overlay 边缘有 1px 微光线 | 代码检查 drawLine |
| FR-4 AC-3 | 时间胶囊有微妙渐变背景 | 代码检查 Brush.linearGradient |
| FR-4 AC-4 | 三横线 DropdownMenu 有阴影层次 | shadowElevation 8.dp |
| FR-4 AC-5 | 新增视觉效果使用 Viewer 深色 token | grep 确认无 MaterialTheme.colorScheme |
| FR-4 AC-6 | 沉浸模式下 overlay 显隐动画流畅 | 不改动画，保留现有 AnimatedVisibility |
| FR-4 AC-7 | 不影响照片本身显示质量 | blur 仅作用于 overlay，不作用于图片层 |
| FR-5 AC-1 | 视频加载失败时显示居中错误状态 UI | ViewerVideoErrorState |
| FR-5 AC-2 | 错误状态 UI 使用 Viewer 深色 token | viewerSurface/viewerAccent/viewerText |
| FR-5 AC-3 | 重试按钮 viewerAccent 色调 + 按压态 | yingShiClickable(pressedScale=0.94f) |
| FR-5 AC-4 | 正常→错误有淡入过渡动画 | AnimatedContent fadeIn |
| FR-5 AC-5 | 错误→加载有过渡动画 | AnimatedContent fadeOut |
| FR-5 AC-6 | 网络恢复自动重试逻辑不变 | 保持现状（不引入自动重试） |
