# Round 2 实施计划

> 基于: 需求文档 v1.0, 调研报告 v1.0
> 日期: 2026-07-17
> 实施顺序: FR-6 → FR-5 → FR-4 → FR-3

---

## FR-6: 问候语响应式更新

**目标**: 问候语在 ON_RESUME 时重新计算，跨时段自动更新

### 变更文件清单

| 文件 | 端 | 变更类型 | 变更内容 |
|------|-----|---------|---------|
| `feature/life/LifeScreen.kt` | Android | 修改 | 添加 DisposableEffect 监听 ON_RESUME，替换 Calendar 为 java.time |

### 实施细节

**新增 DisposableEffect**:
```kotlin
val lifecycleOwner = LocalLifecycleOwner.current
DisposableEffect(lifecycleOwner) {
    val observer = LifecycleEventObserver { _, event ->
        if (event == Lifecycle.Event.ON_RESUME) {
            val now = LocalDateTime.now()
            currentTimeLabel = when (now.hour) {
                in 5..11 -> "早安"
                in 12..17 -> "午安"
                else -> "晚安"
            }
            currentDateLabel = "${now.monthValue}月${now.dayOfMonth}日 ${now.dayOfWeek.getDisplayName(TextStyle.FULL, Locale.CHINA)}"
        }
    }
    lifecycleOwner.lifecycle.addObserver(observer)
    onDispose { lifecycleOwner.lifecycle.removeObserver(observer) }
}
```

**保留**: 首次 LaunchedEffect(Unit) 初始化（改为 java.time）

---

## FR-5: Calendar→java.time + 硬编码清理

**目标**: 消除 java.util.Calendar 使用，统一时区常量，token 化硬编码颜色

### 变更文件清单

| 文件 | 端 | 变更类型 | 变更内容 |
|------|-----|---------|---------|
| `feature/life/LifeScreen.kt` | Android | 修改 | Calendar → java.time, 移除 Calendar import |
| `feature/life/LifeConsoleScreen.kt` | Android | 修改 | 5处 "Asia/Shanghai" → 常量引用, formatTime → java.time, 颜色常量 → YingShiThemeTokens |

### 实施细节

#### 5.1 时区常量

在 `LifeConsoleScreen.kt` 顶部定义:
```kotlin
private const val DEFAULT_ZONE_ID = "Asia/Shanghai"
```

替换位置:
1. `fun LifeConsoleScreen(zoneId: String = DEFAULT_ZONE_ID, ...)`  
2. `LifeConsoleHistoryPage(zoneId = DEFAULT_ZONE_ID, ...)`  
3. `currentLifeConsoleDate(zoneId = DEFAULT_ZONE_ID)`  
4. `millisUntilNextLifeConsoleRefresh(zoneId = DEFAULT_ZONE_ID)`  
5. `LifeConsoleScreen(zoneId = DEFAULT_ZONE_ID, ...)` 调用处

#### 5.2 Calendar→java.time (LifeScreen.kt)

```kotlin
// 替换前
val calendar = Calendar.getInstance()
val hour = calendar.get(Calendar.HOUR_OF_DAY)
currentTimeLabel = when (hour) { ... }
val month = calendar.get(Calendar.MONTH) + 1
val day = calendar.get(Calendar.DAY_OF_MONTH)
val dayOfWeek = calendar.getDisplayName(Calendar.DAY_OF_WEEK, Calendar.LONG, Locale.CHINA)

// 替换后
val now = LocalDateTime.now()
currentTimeLabel = when (now.hour) { ... }
currentDateLabel = "${now.monthValue}月${now.dayOfMonth}日 ${now.dayOfWeek.getDisplayName(TextStyle.FULL, Locale.CHINA)}"
```

添加 import: `java.time.LocalDateTime`, `java.time.format.TextStyle`

#### 5.3 formatTime → java.time

```kotlin
// 替换前
private fun formatTime(timeMillis: Long): String {
    return SimpleDateFormat("HH:mm", Locale.getDefault()).format(Date(timeMillis))
}

// 替换后
private fun formatTime(timeMillis: Long): String {
    val time = Instant.ofEpochMilli(timeMillis).atZone(ZoneId.of(DEFAULT_ZONE_ID))
    return String.format("%02d:%02d", time.hour, time.minute)
}
```

#### 5.4 颜色常量

- `LifeFramePalette`: 保持为文件级常量（UI 专用色，非全局 token）
- `LifePersonGradient`: 保持文件级，添加注释标注颜色来源
- `LifeMealGradient`: 保持文件级，添加注释标注颜色来源

---

## FR-4: 跨文件重复逻辑提取

**目标**: 提取 `isVideo`/`looksLikeVideoUrl`/`firstUsableImageUrl`/`formatTime` 到共享文件

### 变更文件清单

| 文件 | 端 | 变更类型 | 变更内容 |
|------|-----|---------|---------|
| `feature/life/LifeConsoleUtils.kt` | Android | **新建** | 共享工具函数 |
| `feature/life/widget/LifeConsoleWidgetStore.kt` | Android | 修改 | 删除重复函数，改为 import |
| `feature/life/widget/LifeConsoleWidgetProvider.kt` | Android | 修改 | 删除重复函数，改为 import |
| `feature/life/LifeMediaQuickViewerActivity.kt` | Android | 修改 | 删除重复函数，改为 import |
| `feature/life/LifeConsoleScreen.kt` | Android | 修改 | 移动 formatTime 到 Utils |

### 实施细节

#### LifeConsoleUtils.kt 内容

```kotlin
package com.example.yingshi.feature.life

import com.example.yingshi.data.model.RemoteMedia
import java.text.SimpleDateFormat
import java.time.Instant
import java.time.ZoneId
import java.util.Date
import java.util.Locale

fun RemoteMedia.isVideo(): Boolean {
    return mediaType.equals("video", ignoreCase = true) ||
        mimeType?.startsWith("video/", ignoreCase = true) == true ||
        !videoUrl.isNullOrBlank()
}

fun looksLikeVideoUrl(url: String): Boolean {
    val lower = url.substringBefore('?').lowercase()
    return listOf(".mp4", ".mov", ".m4v", ".webm", ".avi", ".mkv").any(lower::endsWith)
}

fun firstUsableImageUrl(vararg urls: String?): String? {
    return urls.firstOrNull { url ->
        val normalized = url?.trim()
        !normalized.isNullOrBlank() && !looksLikeVideoUrl(normalized)
    }?.trim()
}

fun formatTime(timeMillis: Long, zoneId: String = LifeConsoleConstants.DEFAULT_ZONE_ID): String {
    val time = Instant.ofEpochMilli(timeMillis).atZone(ZoneId.of(zoneId))
    return String.format("%02d:%02d", time.hour, time.minute)
}
```

#### 各文件变更

- **LifeConsoleWidgetStore**: 删除 `isVideo()`, `looksLikeVideoUrl()`, `firstUsableImageUrl()` private 定义；使用 `import com.example.yingshi.feature.life.*`
- **LifeConsoleWidgetProvider**: 删除 `isVideo()`, `formatTime()` private 定义；改为调用 `LifeConsoleUtils.formatTime()`
- **LifeMediaQuickViewerActivity**: 删除 `looksLikeVideoUrl()`, `firstUsableImageUrl()`；`isVideo` 属性保留（是 `LifeMediaQuickViewerPayload` 的属性，不是 RemoteMedia 扩展）
- **LifeConsoleScreen**: 删除 `formatTime()` private 定义；保留 `withOptimisticBowelDelta` 和 `withoutMedia`（不提取）

---

## FR-3: 路由管理 sealed class 化

**目标**: 4个 boolean 路由变量 → `LifeSubRoute` sealed class，用 `when` 替代 `if-else` 链

### 变更文件清单

| 文件 | 端 | 变更类型 | 变更内容 |
|------|-----|---------|---------|
| `feature/life/LifeSubRoute.kt` | Android | **新建** | sealed class 定义 |
| `app/YingShiApp.kt` | Android | 修改 | 替换 boolean 路由变量，when 替换 if-else 链 |
| `feature/life/LifeScreen.kt` | Android | 修改 | 3个回调 → 1个 onNavigate 回调 |

### 实施细节

#### LifeSubRoute.kt

```kotlin
package com.example.yingshi.feature.life

sealed class LifeSubRoute {
    data object Life : LifeSubRoute()
    data object Console : LifeSubRoute()
    data object Ledger : LifeSubRoute()
    data object LedgerAdd : LifeSubRoute()
    data object Chat : LifeSubRoute()
}
```

#### YingShiApp.kt 变更

**1. 变量替换** (L172-180):
```kotlin
// 替换前
var ledgerRouteActive by remember { mutableStateOf(false) }
var ledgerOpenAddNonce by remember { mutableIntStateOf(0) }
var ledgerOpenHomeNonce by remember { mutableIntStateOf(0) }
var lifeConsoleRouteActive by remember { mutableStateOf(false) }
var chatViewerRouteActive by remember { mutableStateOf(false) }

// 替换后
var lifeSubRoute by remember { mutableStateOf<LifeSubRoute>(LifeSubRoute.Life) }
```

**2. clearProtectedUiRoutes** (L367-388):
```kotlin
lifeSubRoute = LifeSubRoute.Life
```

**3. nonce 监听** (L648-673):
```kotlin
// openLifeConsoleNonce → lifeSubRoute = LifeSubRoute.Console
// openLedgerRequestNonce → lifeSubRoute = LifeSubRoute.Ledger
// openLedgerAddRequestNonce → lifeSubRoute = LifeSubRoute.LedgerAdd
```

**4. BackHandler** (L1012-1021):
```kotlin
when (lifeSubRoute) {
    LifeSubRoute.Life -> { /* 不做处理，由系统 back 处理 */ }
    is LifeSubRoute.Console -> lifeSubRoute = LifeSubRoute.Life
    is LifeSubRoute.Ledger -> lifeSubRoute = LifeSubRoute.Life
    is LifeSubRoute.LedgerAdd -> lifeSubRoute = LifeSubRoute.Ledger
    is LifeSubRoute.Chat -> lifeSubRoute = LifeSubRoute.Life
}
```

**5. canNavigateBack** (L1108-1112):
```kotlin
val canNavigateBack = lifeSubRoute != LifeSubRoute.Life
```

**6. 路由分发** (L1603-1647):
```kotlin
when (lifeSubRoute) {
    is LifeSubRoute.Life -> LifeScreen(onNavigate = { lifeSubRoute = it })
    is LifeSubRoute.Console -> LifeConsoleScreen(onBack = { lifeSubRoute = LifeSubRoute.Life })
    is LifeSubRoute.Ledger -> LedgerHomeScreen(onBack = { lifeSubRoute = LifeSubRoute.Life }, onAdd = { lifeSubRoute = LifeSubRoute.LedgerAdd })
    is LifeSubRoute.LedgerAdd -> LedgerAddScreen(onBack = { lifeSubRoute = LifeSubRoute.Ledger })
    is LifeSubRoute.Chat -> ImportedChatScreen(onBack = { lifeSubRoute = LifeSubRoute.Life })
}
```

#### LifeScreen.kt 变更

**函数签名**:
```kotlin
// 替换前
@Composable
fun LifeScreen(
    onOpenLifeConsole: () -> Unit,
    onOpenLedger: () -> Unit,
    onOpenChatViewer: () -> Unit,
)

// 替换后
@Composable
fun LifeScreen(
    onNavigate: (LifeSubRoute) -> Unit,
)
```

**卡片回调**:
```kotlin
onClick = { onNavigate(LifeSubRoute.Console) }  // 今日痕迹
onClick = { onNavigate(LifeSubRoute.Ledger) }   // 记账
onClick = { onNavigate(LifeSubRoute.Chat) }     // 聊天记录
```

### 风险

- YingShiApp.kt 中还有其他使用 `ledgerRouteActive` 等变量的地方（通知中心、HomeScreen），需要仔细搜索替换
- 编译需要大量 import 调整