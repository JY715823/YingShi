# Round 3 测试计划

> 基于: 需求文档 FR-7, 实施计划 v1.0, 影响分析 v1.0
> 日期: 2026-07-17

---

## 一、编译/基础设施测试

### T-1: Android 编译通过
- 对应: FR-7/AC-4
- 端: Android
- 步骤: `.\gradlew.bat assembleDebug --no-daemon`
- 预期结果: BUILD SUCCESSFUL
- 通过标准: 编译无错误

### T-2: 新文件语法正确
- 对应: FR-7/AC-1
- 端: Android
- 步骤: 检查 WidgetPendingIntentFactory.kt, ConsoleWidgetBuilder.kt, PeopleWidgetBuilder.kt 编译无错误
- 预期结果: 三个新文件无编译错误
- 通过标准: T-1 通过即可验证

---

## 二、功能测试

### TC-1: LifeConsoleWidgetProvider 行数验证
- 对应: FR-7/AC-2
- 端: Android
- 步骤: 检查 `LifeConsoleWidgetProvider.kt` 行数
- 预期结果: 文件行数显著减少（原617行），Controller 部分约 190 行
- 通过标准: 文件行数 ≤ 250 行，Controller 部分 ≤ 200 行

### TC-2: ConsoleWidgetBuilder 结构验证
- 对应: FR-7/AC-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 检查 `ConsoleWidgetBuilder.kt` 存在
  2. 确认包含 `build()` 函数
  3. 确认包含 `bindSlot()` 函数
  4. 确认包含 `SlotViews` data class 和 4 个实例
  5. 确认包含工具函数 (currentMedia, formatTime, updateStatus 等)
- 预期结果: 所有结构和函数存在
- 通过标准: 全部确认存在

### TC-3: PeopleWidgetBuilder 结构验证
- 对应: FR-7/AC-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 检查 `PeopleWidgetBuilder.kt` 存在
  2. 确认包含 `build()` 函数
  3. 确认 `build()` 中复用 `ConsoleWidgetBuilder.bindSlot()`
- 预期结果: 所有结构和函数存在，正确复用 bindSlot
- 通过标准: 全部确认存在

### TC-4: WidgetPendingIntentFactory 结构验证
- 对应: FR-7/AC-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 检查 `WidgetPendingIntentFactory.kt` 存在
  2. 确认包含 5 个 PendingIntent 构建函数
  3. 确认包含 4 个辅助函数
- 预期结果: 所有函数存在
- 通过标准: 全部确认存在

### TC-5: LifeConsoleWidgetProvider 瘦身验证
- 对应: FR-7/AC-2
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 检查 LifeConsoleWidgetController 中不再包含: buildConsoleViews, buildPeopleViews, bindConsoleActions, bindPeopleActions, bindSlot, bindBowel
  2. 检查不再包含 PendingIntent 函数 (openMainIntent, openMediaEntry, openMediaViewer, slotIntent, widgetBroadcast)
  3. 检查不再包含 SlotViews 数据类和实例
  4. 检查不再包含工具函数 (formatTime, formatAbsoluteTime, updateStatus, widgetTodayDate, initRuntime)
- 预期结果: 以上函数/类均不存在于 Controller 中
- 通过标准: 全部确认不存在

### TC-6: Controller 委托调用正确
- 对应: FR-7/AC-3
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 检查 `updateConsoleWidgets` 调用 `ConsoleWidgetBuilder.build()`
  2. 检查 `updatePeopleWidgets` 调用 `PeopleWidgetBuilder.build()`
  3. 检查 `applySnapshot` 调用 `ConsoleWidgetBuilder.initRuntime()` 和 `ConsoleWidgetBuilder.updateStatus()`
  4. 检查 `fetchAndUpdateAll` 调用 `ConsoleWidgetBuilder.initRuntime()`, `ConsoleWidgetBuilder.widgetTodayDate()`, `ConsoleWidgetBuilder.updateStatus()`
  5. 检查 `deleteCurrentMedia` 调用 `ConsoleWidgetBuilder.currentMedia()`
- 预期结果: 所有委托调用正确
- 通过标准: 全部确认正确

---

## 三、影响范围测试

### TC-7: IA-1 Console Widget 渲染路径
- 对应: IA-1
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 验证 `renderAllFromStore` → `updateConsoleWidgets` → `ConsoleWidgetBuilder.build()` 调用链完整
  2. 验证 `build()` 返回 `RemoteViews` 对象
- 预期结果: 调用链完整，返回正确的 RemoteViews
- 通过标准: 代码审查通过

### TC-8: IA-2 People Widget 渲染路径
- 对应: IA-2
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 验证 `renderAllFromStore` → `updatePeopleWidgets` → `PeopleWidgetBuilder.build()` 调用链完整
  2. 验证 `build()` 返回 `RemoteViews` 对象
- 预期结果: 调用链完整，返回正确的 RemoteViews
- 通过标准: 代码审查通过

### TC-9: IA-3 PendingIntent 构建路径
- 对应: IA-3
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 验证 `ConsoleWidgetBuilder.bindConsoleActions()` 使用 `WidgetPendingIntentFactory.openMainIntent()` 和 `widgetBroadcast()`
  2. 验证 `ConsoleWidgetBuilder.bindSlot()` 使用 `WidgetPendingIntentFactory.slotIntent()`, `openMediaViewer()`, `openMediaEntry()`, `widgetBroadcast()`
  3. 验证 `PeopleWidgetBuilder.bindPeopleActions()` 使用 `WidgetPendingIntentFactory.widgetBroadcast()` 和 `openMediaEntry()`
- 预期结果: 所有 PendingIntent 构建通过工厂类
- 通过标准: 代码审查通过

### TC-10: IA-4 bindSlot 共享路径
- 对应: IA-4
- 端: Android
- 前置条件: 编译通过
- 步骤:
  1. 验证 `PeopleWidgetBuilder.build()` 通过 `with(ConsoleWidgetBuilder) { bindSlot(...) }` 调用共享的 bindSlot
- 预期结果: PeopleWidgetBuilder 正确复用 ConsoleWidgetBuilder 的 bindSlot
- 通过标准: 代码审查通过

---

## 四、跨模块回归测试

### TC-11: LifeConsoleViewModel 编译不报错
- 对应: 依赖映射 - 外部调用方
- 端: Android
- 前置条件: 编译通过
- 步骤: 检查 `LifeConsoleViewModel` 中对 `LifeConsoleWidgetProvider.applySnapshot()` 和 `refreshAll()` 的调用编译通过
- 预期结果: 无编译错误
- 通过标准: T-1 通过即可验证

### TC-12: 全量 Widget 包编译通过
- 对应: 依赖映射 - 回归范围
- 端: Android
- 前置条件: 编译通过
- 步骤: 确认 `widget/` 包下所有文件编译通过
- 预期结果: 无编译错误
- 通过标准: T-1 通过即可验证

---

## 测试用例汇总

| # | 测试名 | 类型 | 对应 |
|---|--------|------|------|
| T-1 | Android 编译通过 | 编译 | FR-7/AC-4 |
| T-2 | 新文件语法正确 | 编译 | FR-7/AC-1 |
| TC-1 | LifeConsoleWidgetProvider 行数验证 | 功能 | FR-7/AC-2 |
| TC-2 | ConsoleWidgetBuilder 结构验证 | 功能 | FR-7/AC-1 |
| TC-3 | PeopleWidgetBuilder 结构验证 | 功能 | FR-7/AC-1 |
| TC-4 | WidgetPendingIntentFactory 结构验证 | 功能 | FR-7/AC-1 |
| TC-5 | LifeConsoleWidgetProvider 瘦身验证 | 功能 | FR-7/AC-2 |
| TC-6 | Controller 委托调用正确 | 功能 | FR-7/AC-3 |
| TC-7 | IA-1 Console Widget 渲染路径 | 影响 | IA-1 |
| TC-8 | IA-2 People Widget 渲染路径 | 影响 | IA-2 |
| TC-9 | IA-3 PendingIntent 构建路径 | 影响 | IA-3 |
| TC-10 | IA-4 bindSlot 共享路径 | 影响 | IA-4 |
| TC-11 | LifeConsoleViewModel 编译不报错 | 回归 | 外部调用方 |
| TC-12 | 全量 Widget 包编译通过 | 回归 | 回归范围 |

**总计**: 14 个测试用例
- 编译测试: 2 个
- 功能测试: 6 个
- 影响范围测试: 4 个
- 跨模块回归测试: 2 个