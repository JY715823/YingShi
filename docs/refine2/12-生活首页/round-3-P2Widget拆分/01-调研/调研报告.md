# Round 3 调研报告

> 调研时间：2026-07-17
> 调研范围：FR-7 Widget Controller 拆分

---

## 一、代码定位

| 文件 | 行号 | 内容 |
|------|------|------|
| `widget/LifeConsoleWidgetProvider.kt` | L1-617 | 完整文件，包含 Provider + Controller + ViewBuilder + IntentFactory |

## 二、当前结构分析

### 2.1 LifeConsoleWidgetProvider (L1-62)

- AppWidgetProvider 子类，非常薄（仅 25 行）
- `onUpdate` → Controller.renderAllFromStore + fetchAndUpdateAll
- `onReceive` → Controller.handleReceive
- `companion object`: 常量 + refreshAll/applySnapshot 委托

### 2.2 LifeConsoleWidgetController (L64-617)

| 区域 | 行号 | 说明 |
|------|------|------|
| 常量 | L65-83 | actions, lanes, widgetScope |
| 公共 API | L85-200 | refreshAll, applySnapshot, handleReceive, renderAllFromStore, fetchAndUpdateAll |
| 删除逻辑 | L202-226 | deleteCurrentMedia |
| Widget 渲染 | L228-277 | updateConsoleWidgets, updatePeopleWidgets, buildConsoleViews, buildPeopleViews |
| Action 绑定 | L279-299 | bindConsoleActions, bindPeopleActions |
| 视图绑定 | L301-384 | bindSlot, bindBowel |
| 工具函数 | L386-430 | currentMedia, latestText, emptyHintFor, formatTime, formatAbsoluteTime, updateStatus, slotKeyOrNull |
| PendingIntent | L432-531 | openMainIntent, openMediaEntry, openMediaViewer, slotIntent, widgetBroadcast |
| 辅助函数 | L533-565 | requestCodeFor, laneFor, providerClassFor, widgetTodayDate, widgetRequestCode, initRuntime |
| SlotViews | L567-616 | data class + 4 个实例 |

### 2.3 依赖关系

```
LifeConsoleWidgetController
├── [PendingIntent 构建] openMainIntent, openMediaEntry, openMediaViewer, slotIntent, widgetBroadcast
│   └── 依赖: AppNavigationRequests, LifeConsoleWidgetProvider.EXTRA_CATEGORY
│   └── 依赖: requestCodeFor, laneFor, providerClassFor, widgetRequestCode
├── [Console 视图] buildConsoleViews
│   └── bindConsoleActions, bindSlot, bindBowel
├── [People 视图] buildPeopleViews
│   └── bindPeopleActions, bindSlot
├── [共享工具] formatTime, formatAbsoluteTime, latestText, emptyHintFor, currentMedia
├── [SlotViews] data class + 4个实例
└── [业务逻辑] refreshAll, applySnapshot, handleReceive, fetchAndUpdateAll, deleteCurrentMedia
```

## 三、拆分方案

### 3.1 WidgetPendingIntentFactory.kt

**提取内容** (L432-565):
- `openMainIntent`, `openMediaEntry`, `openMediaViewer`, `slotIntent`, `widgetBroadcast`
- `requestCodeFor`, `laneFor`, `providerClassFor`, `widgetRequestCode`

**依赖**:
- `AppNavigationRequests`
- `LifeConsoleWidgetProvider.EXTRA_CATEGORY`
- `LifeConsoleWidgetSlotKey`

### 3.2 ConsoleWidgetBuilder.kt

**提取内容** (L252-384, L386-430, L567-616):
- `buildConsoleViews`, `bindConsoleActions`
- `bindSlot`, `bindBowl`
- `SlotViews` data class + 4个实例
- `currentMedia`, `latestText`, `emptyHintFor`, `formatTime`, `formatAbsoluteTime`, `updateStatus`
- `widgetTodayDate`, `initRuntime`

**依赖**:
- `WidgetPendingIntentFactory` (openMainIntent, openMediaEntry, slotIntent, widgetBroadcast)
- `LifeConsoleWidgetStore`
- `isVideo` (from LifeConsoleUtils)

### 3.3 PeopleWidgetBuilder.kt

**提取内容** (L266-299):
- `buildPeopleViews`, `bindPeopleActions`

**依赖**:
- `ConsoleWidgetBuilder.bindSlot` (同包，直接调用)
- `WidgetPendingIntentFactory`

### 3.4 LifeConsoleWidgetProvider.kt (瘦身后)

**保留内容** (L1-226, L228-250):
- `LifeConsoleWidgetProvider` 类
- `LifeConsoleWidgetController` 对象
- 常量定义
- 公共 API: refreshAll, applySnapshot, handleReceive, renderAllFromStore, fetchAndUpdateAll
- deleteCurrentMedia
- updateConsoleWidgets, updatePeopleWidgets (委托给 Builder)

**预计行数**: ~200 行

## 四、技术约束

- 所有新建文件在 `feature/life/widget/` 包下
- 使用 `internal object` 模式（与现有 Controller 一致）
- Widget 行为 100% 不变
- 不影响 `LifeConsoleWidgetStore` 和 `LifeConsoleWidgetSlotKey`