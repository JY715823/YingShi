# Round 3 实施计划

> 基于: 需求文档 v1.0, 调研报告 v1.0
> 日期: 2026-07-17

---

## FR-7: Widget Controller 拆分

**目标**: 618行 LifeConsoleWidgetProvider.kt 拆分为 Provider + 3个 Builder/Factory，瘦身到 200 行以内

### 变更文件清单

| 文件 | 端 | 变更类型 | 变更内容 |
|------|-----|---------|---------|
| `widget/WidgetPendingIntentFactory.kt` | Android | **新建** | PendingIntent 构建逻辑 |
| `widget/ConsoleWidgetBuilder.kt` | Android | **新建** | Console Widget 视图构建 |
| `widget/PeopleWidgetBuilder.kt` | Android | **新建** | People Widget 视图构建 |
| `widget/LifeConsoleWidgetProvider.kt` | Android | 修改 | 瘦身到 200 行以内 |

### 实施细节

#### WidgetPendingIntentFactory.kt

```kotlin
internal object WidgetPendingIntentFactory {
    fun openMainIntent(...): PendingIntent { ... }
    fun openMediaEntry(...): PendingIntent { ... }
    fun openMediaViewer(...): PendingIntent { ... }
    fun slotIntent(...): PendingIntent { ... }
    fun widgetBroadcast(...): PendingIntent { ... }
    fun requestCodeFor(slotKey: LifeConsoleWidgetSlotKey, base: Int): Int { ... }
    fun laneFor(slotKey: LifeConsoleWidgetSlotKey): String { ... }
    fun providerClassFor(slotKey: LifeConsoleWidgetSlotKey): Class<out AppWidgetProvider> { ... }
    fun widgetRequestCode(lane: String, requestCode: Int): Int { ... }
}
```

#### ConsoleWidgetBuilder.kt

```kotlin
internal object ConsoleWidgetBuilder {
    data class SlotViews(...)
    val PersonSelfViews = SlotViews(...)
    val PersonPartnerViews = SlotViews(...)
    val MealSelfViews = SlotViews(...)
    val MealPartnerViews = SlotViews(...)
    
    fun build(context: Context, status: String, snapshot: RemoteLifeConsoleToday?): RemoteViews { ... }
    fun RemoteViews.bindConsoleActions(context: Context) { ... }
    fun RemoteViews.bindSlot(context: Context, snapshot: RemoteLifeConsoleToday?, slotKey: LifeConsoleWidgetSlotKey, views: SlotViews) { ... }
    fun RemoteViews.bindBowel(snapshot: RemoteLifeConsoleToday?) { ... }
    // 工具函数: currentMedia, latestText, emptyHintFor, formatTime, formatAbsoluteTime, updateStatus, widgetTodayDate, initRuntime
}
```

#### PeopleWidgetBuilder.kt

```kotlin
internal object PeopleWidgetBuilder {
    fun build(context: Context, status: String, snapshot: RemoteLifeConsoleToday?): RemoteViews { ... }
    fun RemoteViews.bindPeopleActions(context: Context) { ... }
}
```

#### LifeConsoleWidgetProvider.kt (瘦身后)

保留:
- `LifeConsoleWidgetProvider` 类 (L1-62)
- `LifeConsoleWidgetController` 对象
  - 常量 (L64-83)
  - 公共 API (L85-200)
  - deleteCurrentMedia (L202-226)
  - updateConsoleWidgets → 委托 `ConsoleWidgetBuilder.build()`
  - updatePeopleWidgets → 委托 `PeopleWidgetBuilder.build()`
  - slotKeyOrNull (L428-430)

删除:
- buildConsoleViews, buildPeopleViews, bindConsoleActions, bindPeopleActions
- bindSlot, bindBowel
- 所有 PendingIntent 函数
- 所有工具函数
- SlotViews 数据类和实例

### 实施顺序

1. 新建 `WidgetPendingIntentFactory.kt`
2. 新建 `ConsoleWidgetBuilder.kt`
3. 新建 `PeopleWidgetBuilder.kt`
4. 修改 `LifeConsoleWidgetProvider.kt`（瘦身）
5. 编译验证