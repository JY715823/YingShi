# Round 2 测试计划

> 测试时间: 2026-07-10
> 轮次主题: Android 代码质量提升
> 包含需求: FR-3（颜色 token 化）、FR-4（文件拆分）、FR-5（内存泄漏修复）、FR-6（文档补齐）
> 前置文档: 实施计划.md、影响分析.md、依赖映射.md、需求文档.md

---

## 测试范围说明

本轮全部为 Android + Docs 变更，无 Server 代码改动。因此：
- T-2（Server 编译）和 T-3（Docker 容器健康）标记为 N/A
- 功能测试聚焦 Android 编译验证 + 代码审查 + Compose Preview 验证
- 影响范围测试覆盖 IA-1 ~ IA-9
- 跨模块回归聚焦 09-回收站、04-照片根目录、15-外壳

---

## 一、编译/基础设施测试

### TC-T1: Android 全量编译通过
- 对应: NFR-2 / 所有 FR 的编译 AC
- 端: Android
- 前置条件: 项目无未保存的改动
- 步骤/命令:
  ```powershell
  cd E:\Study\App\YingShi
  .\gradlew.bat assembleDebug
  ```
- 预期结果: `BUILD SUCCESSFUL`，无编译错误，无警告升级为错误
- 通过标准: 退出码 0 + 输出包含 "BUILD SUCCESSFUL"

### TC-T2: Server 编译验证（N/A）
- 对应: NFR-2
- 端: Server
- 说明: 本轮无 Server 代码改动，跳过
- 结果: N/A

### TC-T3: Docker 容器健康（N/A）
- 对应: NFR-2
- 端: Server
- 说明: 本轮无 Server 代码改动，跳过
- 结果: N/A

---

## 二、功能测试

### FR-3: AlbumInlineActionButton 颜色 token 化

#### TC-F3-01: 正向 — danger 分支无硬编码颜色（AC-1, AC-2）
- 对应: FR-3/AC-1, FR-3/AC-2
- 端: Android
- 前置条件: AlbumDirectoryDialog.kt 已包含 AlbumInlineActionButton
- 步骤/命令:
  ```powershell
  # 搜索 AlbumInlineActionButton 函数体中是否还有 Color(0xFF...) 硬编码
  Select-String -Path "E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\AlbumDirectoryDialog.kt" -Pattern "Color\(0xFF" -SimpleMatch
  ```
  再用 Grep 工具确认 `AlbumInlineActionButton` 函数体（L458-501）中：
  - 背景色使用 `colors.destructiveContainer`（L476）
  - 文字色使用 `colors.destructive`（L492）
- 预期结果: 函数体内无 `Color(0xFF...)` 硬编码；背景和文字 danger 分支分别引用 token
- 通过标准: 搜索无匹配 + 代码审查确认 token 引用

#### TC-F3-02: 边界 — 色值视觉一致性（AC-3）
- 对应: FR-3/AC-3
- 端: Android
- 前置条件: Tokens.kt 和 Color.kt 中 destructive token 已定义
- 步骤/命令:
  | 位置 | 原色值 | 新 token | token 色值 | 色系对比 |
  |------|--------|---------|-----------|---------|
  | 背景 | `#F7D7D7` (浅粉) | `destructiveContainer` | `#FFDAD4` (浅珊瑚) | 同属浅红系，视觉相近 |
  | 文字 | `#9E2A2B` (深红) | `destructive` | `#BA1A1A` (Material 红) | 同属深红系，视觉相近 |
- 预期结果: 新旧色值同属红色系，视觉意图一致（danger/destructive 语义）
- 通过标准: 色值对比确认同色系 + Compose Preview 渲染无突兀差异

#### TC-F3-03: 编译通过（AC-4）
- 对应: FR-3/AC-4
- 端: Android
- 前置条件: FR-3 代码改动已完成
- 步骤/命令:
  ```powershell
  .\gradlew.bat :app:compileDebugKotlin
  ```
- 预期结果: `BUILD SUCCESSFUL`
- 通过标准: 编译无错误

---

### FR-4: AlbumPageScreen 文件拆分

#### TC-F4-01: 正向 — 主文件行数 ≤ 600（AC-1）
- 对应: FR-4/AC-1
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  ```powershell
  (Get-Content "E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\AlbumPageScreen.kt" | Measure-Object -Line).Lines
  ```
- 预期结果: 行数 ≤ 600（实际: 553）
- 通过标准: 数值 ≤ 600

#### TC-F4-02: 正向 — AlbumDirectoryDialog.kt 存在且包含目录弹窗函数（AC-2）
- 对应: FR-4/AC-2
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  - 确认文件存在: `E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\AlbumDirectoryDialog.kt`
  - 确认包含函数: `AlbumDirectoryDialog`、`AlbumDirectoryManagementCard`、`AlbumSearchField`、`AlbumDirectoryRow`、`AlbumInlineActionButton`、`AlbumDialogActionButton`
- 预期结果: 文件存在（506 行），6 个函数全部存在
- 通过标准: 文件存在 + 函数清单完整

#### TC-F4-03: 正向 — AlbumPostCard.kt 存在且包含卡片函数（AC-3）
- 对应: FR-4/AC-3
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  - 确认文件存在: `E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\AlbumPostCard.kt`
  - 确认包含函数: `AlbumPostCard`、`AlbumPostPreviewThumbnail`、`PostDetailPlaceholderScreen`、`DetailMetaCapsule`、`formatAlbumPostTime`、`PostDetailPlaceholderScreenPreview`
- 预期结果: 文件存在（403 行），6 个函数全部存在
- 通过标准: 文件存在 + 函数清单完整

#### TC-F4-04: 正向 — AlbumSwitchSection.kt 存在且包含切换区域函数（AC-4）
- 对应: FR-4/AC-4
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  - 确认文件存在: `E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\AlbumSwitchSection.kt`
  - 确认包含函数: `AlbumSwitchSection`、`AlbumSwitchChip`、`AlbumIconAction`、`formatAlbumChipTitle`、`preferredVisibleAlbums`
- 预期结果: 文件存在（264 行），5 个函数全部存在
- 通过标准: 文件存在 + 函数清单完整

#### TC-F4-05: 正向 — 跨文件调用的 Composable 为 internal（AC-5）
- 对应: FR-4/AC-5
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  | 函数名 | 所在文件 | 应有可见性 | 验证方法 |
  |--------|---------|-----------|---------|
  | `AlbumSwitchSection` | AlbumSwitchSection.kt | internal | Grep `internal fun AlbumSwitchSection` |
  | `AlbumSwitchChip` | AlbumSwitchSection.kt | internal | Grep `internal fun AlbumSwitchChip` |
  | `AlbumIconAction` | AlbumSwitchSection.kt | internal | Grep `internal fun AlbumIconAction` |
  | `AlbumDirectoryDialog` | AlbumDirectoryDialog.kt | internal | Grep `internal fun AlbumDirectoryDialog` |
  | `AlbumInlineActionButton` | AlbumDirectoryDialog.kt | internal | Grep `internal fun AlbumInlineActionButton` |
  | `AlbumPostCard` | AlbumPostCard.kt | internal | Grep `internal fun AlbumPostCard` |
  | `PostDetailPlaceholderScreen` | AlbumPostCard.kt | public | Grep `fun PostDetailPlaceholderScreen`（无 internal/private）|
- 预期结果: 所有跨文件调用的函数为 `internal fun`，PostDetailPlaceholderScreen 为 public
- 通过标准: 7 个函数可见性全部正确

#### TC-F4-06: 正向 — 编译通过（AC-6）
- 对应: FR-4/AC-6
- 端: Android
- 前置条件: 所有拆分文件已创建
- 步骤/命令:
  ```powershell
  .\gradlew.bat assembleDebug
  ```
- 预期结果: `BUILD SUCCESSFUL`
- 通过标准: 编译无错误

#### TC-F4-07: 边界 — 死代码 AlbumGridDensitySwitcher 已删除（AC-7 附属）
- 对应: FR-4/AC-7
- 端: Android
- 前置条件: 拆分完成
- 步骤/命令:
  ```powershell
  # 全代码库搜索 AlbumGridDensitySwitcher
  Select-String -Path "E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\photos\*.kt" -Pattern "AlbumGridDensitySwitcher" -SimpleMatch
  ```
- 预期结果: 无匹配（死代码已彻底删除）
- 通过标准: 搜索结果为空

#### TC-F4-08: 边界 — 功能行为完全不变（AC-7）
- 对应: FR-4/AC-7
- 端: Android
- 前置条件: 编译通过
- 步骤/命令:
  - Compose Preview 验证 `AlbumPageScreenPreview` 正常渲染
  - 代码审查确认：AlbumPageStateStore、AlbumPageScreen、RealAlbumPageScreen 逻辑完全保留
  - 代码审查确认：所有迁出函数的函数体未被修改（仅可见性可能从 private 提升为 internal）
- 预期结果: Preview 正常渲染，逻辑代码无功能变更
- 通过标准: Preview 无报错 + 代码审查确认

---

### FR-5: RealTrashMemoryCache 内存泄漏修复

#### TC-F5-01: 正向 — LRU 限制常量存在（AC-1）
- 对应: FR-5/AC-1
- 端: Android
- 前置条件: RealTrashViewModels.kt 已修改
- 步骤/命令:
  - Grep `MAX_LIST_STATES` 确认值为 20
  - Grep `MAX_DETAIL_STATES` 确认值为 50
- 预期结果: `private const val MAX_LIST_STATES = 20` 和 `private const val MAX_DETAIL_STATES = 50` 存在
- 通过标准: 两个常量存在且值正确

#### TC-F5-02: 正向 — LinkedHashMap accessOrder=true（AC-2）
- 对应: FR-5/AC-2
- 端: Android
- 前置条件: LRU 改动已完成
- 步骤/命令:
  - 代码审查确认 `listStates` 和 `detailStates` 为 `LinkedHashMap` 匿名子类
  - 确认构造参数 `accessOrder = true`（第三个参数）
  - 确认 `removeEldestEntry` 重写逻辑: `size > MAX_*_STATES`
- 预期结果:
  ```kotlin
  private val listStates = object : LinkedHashMap<String, RealTrashListUiState>(16, 0.75f, true) {
      override fun removeEldestEntry(eldest: MutableMap.MutableEntry<String, RealTrashListUiState>?): Boolean =
          size > MAX_LIST_STATES
  }
  ```
- 通过标准: 两个 map 都使用 LinkedHashMap + accessOrder=true + removeEldestEntry

#### TC-F5-03: 边界 — 读写方法签名不变（AC-2）
- 对应: FR-5/AC-2
- 端: Android
- 前置条件: LRU 改动已完成
- 步骤/命令:
  - 确认 `readListState`、`writeListState`、`readDetailState`、`writeDetailState`、`hasListState`、`listKey` 方法签名未变
  - 确认 `readListState` 返回 `listStates[listKey(selectedType)]`（触发 accessOrder 更新）
- 预期结果: 方法签名和调用方式完全不变，对调用方透明
- 通过标准: RealTrashListViewModel 和 RealTrashDetailViewModel 无需修改

#### TC-F5-04: 编译通过（AC-3）
- 对应: FR-5/AC-3
- 端: Android
- 前置条件: FR-5 代码改动已完成
- 步骤/命令:
  ```powershell
  .\gradlew.bat :app:compileDebugKotlin
  ```
- 预期结果: `BUILD SUCCESSFUL`
- 通过标准: 编译无错误

---

### FR-6: album-api.md 文档补齐

#### TC-F6-01: 正向 — PATCH 端点文档完整（AC-1）
- 对应: FR-6/AC-1
- 端: Docs
- 前置条件: album-api.md 已创建
- 步骤/命令:
  - 读取 `E:\Study\App\YingShi\docs\contracts\album-api.md`
  - 确认包含 `## PATCH /api/albums/{albumId}` 章节
  - 确认包含请求体 `UpdateAlbumRequest` 字段说明
  - 确认包含响应体 `AlbumDto` 字段说明
  - 确认包含 400/404 错误码
- 预期结果: PATCH 端点文档完整覆盖请求/响应/错误码
- 通过标准: 5 项内容全部存在

#### TC-F6-02: 正向 — DELETE 端点文档完整（AC-2）
- 对应: FR-6/AC-2
- 端: Docs
- 前置条件: album-api.md 已创建
- 步骤/命令:
  - 确认包含 `## DELETE /api/albums/{albumId}` 章节
  - 确认说明无请求体
  - 确认包含响应体 `TrashItemDto` 字段说明
  - 确认包含 404 错误码
- 预期结果: DELETE 端点文档完整覆盖请求/响应/错误码
- 通过标准: 4 项内容全部存在

#### TC-F6-03: 边界 — 文档与实际代码一致（AC-3）
- 对应: FR-6/AC-3
- 端: Docs
- 前置条件: album-api.md 和 AlbumController.java 都可读
- 步骤/命令:
  - 对比文档中端点路径与 `AlbumController.java` 的 `@RequestMapping`/`@GetMapping`/`@PostMapping`/`@PatchMapping`/`@DeleteMapping` 注解
  - 对比文档中请求体字段与 `CreateAlbumRequest.java`、`UpdateAlbumRequest.java` 的字段
  - 对比文档中响应体字段与 `AlbumDto.java`、`PostSummaryDto.java`、`TrashItemDto.java` 的字段
- 预期结果: 文档端点路径、请求体字段、响应体字段与实际代码完全一致
- 通过标准: 5 个端点路径 + 2 个请求体 + 3 个响应体全部匹配

---

## 三、影响范围测试

### TC-IA-01: danger 按钮在多处调用点的视觉表现（IA-1）
- 对应: IA-1
- 端: Android
- 前置条件: FR-3 完成，编译通过
- 步骤/命令:
  - 代码审查确认 4 个调用点使用 `danger = true`:
    1. `AlbumPageNoticeCard` 的"重试"按钮
    2. `AlbumEmptyStateCard` 的"创建大相册"按钮（非 danger，验证正常分支）
    3. `AlbumDirectoryManagementCard` 的"重命名/删除"按钮
    4. `AlbumDialogActionButton` 的 danger 传递
  - Compose Preview 渲染验证 danger 按钮颜色为红色系
- 预期结果: danger=true 时背景为 `destructiveContainer`（#FFDAD4），文字为 `destructive`（#BA1A1A）
- 通过标准: 代码审查 + Preview 视觉确认

### TC-IA-02: 跨文件引用编译验证（IA-2）
- 对应: IA-2
- 端: Android
- 前置条件: FR-4 拆分完成
- 步骤/命令:
  ```powershell
  .\gradlew.bat assembleDebug
  ```
  确认无 "unresolved reference" 错误
- 预期结果: 所有跨文件引用（File1→File2、File1→File3、File1→File4、File4→File2、File2→File4）编译通过
- 通过标准: 编译无 unresolved reference

### TC-IA-03: 大相册目录页 UI 行为不变（IA-3）
- 对应: IA-3
- 端: Android
- 前置条件: 编译通过
- 步骤/命令:
  - 代码审查确认 `AlbumPageScreen` 和 `RealAlbumPageScreen` 的状态管理逻辑完全保留:
    1. `AlbumPageStateStore.pendingSelectedAlbumId` / `pendingUpdatedPostId` 处理
    2. `densityName` + `gridDensity` 网格密度切换
    3. `gridState` + `filteredPosts` + `recentlyUpdatedPostId` 滚动定位
    4. `backendMutationEvent` 变更通知监听
    5. `syncStaleState` 过期刷新
    6. `ReconnectRefreshEffect` 重连刷新
    7. `statusMessage` 自动消失
  - Compose Preview 验证 `AlbumPageScreenPreview` 正常渲染
- 预期结果: 所有状态管理逻辑保留，UI 行为不变
- 通过标准: 7 项状态管理逻辑全部保留 + Preview 正常

### TC-IA-04: PostDetailPlaceholderScreen 导航入口正常（IA-4）
- 对应: IA-4
- 端: Android
- 前置条件: 编译通过
- 步骤/命令:
  - 确认 `PostDetailPlaceholderScreen` 在 AlbumPostCard.kt 中为 `public fun`（非 internal/private）
  - Grep 确认 YingShiApp.kt 中引用 `PostDetailPlaceholderScreen` 无需 import（同包）或 import 路径不变
- 预期结果: 导航入口可见性正确，引用路径不变
- 通过标准: public 可见性 + 编译通过

### TC-IA-05: AlbumDirectoryDialog 弹窗完整流程（IA-5）
- 对应: IA-5
- 端: Android
- 前置条件: 编译通过
- 步骤/命令:
  - 代码审查确认 AlbumDirectoryDialog 完整流程保留:
    1. 打开: AlbumSwitchSection 中 Menu 按钮 → `showAlbumMenu = true`
    2. 搜索: `AlbumSearchField` 输入过滤
    3. 选择: `AlbumDirectoryRow` 点击 → `onSelectAlbum`
    4. 管理: `AlbumDirectoryManagementCard` 展开
    5. 重命名: AlertDialog 确认 → `onRenameSelectedAlbum`
    6. 删除: AlertDialog 确认 → `onDeleteSelectedAlbum`
    7. 新建: `onCreateLargeAlbum`
    8. 关闭: `onDismiss`
- 预期结果: 8 个交互流程全部保留
- 通过标准: 代码审查确认 8 个流程完整

### TC-IA-06: 回收站列表缓存命中（≤20 条）（IA-6）
- 对应: IA-6
- 端: Android
- 前置条件: FR-5 完成
- 步骤/命令:
  - 代码审查确认 `RealTrashListViewModel.refresh()` 先调 `readListState` 再决定是否网络加载
  - 确认切换不同 TrashEntryType 时，已访问的类型缓存命中（≤20 种类型内）
- 预期结果: 切换类型时若缓存存在，直接使用缓存（isLoading=false）
- 通过标准: 代码审查确认缓存读取逻辑不变

### TC-IA-07: 回收站详情缓存命中（≤50 条）（IA-7）
- 对应: IA-7
- 端: Android
- 前置条件: FR-5 完成
- 步骤/命令:
  - 代码审查确认 `RealTrashDetailViewModel.loadDetail()` 先调 `readDetailState` 再决定是否网络加载
  - 确认打开详情后返回再打开同一详情，缓存命中（≤50 个详情内）
- 预期结果: 重复打开同一详情时直接使用缓存
- 通过标准: 代码审查确认缓存读取逻辑不变

### TC-IA-08: LRU 淘汰后重新加载（IA-8）
- 对应: IA-8
- 端: Android
- 前置条件: FR-5 完成
- 步骤/命令:
  - 代码审查确认 `LinkedHashMap(accessOrder=true)` 的行为:
    1. 写入第 21 个 listState 时，第 1 个（最久未访问）被自动淘汰
    2. 写入第 51 个 detailState 时，最久未访问的被自动淘汰
    3. 被淘汰后再次访问该 key 时 `readListState`/`readDetailState` 返回 null
    4. 返回 null 后 ViewModel 发起网络加载
- 预期结果: 超过上限时自动淘汰最久未访问的条目，再次访问时重新加载
- 通过标准: 代码审查确认 LRU 淘汰链路完整

### TC-IA-09: 文档与代码一致性验证（IA-9）
- 对应: IA-9
- 端: Docs
- 前置条件: album-api.md 和 Server 代码都可读
- 步骤/命令:
  - 对比文档端点清单与 AlbumController.java 的 5 个端点:
    1. `POST /api/albums` ← `@PostMapping("/api/albums")`
    2. `GET /api/albums` ← `@GetMapping("/api/albums")`
    3. `GET /api/albums/{albumId}/small-albums` ← `@GetMapping("/api/albums/{albumId}/small-albums")`
    4. `PATCH /api/albums/{albumId}` ← `@PatchMapping("/api/albums/{albumId}")`
    5. `DELETE /api/albums/{albumId}` ← `@DeleteMapping("/api/albums/{albumId}")`
- 预期结果: 5 个端点路径完全匹配
- 通过标准: 5/5 端点路径一致

---

## 四、跨模块回归测试

### TC-RG-01: 09-回收站 — RealTrashMemoryCache LRU 不影响正常读写
- 对应: 依赖映射 — 09-回收站
- 端: Android
- 前置条件: FR-5 完成
- 步骤/命令:
  - 确认 `RealTrashListViewModel` 和 `RealTrashDetailViewModel` 无代码改动（LRU 对调用方透明）
  - 编译验证: `.\gradlew.bat assembleDebug`
  - 代码审查: `readListState`/`writeListState`/`readDetailState`/`writeDetailState` 方法签名不变
- 预期结果: 回收站 ViewModel 无需修改，编译通过
- 通过标准: 编译通过 + ViewModel 代码无改动

### TC-RG-02: 04-照片根目录 — AlbumPageStateStore 引用不变
- 对应: 依赖映射 — 04-照片根目录
- 端: Android
- 前置条件: FR-4 拆分完成
- 步骤/命令:
  - Grep 确认 `AlbumPageStateStore` 仍在 AlbumPageScreen.kt 中定义为 `object`
  - Grep 确认 04-照片根目录模块中对 `AlbumPageStateStore.pendingSelectedAlbumId` 的引用编译通过
  - 编译验证: `.\gradlew.bat assembleDebug`
- 预期结果: AlbumPageStateStore 定义位置不变，引用正常
- 通过标准: 编译通过 + 引用无 unresolved

### TC-RG-03: 15-外壳 — YingShiApp.kt 导航不变
- 对应: 依赖映射 — 15-外壳
- 端: Android
- 前置条件: FR-4 拆分完成
- 步骤/命令:
  - Grep 确认 YingShiApp.kt 中对 `AlbumPageScreen`、`PostDetailPlaceholderScreen`、`AlbumPageStateStore` 的引用
  - 确认这些符号的 package 仍为 `com.example.yingshi.feature.photos`（未跨包迁移）
  - 编译验证: `.\gradlew.bat assembleDebug`
- 预期结果: 导航入口引用正常，无 import 路径变更
- 通过标准: 编译通过 + 无 import 错误

---

## 五、测试用例汇总

| 用例编号 | 类型 | 对应 | 端 | 预期结果 |
|---------|------|------|-----|---------|
| TC-T1 | 编译 | NFR-2 | Android | assembleDebug BUILD SUCCESSFUL |
| TC-T2 | 编译 | NFR-2 | Server | N/A（无 Server 改动）|
| TC-T3 | 编译 | NFR-2 | Server | N/A（无 Server 改动）|
| TC-F3-01 | 功能正向 | FR-3/AC-1,2 | Android | danger 分支无硬编码，使用 token |
| TC-F3-02 | 功能边界 | FR-3/AC-3 | Android | 色值同属红色系，视觉相近 |
| TC-F3-03 | 功能编译 | FR-3/AC-4 | Android | compileDebugKotlin 通过 |
| TC-F4-01 | 功能正向 | FR-4/AC-1 | Android | AlbumPageScreen.kt ≤600 行 |
| TC-F4-02 | 功能正向 | FR-4/AC-2 | Android | AlbumDirectoryDialog.kt 存在含 6 函数 |
| TC-F4-03 | 功能正向 | FR-4/AC-3 | Android | AlbumPostCard.kt 存在含 6 函数 |
| TC-F4-04 | 功能正向 | FR-4/AC-4 | Android | AlbumSwitchSection.kt 存在含 5 函数 |
| TC-F4-05 | 功能正向 | FR-4/AC-5 | Android | 7 个函数可见性正确 |
| TC-F4-06 | 功能编译 | FR-4/AC-6 | Android | assembleDebug 通过 |
| TC-F4-07 | 功能边界 | FR-4/AC-7 | Android | AlbumGridDensitySwitcher 零引用 |
| TC-F4-08 | 功能边界 | FR-4/AC-7 | Android | 功能行为不变 |
| TC-F5-01 | 功能正向 | FR-5/AC-1 | Android | MAX_LIST_STATES=20, MAX_DETAIL_STATES=50 |
| TC-F5-02 | 功能正向 | FR-5/AC-2 | Android | LinkedHashMap accessOrder=true + removeEldestEntry |
| TC-F5-03 | 功能边界 | FR-5/AC-2 | Android | 读写方法签名不变 |
| TC-F5-04 | 功能编译 | FR-5/AC-3 | Android | compileDebugKotlin 通过 |
| TC-F6-01 | 功能正向 | FR-6/AC-1 | Docs | PATCH 端点文档完整 |
| TC-F6-02 | 功能正向 | FR-6/AC-2 | Docs | DELETE 端点文档完整 |
| TC-F6-03 | 功能边界 | FR-6/AC-3 | Docs | 文档与代码一致 |
| TC-IA-01 | 影响范围 | IA-1 | Android | danger 按钮视觉表现正确 |
| TC-IA-02 | 影响范围 | IA-2 | Android | 跨文件引用编译通过 |
| TC-IA-03 | 影响范围 | IA-3 | Android | 目录页 UI 行为不变 |
| TC-IA-04 | 影响范围 | IA-4 | Android | PostDetailPlaceholderScreen 导航正常 |
| TC-IA-05 | 影响范围 | IA-5 | Android | AlbumDirectoryDialog 8 个流程完整 |
| TC-IA-06 | 影响范围 | IA-6 | Android | 列表缓存命中（≤20）|
| TC-IA-07 | 影响范围 | IA-7 | Android | 详情缓存命中（≤50）|
| TC-IA-08 | 影响范围 | IA-8 | Android | LRU 淘汰后重新加载 |
| TC-IA-09 | 影响范围 | IA-9 | Docs | 文档端点与代码一致 |
| TC-RG-01 | 跨模块回归 | 09-回收站 | Android | RealTrashMemoryCache 透明兼容 |
| TC-RG-02 | 跨模块回归 | 04-照片根目录 | Android | AlbumPageStateStore 引用不变 |
| TC-RG-03 | 跨模块回归 | 15-外壳 | Android | YingShiApp.kt 导航不变 |

**总计**: 33 个测试用例（含 2 个 N/A）
- 编译/基础设施: 3（1 有效 + 2 N/A）
- 功能测试: 19
- 影响范围测试: 9
- 跨模块回归测试: 3（Android）

---

## 六、测试执行优先级

| 优先级 | 用例 | 理由 |
|--------|------|------|
| P0 | TC-T1, TC-F4-06 | 全量编译是所有测试的基础 |
| P0 | TC-F4-01~05, TC-F4-07 | FR-4 拆分完整性验证 |
| P0 | TC-F3-01, TC-F5-01, TC-F5-02 | FR-3/FR-5 核心改动验证 |
| P1 | TC-IA-02~05, TC-IA-08 | 影响范围关键路径 |
| P1 | TC-RG-01~03 | 跨模块回归 |
| P2 | TC-F3-02, TC-F4-08, TC-IA-01, TC-IA-06, TC-IA-07 | 视觉/行为一致性 |
| P2 | TC-F6-01~03, TC-IA-09 | 文档验证 |
