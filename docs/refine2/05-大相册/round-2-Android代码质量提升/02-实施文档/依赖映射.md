# Round 2 依赖映射

> 映射时间: 2026-07-10
> 映射范围: FR-3 + FR-4 + FR-5 + FR-6

## 一、模块依赖关系

### 本模块依赖的外部模块

| 依赖模块 | 依赖方向 | 依赖内容 | 本轮是否改动 |
|---------|---------|---------|------------|
| 04-照片根目录 | 05 → 04 | 共享 AlbumPageStateStore、mutation bus | 否 |
| 09-回收站 | 05 → 09 | RealTrashMemoryCache 被回收站 ViewModel 使用 | 是（FR-5）|
| 15-外壳与基础设备 | 05 → 15 | YingShiApp.kt 导航、YingShiThemeTokens | 否 |

### 依赖本模块的外部调用方

| 调用方模块 | 调用内容 | 本轮是否影响 |
|-----------|---------|------------|
| YingShiApp.kt（15-外壳）| `AlbumPageStateStore`、`AlbumPageScreen`、`PostDetailPlaceholderScreen` | 否（同包，路径不变）|
| 04-照片根目录 | `AlbumPageStateStore.pendingSelectedAlbumId` | 否 |
| 09-回收站 | `RealTrashMemoryCache`（FR-5 改动）| 是（LRU 淘汰，透明兼容）|

## 二、FR-4 文件依赖树

### 拆分后文件依赖图

```
AlbumPageScreen.kt (File 1)
├── AlbumSwitchSection.kt (File 4)
│   ├── AlbumDirectoryDialog.kt (File 2) [弹窗触发]
│   │   ├── AlbumInlineActionButton (File 2) [内部]
│   │   └── AlbumIconAction (File 4) [跨文件调用]
│   └── AlbumIconAction (File 4) [内部]
├── AlbumPostCard.kt (File 3)
│   └── PostDetailPlaceholderScreen (File 3) [public 导航入口]
└── AlbumInlineActionButton (File 2) [跨文件调用: AlbumPageNoticeCard + AlbumEmptyStateCard]
```

### 跨文件调用清单

| 调用方 → 被调用方 | 调用点 | 可见性 |
|------------------|--------|--------|
| File 1 → File 2 `AlbumInlineActionButton` | AlbumPageNoticeCard L549、AlbumEmptyStateCard L635 | internal |
| File 1 → File 3 `AlbumPostCard` | AlbumPageScreen L214、RealAlbumPageScreen L456 | internal |
| File 1 → File 4 `AlbumSwitchSection` | AlbumPageScreen L161、RealAlbumPageScreen L377 | internal |
| File 4 → File 2 `AlbumDirectoryDialog` | AlbumSwitchSection L712 | internal |
| File 2 → File 4 `AlbumIconAction` | AlbumDirectoryDialog L822 | internal |

## 三、前后端共享组件依赖

| 共享组件 | 本轮是否改动 | 影响 |
|---------|------------|------|
| `YingShiThemeTokens` | 否 | FR-3 仅使用，不修改 |
| `YingShiToolSurface` | 否 | File 4 使用，不修改 |
| `YingShiMediaFrame` | 否 | File 3 使用，不修改 |
| `AppContentMediaThumbnail` | 否 | File 3 使用，不修改 |
| `VideoMediaMarker` | 否 | File 3 使用，不修改 |
| `YingShiMemoryBadge` | 否 | File 3 使用，不修改 |
| `yingShiClickable` | 否 | 多文件使用，不修改 |

## 四、数据流图

### FR-5 RealTrashMemoryCache 数据流

```
RealTrashListViewModel.refresh()
  → RealTrashMemoryCache.readListState()  [LRU 更新 accessOrder]
  → RealTrashMemoryCache.writeListState() [超限自动淘汰]
  → UI State 更新

RealTrashDetailViewModel.loadDetail()
  → RealTrashMemoryCache.readDetailState()  [LRU 更新 accessOrder]
  → RealTrashMemoryCache.writeDetailState() [超限自动淘汰]
  → UI State 更新
```

## 五、回归测试范围

### 后端回归（无）

本轮全部为 Android + Docs 变更，无服务端代码改动。

### Android 回归

| 测试范围 | 关联 FR | 验证方法 |
|---------|---------|---------|
| 大相册目录页 UI | FR-3/FR-4 | 编译 + Compose Preview |
| 回收站缓存行为 | FR-5 | 编译 |
| PostDetailPlaceholder 导航 | FR-4 | 编译（YingShiApp.kt 引用不变）|
| AlbumDirectoryDialog 弹窗 | FR-4 | 编译（同包 internal）|

### 跨模块回归

| 模块 | 验证点 | 方法 |
|------|--------|------|
| 09-回收站 | RealTrashMemoryCache LRU 不影响正常读写 | 编译 + 手动验证 |
| 04-照片根目录 | AlbumPageStateStore 引用不变 | 编译 |
| 15-外壳 | YingShiApp.kt 导航不变 | 编译 |
