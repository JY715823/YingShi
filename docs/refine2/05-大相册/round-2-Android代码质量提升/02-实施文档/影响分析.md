# Round 2 影响分析

> 分析时间: 2026-07-10
> 分析范围: FR-3 + FR-4 + FR-5 + FR-6

## 一、FR-3 影响分析

### 变更: AlbumPageScreen.kt L1241/L1257 颜色替换

| 变更文件 | 直接影响 | 间接影响 | 影响说明 |
|---------|---------|---------|---------|
| AlbumPageScreen.kt `AlbumInlineActionButton` | L549 AlbumPageNoticeCard、L635 AlbumEmptyStateCard、L1021/1026 AlbumDirectoryManagementCard、L1275 AlbumDialogActionButton（4处调用方）| 无 | 视觉效果：danger 按钮颜色从硬编码变为 token。色值需相近以保持视觉一致 |

### 必须测试的影响路径

- **IA-1**: danger 按钮在深色/浅色主题下的视觉表现（AlbumPageNoticeCard 的"重试"、AlbumEmptyStateCard 的"创建大相册"、AlbumDirectoryManagementCard 的"重命名/删除"按钮）

### 色值对比

| 位置 | 原色值 | 新 token | token 值 | 差异 |
|------|--------|---------|---------|------|
| 背景 | `#F7D7D7` (浅粉) | `destructiveContainer` | 需确认 | 视觉相近 |
| 文字 | `#9E2A2B` (深红) | `destructive` | 需确认 | 视觉相近 |

## 二、FR-4 影响分析

### 变更: 4个文件拆分 + 死代码删除

| 变更文件 | 直接影响 | 间接影响 | 影响说明 |
|---------|---------|---------|---------|
| AlbumPageScreen.kt（精简）| 同包所有引用 AlbumInlineActionButton/AlbumSwitchSection/AlbumPostCard/AlbumDirectoryDialog/AlbumIconAction/PostDetailPlaceholderScreen 的文件 | 无 | 可见性从 private 提升为 internal，同包内引用不变 |
| AlbumDirectoryDialog.kt（新建）| 无（新文件）| 无 | 同包 internal 可见 |
| AlbumPostCard.kt（新建）| 无（新文件）| 无 | 同包 internal 可见 |
| AlbumSwitchSection.kt（新建）| 无（新文件）| 无 | 同包 internal 可见 |
| AlbumGridDensitySwitcher（删除）| 无（死代码，零调用）| 无 | 安全删除 |

### 跨文件引用验证

| 调用方 | 被引用符号 | 定义文件 | 引用方式 | 影响 |
|--------|-----------|---------|---------|------|
| YingShiApp.kt | `AlbumPageStateStore` | AlbumPageScreen.kt | internal object | 无变化 |
| YingShiApp.kt | `AlbumPageScreen` | AlbumPageScreen.kt | public fun | 无变化 |
| YingShiApp.kt | `PostDetailPlaceholderScreen` | AlbumPostCard.kt（迁移后）| public fun | **import 路径不变**（同包）|

### 必须测试的影响路径

- **IA-2**: 编译通过（所有跨文件引用正确）
- **IA-3**: 大相册目录页 UI 行为完全不变（状态管理、弹窗、卡片、切换）
- **IA-4**: PostDetailPlaceholderScreen 导航入口正常（从 YingShiApp.kt 导航）
- **IA-5**: AlbumDirectoryDialog 弹窗打开/关闭/搜索/管理/重命名/删除流程

## 三、FR-5 影响分析

### 变更: RealTrashMemoryCache 改用 LRU

| 变更文件 | 直接影响 | 间接影响 | 影响说明 |
|---------|---------|---------|---------|
| RealTrashViewModels.kt `RealTrashMemoryCache` | `RealTrashListViewModel`、`RealTrashDetailViewModel`（读写缓存）| 照片根目录/回收站页面的缓存读取行为 | LRU 淘汰后，旧条目被清除，需重新从网络加载 |

### 必须测试的影响路径

- **IA-6**: 回收站列表切换类型时，缓存命中（20条以内）
- **IA-7**: 回收站详情页打开后返回再打开，缓存命中（50条以内）
- **IA-8**: 长时间使用后（切换超过20种类型/打开超过50个详情），旧缓存被淘汰，新访问重新加载

## 四、FR-6 影响分析

### 变更: 新建 album-api.md

| 变更文件 | 直接影响 | 间接影响 | 影响说明 |
|---------|---------|---------|---------|
| docs/contracts/album-api.md（新建）| 无（纯文档）| 无 | 文档与实际 API 行为一致性需验证 |

### 必须测试的影响路径

- **IA-9**: 文档中的端点路径、请求体、响应体与 AlbumController.java 实际代码一致

## 五、风险矩阵

| # | 风险 | 严重度 | 关联 FR | 缓解措施 |
|---|------|--------|---------|---------|
| 1 | FR-4 拆分遗漏 import | 中 | FR-4 | 每步编译验证 |
| 2 | FR-4 可见性提升遗漏 | 中 | FR-4 | 编译错误会立即暴露 |
| 3 | FR-3 token 色值差异 | 低 | FR-3 | token 已定义，视觉相近 |
| 4 | FR-5 LRU 淘汰导致频繁重新加载 | 低 | FR-5 | max 20/50 足够日常使用 |
| 5 | FR-6 文档与代码不一致 | 低 | FR-6 | 从代码生成文档 |
