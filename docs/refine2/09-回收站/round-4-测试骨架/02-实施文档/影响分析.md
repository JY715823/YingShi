# Round 4 影响分析

> 日期：2026-07-15
> 本轮变更文件：11 个（2 build + 8 test + 1 main）
> 端：Android

---

## 一、变更文件影响路径

### 1.1 app/build.gradle.kts + libs.versions.toml

| 变更 | 直接影响 | 间接影响 | 影响说明 |
|------|---------|---------|---------|
| 新增 kotlinx-coroutines-test + turbine testImplementation | testDebugUnitTest task | 无 | 仅 test classpath 新增依赖，不影响 main 编译；现有 22 tests 不受影响（不使用 runTest/turbine） |

**IA-1**: 所有 test 文件（现有 22 tests + 新增测试）的 classpath 变化，需验证现有测试仍 PASS。

### 1.2 RealTrashTestFixtures.kt + FakeTrashRepository.kt + MainDispatcherRule.kt（新增 test 文件）

| 变更 | 直接影响 | 间接影响 | 影响说明 |
|------|---------|---------|---------|
| 新建 3 个 test 工具类 | 仅被同包 test 文件引用 | 无 | internal 可见性，不暴露到 main；不影响 main 编译 |

**IA-2**: 3 个工具类编译通过（FakeTrashRepository 正确实现 TrashRepository 接口的 7 个 suspend 方法）。

### 1.3 RealTrashShared.kt（修改 main 文件）

| 变更 | 直接影响 | 间接影响 | 影响说明 |
|------|---------|---------|---------|
| 提取 countdownSeverity 纯函数 + RealTrashCountdownBadge 调用 | RealTrashCountdownBadge 内部逻辑 | 无（行为等价） | 行为等价重构，三档阈值不变 |
| 修复 realTrashGridPostTitle 空串回退 | RealTrashListScreen.kt 调用点 + RealTrashDetailScreen.kt 调用点 | 无 | 修复潜在 bug：sourcePostTitle="" 现在回退到 title，之前返回空串 |

**IA-3**: RealTrashCountdownBadge 调用 countdownSeverity 后三档颜色映射不变（编译 + 视觉回归核查）。
**IA-4**: realTrashGridPostTitle 空串场景行为变化（之前返回空串，现在回退到 title/sourcePostId/默认值）。需验证现有调用点不受影响（所有调用点都期望非空字符串）。

### 1.4 5 个新建测试文件

| 变更 | 直接影响 | 间接影响 | 影响说明 |
|------|---------|---------|---------|
| RealTrashSharedLogicTest.kt | 测试 RealTrashShared.kt 内部函数 | 无 | 需函数 internal 可见 |
| TrashModelsLogicTest.kt | 测试 TrashModels.kt 内部函数 | 无 | 需函数 internal 可见 |
| CollaboratorSelectionTest.kt | 测试 CollaboratorIdentity.kt 内部函数 | 无 | 需函数 internal 可见 |
| RealTrashListViewModelTest.kt | 实例化 RealTrashListViewModel | RealBackendMutationBus 单例状态 | 实例化时触发 mutation 会修改单例，但不影响断言 |
| RealTrashDetailViewModelTest.kt | 实例化 RealTrashDetailViewModel | init 自动 refresh 触发 fakeRepo.getTrashDetail | 需在 @Before 中预配 fakeRepo 返回值 |

**IA-5**: 5 个测试文件全部编译通过 + 运行 PASS。

---

## 二、必须测试的影响路径清单

| # | 路径 | 验证方法 |
|---|------|---------|
| IA-1 | 现有 22 tests 在新增依赖后仍 PASS | 运行 RealTrashArchitectureTest + RealTrashNavigationTest |
| IA-2 | 3 个工具类编译通过 | `gradlew.bat compileDebugUnitTestKotlin` |
| IA-3 | RealTrashCountdownBadge 三档颜色映射不变 | 代码核查 + countdownSeverity 纯函数测试 |
| IA-4 | realTrashGridPostTitle 空串回退修复不破坏现有调用 | 编译 + 现有 22 tests PASS |
| IA-5 | 5 个新测试文件全部 PASS | `gradlew.bat testDebugUnitTest` |

---

## 三、后端影响链

本轮无服务端变更，无后端影响。

---

## 四、前端影响链

```
RealTrashShared.kt (修改)
├── countdownSeverity 提取
│   └── RealTrashCountdownBadge (调用) — 行为等价
└── realTrashGridPostTitle 修复
    ├── RealTrashListScreen.kt (调用) — 期望非空字符串，修复后更安全
    └── RealTrashDetailScreen.kt (调用) — 同上

新增 test 文件 (8 个)
├── RealTrashTestFixtures.kt — 仅 test 引用
├── FakeTrashRepository.kt — 实现 TrashRepository 接口
├── MainDispatcherRule.kt — 协程测试规则
├── RealTrashSharedLogicTest.kt — 测试 RealTrashShared.kt internal 函数
├── TrashModelsLogicTest.kt — 测试 TrashModels.kt internal 函数
├── CollaboratorSelectionTest.kt — 测试 CollaboratorIdentity.kt internal 函数
├── RealTrashListViewModelTest.kt — 实例化 RealTrashListViewModel
└── RealTrashDetailViewModelTest.kt — 实例化 RealTrashDetailViewModel
```

---

## 五、跨模块回归

| 模块 | 影响 | 验证 |
|------|------|------|
| 06 小相册 | 无 | realTrashGridPostTitle 修复不影响小相册代码 |
| 07 照片查看器 | 无 | countdownSeverity 提取不影响 Viewer |
| 08 系统媒体 | 无 | 测试不涉及系统媒体代码 |
| YingShiThemeTokens | 无 | countdownSeverity 不引用 token |
| SyncVersionTracker | 无 | ViewModel 测试不涉及 SyncVersionTracker |
| RealBackendMutationBus | 间接 | ViewModel mutation 会触发 notifyRealBackendContentChanged，但测试不验证单例状态 |
