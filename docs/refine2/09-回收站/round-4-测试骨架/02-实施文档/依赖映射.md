# Round 4 依赖映射

> 日期：2026-07-15
> 本轮变更文件：11 个（2 build + 8 test + 1 main）
> 依赖方向：本模块依赖的外部模块 + 依赖本模块的外部调用方

---

## 一、本模块依赖的外部模块/服务

| 依赖模块 | 依赖内容 | 本轮是否改动接口 | 影响说明 |
|---------|---------|----------------|---------|
| 06 小相册 | AlbumPostCardUiModel（TrashPostSnapshot 引用） | 否 | 仅测试夹具构造，不修改 |
| TrashRepository 接口 | 7 个 suspend 方法 | 否 | FakeTrashRepository 实现接口，不修改接口 |
| RemoteTrashItem / RemoteTrashDetail / RemotePendingCleanup | 数据模型 | 否 | 测试夹具构造，不修改 |
| ApiResult | sealed class（Success/Error） | 否 | FakeTrashRepository 返回 ApiResult，不修改 |
| kotlinx-coroutines-test | runTest + TestDispatcher | 新增依赖 | 仅 test classpath |
| turbine | Flow 测试工具 | 新增依赖 | 仅 test classpath |

---

## 二、依赖本模块的外部调用方

| 调用方 | 调用内容 | 本轮是否影响 | 影响说明 |
|--------|---------|-------------|---------|
| RealTrashCountdownBadge | countdownSeverity 纯函数（新提取） | 是 | 内部调用改为 countdownSeverity()，行为等价 |
| RealTrashListScreen.kt | realTrashGridPostTitle（修复） | 是 | 空串场景行为变化，但所有调用点期望非空字符串，修复更安全 |
| RealTrashDetailScreen.kt | realTrashGridPostTitle（修复） | 是 | 同上 |

---

## 三、前后端共享组件依赖

| 共享组件 | 本轮是否受影响 | 说明 |
|---------|--------------|------|
| YingShiThemeTokens | 否 | countdownSeverity 不引用 token |
| YingShiMotion | 否 | 测试不涉及动画 |
| AppShell | 否 | 测试不涉及外壳 |
| RealBackendMutationBus | 间接 | ViewModel mutation 触发 notifyRealBackendContentChanged，但测试不验证单例状态 |

---

## 四、数据流图

```
[测试用例]
    ↓ 构造
[RealTrashTestFixtures.kt] — sampleRemoteTrashItem / sampleTrashEntryUiModel 等
    ↓ 注入
[FakeTrashRepository.kt] — 实现 TrashRepository 接口
    ↓ 构造
[RealTrashListViewModel / RealTrashDetailViewModel]
    ↓ 触发方法
[restoreEntries / moveEntriesToPendingCleanup / purgePendingCleanupEntries / undoPendingCleanup / restore / remove / undoRemove]
    ↓ 调用 fakeRepo
[FakeTrashRepository 记录调用]
    ↓ 返回
[ApiResult.Success / Error]
    ↓ 更新
[ViewModel.uiState]
    ↓ 断言
[测试用例 PASS/FAIL]
```

```
[纯函数测试]
    ↓ 调用
[formatCountdownRemaining / countdownSeverity / realTrashEntrySourceLine / ...]
    ↓ 断言
[测试用例 PASS/FAIL]
```

---

## 五、回归测试范围

| 范围 | 测试 | 验证方法 |
|------|------|---------|
| 现有 22 tests | RealTrashArchitectureTest + RealTrashNavigationTest | 运行 testDebugUnitTest 确认 22/22 PASS |
| 新增纯函数测试 | RealTrashSharedLogicTest + TrashModelsLogicTest + CollaboratorSelectionTest | 运行 testDebugUnitTest 确认 PASS |
| 新增 ViewModel 测试 | RealTrashListViewModelTest + RealTrashDetailViewModelTest | 运行 testDebugUnitTest 确认 PASS |
| RealTrashShared.kt 重构回归 | RealTrashCountdownBadge 行为 + realTrashGridPostTitle 调用点 | 编译 + 现有 22 tests PASS |
| 跨模块回归 | 06/07/08 模块无影响 | 无需额外测试 |
