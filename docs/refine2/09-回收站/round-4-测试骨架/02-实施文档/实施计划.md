# Round 4 实施计划

> 日期：2026-07-15
> 本轮 FR：FR-5 测试骨架搭建
> 端：Android（服务端本轮无变更）
> 基线：Round 1 + Round 2 + Round 3

---

## 一、变更文件清单（完整）

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `app/build.gradle.kts` | Android | 修改 | 新增 testImplementation: kotlinx-coroutines-test 1.10.1 + turbine 1.1.0 |
| 2 | `gradle/libs.versions.toml` | Android | 修改 | 新增 [versions] + [libraries] 条目（kotlinx-coroutines-test / turbine） |
| 3 | `app/src/test/java/com/example/yingshi/feature/photos/RealTrashTestFixtures.kt` | Android (test) | 新增 | 测试夹具工厂函数：sampleRemoteTrashItem / sampleRemoteTrashDetail / sampleRemotePendingCleanup / sampleTrashEntryUiModel / sampleTrashPendingCleanupUiModel / sampleTrashPostSnapshot / sampleTrashMediaSnapshot |
| 4 | `app/src/test/java/com/example/yingshi/feature/photos/FakeTrashRepository.kt` | Android (test) | 新增 | TrashRepository 接口的 fake 实现：可配置返回值 + 调用记录 |
| 5 | `app/src/test/java/com/example/yingshi/feature/photos/MainDispatcherRule.kt` | Android (test) | 新增 | 协程测试规则：StandardTestDispatcher + Dispatchers.setMain/resetMain |
| 6 | `app/src/test/java/com/example/yingshi/feature/photos/RealTrashSharedLogicTest.kt` | Android (test) | 新增 | 纯函数单测：formatCountdownRemaining + realTrashEntrySourceLine + pendingCleanupDeleteCopy + toTrashStateLabel + realTrashViewerMediaContentDescription + realTrashGridPostTitle + previewMediaIds + commentMediaId + isRealMediaTrashType + viewerVideoDurationMillis |
| 7 | `app/src/test/java/com/example/yingshi/feature/photos/TrashModelsLogicTest.kt` | Android (test) | 新增 | TrashModels helper 测试：parseTrashEntryTypeOrNull + parseTrashEntryTypeOrDefault + restoreTargetMediaIds + businessIdentityKey |
| 8 | `app/src/test/java/com/example/yingshi/feature/photos/CollaboratorSelectionTest.kt` | Android (test) | 新增 | 协作者 helper 测试：defaultCollaboratorSelection + normalizeCollaboratorSelectionKeepingEmpty + isAllCollaboratorsSelected + toggleCollaboratorSelectionKeepingEmpty + initialOrNormalizedCollaboratorSelection |
| 9 | `app/src/test/java/com/example/yingshi/feature/photos/RealTrashListViewModelTest.kt` | Android (test) | 新增 | List ViewModel 行为测试：restoreEntries + moveEntriesToPendingCleanup + purgePendingCleanupEntries + undoPendingCleanup + showSelectionMessage |
| 10 | `app/src/test/java/com/example/yingshi/feature/photos/RealTrashDetailViewModelTest.kt` | Android (test) | 新增 | Detail ViewModel 行为测试：restore + restoreItem + remove + removeItem + undoRemove + purgePendingCleanup |
| 11 | `app/src/main/java/com/example/yingshi/feature/photos/RealTrashShared.kt` | Android | 修改 | 提取 countdownSeverity 纯函数 + 修复 realTrashGridPostTitle 空串回退（`?:` → `takeIf { isNotBlank() }`） |

**合计**: 11 个文件（2 个 build 配置 + 8 个 test 新建 + 1 个 main 修改）

---

## 二、FR-5 测试骨架搭建

### 目标

为回收站 ViewModel 和关键业务逻辑添加基础单元测试，覆盖状态流转（分类切换/恢复/移出/purge）和业务逻辑（倒计时/过期判断），建立项目首个 ViewModel 行为测试基础设施。

### AC 对照

| AC | 描述 | 覆盖 |
|----|------|------|
| AC-1 | ViewModel 状态流转测试（分类切换/恢复/移出/purge） | RealTrashListViewModelTest + RealTrashDetailViewModelTest |
| AC-2 | 关键业务逻辑测试（待清理倒计时/过期判断） | RealTrashSharedLogicTest (formatCountdownRemaining) + countdownSeverity |
| AC-3 | 测试全部 PASS | 阶段5 验证 |

---

## 三、各文件实施计划

### 3.1 build.gradle.kts + libs.versions.toml

**libs.versions.toml** `[versions]` 块新增：
```toml
kotlinxCoroutinesTest = "1.10.1"
turbine = "1.1.0"
```

**libs.versions.toml** `[libraries]` 块新增：
```toml
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "kotlinxCoroutinesTest" }
turbine = { group = "app.cash.turbine", name = "turbine", version.ref = "turbine" }
```

**app/build.gradle.kts** dependencies 块新增：
```kotlin
testImplementation(libs.kotlinx.coroutines.test)
testImplementation(libs.turbine)
```

### 3.2 RealTrashTestFixtures.kt（测试夹具）

提供工厂函数（所有参数都有默认值，调用方按需覆盖）：

```kotlin
internal fun sampleRemoteTrashItem(
    id: String = "trash-item-1",
    type: String = "MEDIA_SYSTEM_DELETED",
    title: String = "测试媒体",
    ...
): RemoteTrashItem

internal fun sampleRemoteTrashDetail(
    item: RemoteTrashItem = sampleRemoteTrashItem(),
    ...
): RemoteTrashDetail

internal fun sampleRemotePendingCleanup(
    id: String = "pending-1",
    undoDeadlineMillis: Long = System.currentTimeMillis() + 24L * 60L * 60L * 1000L,
    ...
): RemotePendingCleanup

internal fun sampleTrashEntryUiModel(
    id: String = "entry-1",
    type: TrashEntryType = TrashEntryType.MEDIA_SYSTEM_DELETED,
    deletedAtMillis: Long = System.currentTimeMillis() - 3_600_000L,
    title: String? = "测试条目",
    sourcePostId: String? = null,
    sourceMediaId: String? = null,
    relatedMediaIds: List<String> = emptyList(),
    ...
): TrashEntryUiModel

internal fun sampleTrashPendingCleanupUiModel(
    entry: TrashEntryUiModel = sampleTrashEntryUiModel(),
    removedAtMillis: Long = System.currentTimeMillis(),
    ...
): TrashPendingCleanupUiModel

internal fun sampleTrashMediaSnapshot(
    mediaId: String = "media-1",
    ...
): TrashMediaSnapshot

internal fun sampleTrashPostSnapshot(
    postId: String = "post-1",
    ...
): TrashPostSnapshot
```

### 3.3 FakeTrashRepository.kt

```kotlin
internal class FakeTrashRepository : TrashRepository {
    // 可配置返回值（默认 Success 空列表）
    var trashItemsResult: ApiResult<List<RemoteTrashItem>> = ApiResult.Success(emptyList())
    var trashDetailResult: ApiResult<RemoteTrashDetail> = ApiResult.Success(sampleRemoteTrashDetail())
    var restoreResult: ApiResult<RemoteTrashItem> = ApiResult.Success(sampleRemoteTrashItem())
    var moveOutResult: ApiResult<RemotePendingCleanup> = ApiResult.Success(sampleRemotePendingCleanup())
    var purgeResult: ApiResult<RemoteTrashItem> = ApiResult.Success(sampleRemoteTrashItem())
    var undoResult: ApiResult<RemoteTrashItem> = ApiResult.Success(sampleRemoteTrashItem())
    var pendingItemsResult: ApiResult<List<RemotePendingCleanup>> = ApiResult.Success(emptyList())

    // 调用记录
    val getTrashItemsCalls = mutableListOf<String?>()
    var getTrashDetailCalls = mutableListOf<String>()
    val restoreCalls = mutableListOf<String>()
    val moveOutCalls = mutableListOf<String>()
    val purgeCalls = mutableListOf<String>()
    val undoCalls = mutableListOf<String>()
    var getPendingCleanupCalls = 0

    // 按需返回不同结果（用于部分失败场景）
    var restoreResultsById: Map<String, ApiResult<RemoteTrashItem>> = emptyMap()

    override suspend fun getTrashItems(type: String?): ApiResult<List<RemoteTrashItem>> {
        getTrashItemsCalls.add(type)
        return trashItemsResult
    }
    // ... 其他方法类似
}
```

### 3.4 MainDispatcherRule.kt

```kotlin
internal class MainDispatcherRule(
    val dispatcher: TestDispatcher = StandardTestDispatcher(),
) : TestWatcher() {
    override fun starting(description: Description) {
        Dispatchers.setMain(dispatcher)
    }
    override fun finished(description: Description) {
        Dispatchers.resetMain()
    }
}
```

### 3.5 RealTrashShared.kt 修改

**3.5.1 提取 countdownSeverity 纯函数**：

```kotlin
internal enum class CountdownSeverity { URGENT, WARNING, SAFE }

internal fun countdownSeverity(remainingMillis: Long): CountdownSeverity {
    val oneHourMillis = 1L * 60L * 60L * 1000L
    val sixHourMillis = 6L * 60L * 60L * 1000L
    return when {
        remainingMillis <= oneHourMillis -> CountdownSeverity.URGENT
        remainingMillis <= sixHourMillis -> CountdownSeverity.WARNING
        else -> CountdownSeverity.SAFE
    }
}
```

RealTrashCountdownBadge 内联逻辑改为调用 `countdownSeverity(remainingMillis)`，颜色映射不变。

**3.5.2 修复 realTrashGridPostTitle 空串回退**：

```kotlin
// 修复前
internal fun realTrashGridPostTitle(entry: TrashEntryUiModel): String {
    return entry.mediaSnapshot?.sourcePostTitle
        ?: entry.title
        ?: entry.sourcePostId
        ?: "来源小相册"
}

// 修复后
internal fun realTrashGridPostTitle(entry: TrashEntryUiModel): String {
    return entry.mediaSnapshot?.sourcePostTitle?.takeIf { it.isNotBlank() }
        ?: entry.title?.takeIf { it.isNotBlank() }
        ?: entry.sourcePostId?.takeIf { it.isNotBlank() }
        ?: "来源小相册"
}
```

### 3.6 测试用例覆盖矩阵

#### 3.6.1 RealTrashSharedLogicTest.kt（纯函数单测，约 50 用例）

| 函数 | 用例数 | 覆盖 |
|------|-------|------|
| formatCountdownRemaining | 11 | 已过期(0/-1/-3600000) + 即将过期(1/59999) + 剩余 Xm(60000/3599999) + 剩余 Xh Ym(3600000/3600001/3660000/7384000) |
| countdownSeverity | 8 | URGENT(-1/0/1/3600000) + WARNING(3600001/21600000) + SAFE(21600001/86400000) |
| realTrashEntrySourceLine | 8 | LARGE_ALBUM_DELETED(albumSnapshot/null) + SMALL_ALBUM_DELETED(空/非空) + MEDIA_REMOVED(sourcePostId/null/全空) + MEDIA_SYSTEM_DELETED |
| pendingCleanupDeleteCopy | 4 | 4 种 type 分支 |
| toTrashStateLabel | 12 | null/""/空格/intrash/IN_TRASH/pendingcleanup/PENDING_CLEANUP/restored/Restored/purged/deleted/unknown |
| realTrashViewerMediaContentDescription | 2 | VIDEO/IMAGE |
| realTrashGridPostTitle | 6 | sourcePostTitle 非空/空串/null→title 非空/空串/null→sourcePostId 非空/null→默认 |
| previewMediaIds | 6 | 合并/去重/去空/空白过滤 |
| commentMediaId | 5 | commentTarget/source/mediaSnapshot/related/全空 |
| isRealMediaTrashType | 4 | 4 种 type |
| viewerVideoDurationMillis | 2 | null→18000/非 null |

#### 3.6.2 TrashModelsLogicTest.kt（约 15 用例）

| 函数 | 用例数 | 覆盖 |
|------|-------|------|
| parseTrashEntryTypeOrNull | 8 | largeAlbumDeleted/LARGEALBUMDELETED/带空格/POST_DELETED/enum.name/null/""/unknown |
| parseTrashEntryTypeOrDefault | 3 | 合法值/null→default/unknown→default |
| restoreTargetMediaIds | 2 | 多源合并/去重 |
| businessIdentityKey | 5 | 完全相等/type 不同/relatedPostIds 顺序不同（sorted）/actorUserId 不参与/palette 不参与 |

#### 3.6.3 CollaboratorSelectionTest.kt（约 22 用例）

| 函数 | 用例数 | 覆盖 |
|------|-------|------|
| defaultCollaboratorSelection | 4 | 保序/过滤空白/空集/全空白 |
| normalizeCollaboratorSelectionKeepingEmpty | 5 | 交集/全不在 all 返回空/all 空返回空/selected 空/all 空 |
| isAllCollaboratorsSelected | 6 | 全选 true/部分 false/超集 false/all 空 false/selected 空 false/去重后全选 true |
| toggleCollaboratorSelectionKeepingEmpty | 4 | 选中新增/取消移除/不在 all 仅过滤/all 空 |
| initialOrNormalizedCollaboratorSelection | 3 | 未初始化→default/已初始化→normalize/all 空 |

#### 3.6.4 RealTrashListViewModelTest.kt（约 11 用例）

| 方法 | 用例 | 验证 |
|------|-----|------|
| restoreEntries | restore_success_updatesStateAndTriggersCallback | isMutating 状态变化 + statusMessage + onFirstRestoredMediaIds 调用 |
| restoreEntries | restore_offlineReadOnly_blocksMutation | errorMessage 设置 + repo 未调用 |
| restoreEntries | restore_emptyList_returnsEarly | repo 未调用 + statusMessage |
| restoreEntries | restore_partialFailure_reportsStatus | 部分失败 + statusMessage |
| moveEntriesToPendingCleanup | move_success_updatesState | isMutating + repo 调用次数 |
| moveEntriesToPendingCleanup | move_offlineReadOnly_blocks | errorMessage + repo 未调用 |
| purgePendingCleanupEntries | purge_success_updatesState | isMutating + repo 调用 |
| purgePendingCleanupEntries | purge_offlineReadOnly_blocks | errorMessage + repo 未调用 |
| undoPendingCleanup | undo_success_updatesState | isMutating + repo 调用 |
| undoPendingCleanup | undo_offlineReadOnly_blocks | errorMessage + repo 未调用 |
| showSelectionMessage | showSelectionMessage_updatesStatus | statusMessage 设置 |

#### 3.6.5 RealTrashDetailViewModelTest.kt（约 6 用例）

| 方法 | 用例 | 验证 |
|------|-----|------|
| restore | restore_success_triggersCallback | onSuccess 回调 + statusMessage |
| restoreItem | restoreItem_success_triggersCallback | onSuccess 回调 |
| remove | remove_success_triggersCallback | onSuccess 回调 |
| removeItem | removeItem_success_triggersCallback | onSuccess 回调 |
| undoRemove | undoRemove_success_autoRefreshes | repo.undoMoveTrashItemOut 调用 + refresh |
| purgePendingCleanup | purge_success_triggersCallback | onSuccess 回调 + forgetImportStatusByAppMediaIds |

---

## 四、Flyway 迁移

本轮无 DB schema 变更，无需 Flyway 迁移。

---

## 五、实施顺序

1. 修改 build.gradle.kts + libs.versions.toml（引入依赖）
2. 新建 RealTrashTestFixtures.kt（测试夹具）
3. 新建 FakeTrashRepository.kt（接口 fake 实现）
4. 新建 MainDispatcherRule.kt（协程规则）
5. 修改 RealTrashShared.kt（提取 countdownSeverity + 修复 realTrashGridPostTitle）
6. 编译验证：`gradlew.bat compileDebugKotlin` + `gradlew.bat compileDebugUnitTestKotlin`
7. 新建 RealTrashSharedLogicTest.kt（纯函数单测）
8. 新建 TrashModelsLogicTest.kt
9. 新建 CollaboratorSelectionTest.kt
10. 新建 RealTrashListViewModelTest.kt（ViewModel 行为测试）
11. 新建 RealTrashDetailViewModelTest.kt
12. 运行全部测试：`gradlew.bat testDebugUnitTest`

每步完成后编译验证。
