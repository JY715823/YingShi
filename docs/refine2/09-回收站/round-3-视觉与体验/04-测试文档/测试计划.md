# Round 3 测试计划

> 模块: 09-回收站
> 轮次: Round 3 — 视觉提升 + 体验优化
> 编制时间: 2026-07-15
> 测试范围: 5 个 Android 文件变更，4 个 FR (FR-3/FR-4/FR-7/FR-8)，8 条 IA 影响路径

## 一、测试矩阵概览

| 类别 | 用例数 | 端 | 执行方式 |
|------|--------|-----|---------|
| 编译/基础设施 | 2 | Android | 自动 (gradle) |
| 功能测试 (FR/AC) | 11 | Android | 代码核查 + 手动 UI |
| 影响范围测试 (IA) | 8 | Android | 代码核查 + 手动 UI |
| 跨模块回归测试 | 4 | Android | 自动 (unit test) + 代码核查 |
| **合计** | **25** | Android | |

---

## 二、编译/基础设施测试

### TC-1: Android Kotlin 编译通过
- 对应: 全部 FR (前置条件)
- 端: Android
- 前置条件: 已应用 Round 3 全部 5 个文件变更
- 步骤/命令: `.\gradlew.bat compileDebugKotlin --no-daemon 2>&1 | Select-String "BUILD|^e:"`
- 预期结果: 输出 `BUILD SUCCESSFUL`，无 `^e:` 开头的 Kotlin 编译错误行
- 通过标准: 编译退出码 0 + 无 `e: file://...` 错误

### TC-2: Android assembleDebug 完整构建通过
- 对应: 全部 FR (前置条件)
- 端: Android
- 前置条件: TC-1 通过
- 步骤/命令: `.\gradlew.bat assembleDebug --no-daemon 2>&1 | Select-String "BUILD|FAILED"`
- 预期结果: 输出 `BUILD SUCCESSFUL`，APK 产出至 `app/build/outputs/apk/debug/app-debug.apk`
- 通过标准: 构建退出码 0 + APK 文件存在

---

## 三、功能测试 (按 FR 分组)

### FR-3: 回收站视觉升级

#### TC-3: FR-3/AC-1 列表卡片 viewer 深色 token 边线+微光
- 对应: FR-3/AC-1
- 端: Android
- 前置条件: TC-2 通过；设备/模拟器运行 debug APK
- 步骤:
  1. 进入"照片"Tab → 回收站入口
  2. 切换到"系统媒体删除"分类
  3. 检查媒体网格卡片视觉
  4. 切换到"小相册删除"分类，检查列表卡片视觉
- 预期结果:
  - 卡片带 1dp `viewerOverlayBorder` 边线
  - 卡片有 `glowWash` 渐变微光 (顶部亮、底部透明)
  - 卡片 `shadowElevation = 2.dp`
  - 圆角为 `YingShiThemeTokens.radius.xl`
- 通过标准: 视觉特征全部存在

#### TC-4: FR-3/AC-2 待清理倒计时渐变+阴影
- 对应: FR-3/AC-2
- 端: Android
- 前置条件: 回收站有待清理条目 (或通过开发者入口进入待清理页)
- 步骤:
  1. 进入待清理页
  2. 检查每个待清理卡片
  3. 重点检查倒计时 badge
- 预期结果:
  - 卡片底色为 memoryWash → raisedSurface 的渐变
  - 卡片边线为 `memoryAccent α0.22`
  - 卡片 `shadowElevation = 2.dp`
  - 倒计时 badge 渐变背景，按剩余时长分 3 档颜色:
    - ≤1h: destructive (红)
    - ≤6h: memoryAccent (暖橙)
    - >6h: textSecondary (灰)
- 通过标准: 4 项视觉特征全部存在

#### TC-5: FR-3/AC-3 操作按钮质感
- 对应: FR-3/AC-3
- 端: Android
- 前置条件: 列表/详情/待清理页均可达
- 步骤:
  1. 在回收站列表顶栏检查"刷新"按钮 (normal 态)
  2. 在小相册详情顶部栏检查"恢复"按钮 (emphasized 态) + "移出"按钮 (danger 态)
  3. 在待清理页检查"撤销移出"(emphasized) + "永久删除"(danger)
  4. 列表中某条目点击菜单，检查 DropdownMenu 中按钮
- 预期结果:
  - normal 态: raisedSurface + glassSurfaceBase 渐变 + 2dp shadow
  - emphasized 态: primaryContainer + primaryActionPressed 渐变 + 4dp shadow
  - danger 态: destructiveContainer + destructive 渐变 + 4dp shadow
  - disabled 态: sectionBackground 半透明 + 2dp shadow
  - 所有按钮带 1dp dividerSoft / destructive 边线
- 通过标准: 4 种状态视觉均正确

#### TC-6: FR-3/AC-4 DropdownMenu 阴影层次
- 对应: FR-3/AC-4
- 端: Android
- 前置条件: 列表中有带菜单的条目
- 步骤:
  1. 点击列表条目右侧的"更多"按钮展开 DropdownMenu
  2. 观察 DropdownMenu 视觉
- 预期结果:
  - DropdownMenu `containerColor = raisedSurface α0.96`
  - `shadowElevation = 8.dp` (强阴影层次)
  - `tonalElevation = 0.dp` (无 tonal 叠加，保持 raisedSurface 纯净色)
- 通过标准: 强阴影 + 无 tonal 叠加

### FR-4: 分类切换体验优化 (AC-3 切换动画)

#### TC-7: FR-4/AC-3 分类切换动画 (AnimatedContent)
- 对应: FR-4/AC-3
- 端: Android
- 前置条件: 回收站列表页可见
- 步骤:
  1. 当前为"全部"分类
  2. 通过 DropdownMenu 切换到"系统媒体删除"
  3. 观察切换过程是否有 fadeIn/fadeOut 过渡
  4. 继续切换到"小相册删除"/"大相册删除"，观察动画
- 预期结果:
  - 切换时有 fadeIn + fadeOut 过渡 (时长 `motion.sectionMillis` = 240ms，缓动 `motion.easing` = FastOutSlowInEasing)
  - 过渡中无白屏、无空状态闪烁
  - 切换后内容正确为目标分类
- 通过标准: 4 次切换均有动画 + 内容正确

### FR-7: 分类名展示

#### TC-8: FR-7/AC-1 顶栏下方显示当前分类名
- 对应: FR-7/AC-1
- 端: Android
- 前置条件: 回收站列表页可见
- 步骤:
  1. 当前为"全部"分类
  2. 检查列表顶部第一个 item 是否为分类标题栏
  3. 切换到"系统媒体删除"
  4. 检查标题栏
- 预期结果:
  - 每个列表分支首项为 `RealTrashCategoryHeader`
  - 标题文字 = 当前分类 `label` (如"系统媒体删除")
  - 副标题 (当 entries 非空) 显示"共 N 项"
  - 副标题 (当 entries 为空) 为 null
- 通过标准: 4 个分类的标题栏均正确

#### TC-9: FR-7/AC-2 切换时同步更新
- 对应: FR-7/AC-2
- 端: Android
- 前置条件: TC-8 通过
- 步骤:
  1. 快速连续切换"全部"→"系统媒体删除"→"小相册删除"
  2. 观察标题栏是否跟随切换
- 预期结果:
  - 标题栏跟随 `targetType` 立即更新 (因 AnimatedContent 内 targetType 联动)
  - 无滞后、无错位
- 通过标准: 标题栏与列表内容始终一致

### FR-8: 详情排版优化

#### TC-10: FR-8/AC-1 大相册子小相册列表
- 对应: FR-8/AC-1
- 端: Android
- 前置条件: 回收站有大相册删除条目
- 步骤:
  1. 进入"大相册删除"分类
  2. 点击某条目进入详情
  3. 检查详情页"原大相册内容"区
- 预期结果:
  - 显示 `RealTrashLargeAlbumChildList` 组件 (替换原文字卡)
  - 每项含: 缩略图 + 小相册标题 + 媒体数
  - 数据来自 `entry.albumSnapshot?.postSnapshots`
  - 空列表回退为 RealTrashSectionCard 文字提示
- 通过标准: 子小相册列表正确渲染

#### TC-11: FR-8/AC-2 统一详情顶部栏
- 对应: FR-8/AC-2
- 端: Android
- 前置条件: 各类详情可达
- 步骤:
  1. 进入小相册删除详情，检查顶部栏
  2. 进入大相册删除详情，检查顶部栏
  3. 进入系统媒体删除详情 (Viewer)，检查顶部栏
- 预期结果:
  - 三类详情均使用 `RealTrashDetailTopBar` (统一组件)
  - 标题、返回按钮、可选恢复/移出按钮位置一致
  - 小相册详情顶部栏在 Column 中，下方可显示离线 banner
- 通过标准: 三类详情顶部栏视觉一致

#### TC-12: FR-8/AC-3 离线只读态视觉区分
- 对应: FR-8/AC-3
- 端: Android
- 前置条件: 模拟离线 (断网或后端不可达)
- 步骤:
  1. 离线状态下进入小相册详情
  2. 检查顶部 banner
  3. 检查恢复/移出按钮 enabled 状态
  4. 检查大相册详情同样行为
- 预期结果:
  - 顶部显示 `RealTrashOfflineBanner` (memoryWash 渐变 + memoryAccent 边线 + 离线图标)
  - banner 文字: "当前离线，仅可查看回收站内容。"
  - 按钮 `enabled = !isMutating && !isOfflineReadOnly` → 离线时禁用
  - 在线后 banner 消失 + 按钮恢复
- 通过标准: 离线 banner + 按钮禁用 + 在线恢复

#### TC-13: FR-8/AC-4 小相册 Pager 高度自适应
- 对应: FR-8/AC-4
- 端: Android
- 前置条件: 小相册详情含不同宽高比媒体
- 步骤:
  1. 进入竖图 (aspectRatio < 1) 小相册详情
  2. 检查 Pager 高度
  3. 切换到宽图 (aspectRatio > 1) 媒体
  4. 切换到方图 (aspectRatio = 1) 媒体
- 预期结果:
  - Pager 高度 = `(maxWidth / mediaAspectRatio).coerceIn(240.dp, 520.dp)`
  - 竖图: 高度较高 (但 ≤520dp)
  - 宽图: 高度较低 (但 ≥240dp)
  - 方图: 高度 = maxWidth
  - 无压缩、无大量留白
- 通过标准: 3 种宽高比媒体展示均合理

---

## 四、影响范围测试 (对应影响分析 IA-N)

### TC-14: IA-1 RealTrashIconActionButton 四处按钮视觉+功能
- 对应: IA-1
- 端: Android
- 前置条件: TC-5 通过
- 步骤:
  1. 列表顶栏按钮点击 → 触发刷新/排序等动作
  2. 详情顶栏按钮点击 → 触发恢复/移出
  3. 待清理按钮点击 → 触发撤销/永久删除
  4. 对话框按钮点击 → 触发确认/取消
- 预期结果:
  - 4 处按钮视觉与 TC-5 一致
  - 点击响应正常，回调正确连接
  - danger 态按钮点击触发 destructive 流程
- 通过标准: 4 处按钮视觉 + 功能均正确

### TC-15: IA-2 小相册详情导航回归 (Round 2 FR-6 不破坏)
- 对应: IA-2
- 端: Android
- 前置条件: TC-11 通过
- 步骤:
  1. 进入小相册详情
  2. 点击返回 → 应回到列表
  3. 再次进入 → 点击恢复 → 应回到照片流 (Round 2 FR-6: photosTopDestinationName=TRASH)
  4. 进入 Viewer → 退出 → 应回到列表 (不是照片流)
- 预期结果:
  - 返回按钮回调正确
  - 恢复/移出后导航正确
  - Viewer 退出回到回收站 (Round 2 FR-6 修复不破坏)
- 通过标准: 4 个导航路径均正确

### TC-16: IA-3 大相册子小相册列表数据正确性
- 对应: IA-3
- 端: Android
- 前置条件: TC-10 通过
- 步骤:
  1. 进入大相册详情
  2. 对比子小相册列表条目数与 `entry.albumSnapshot?.postSnapshots?.size`
  3. 检查每项标题与对应 post.title 一致
  4. 检查媒体数 label 与 mediaSnapshots.size 一致
- 预期结果: 数据与 albumSnapshot 完全一致
- 通过标准: 数量 + 标题 + 媒体数均匹配

### TC-17: IA-4 小相册 Pager 高度自适应边界
- 对应: IA-4
- 端: Android
- 前置条件: TC-13 通过
- 步骤:
  1. 极端竖图 (aspectRatio = 0.5) → 高度应被 clamp 到 520dp
  2. 极端宽图 (aspectRatio = 2.5) → 高度应被 clamp 到 240dp
  3. aspectRatio = null → 默认 1f → 高度 = maxWidth
- 预期结果:
  - 极端比例被 `coerceIn(240.dp, 520.dp)` 限制
  - 无超屏、无异常压缩
- 通过标准: 3 种边界均正常

### TC-18: IA-5 AnimatedContent 切换动画+状态
- 对应: IA-5
- 端: Android
- 前置条件: TC-7 通过
- 步骤:
  1. 在"系统媒体删除"列表滚动到中部
  2. 切换到"小相册删除"
  3. 切回"系统媒体删除"
  4. 检查滚动位置是否保持
- 预期结果:
  - AnimatedContent 切换流畅
  - LazyGrid 状态由 `key = { page -> viewerEntries[page].id }` + rememberLazyGridState 保持
  - 切回时滚动位置恢复 (或符合 Compose 默认行为)
- 通过标准: 切换流畅 + 状态保持合理

### TC-19: IA-6 离线态按钮禁用
- 对应: IA-6
- 端: Android
- 前置条件: TC-12 通过
- 步骤:
  1. 离线状态下进入小相册详情
  2. 尝试点击恢复/移出按钮
  3. 检查按钮视觉 (半透明)
  4. 网络恢复后检查按钮状态
- 预期结果:
  - 离线时按钮 `enabled = false`，点击无响应
  - 按钮视觉半透明 (Material 默认 disabled 态)
  - 网络恢复后 `ReconnectRefreshEffect` 触发刷新，`isOfflineReadOnly = false`，按钮恢复
- 通过标准: 禁用 + 恢复两态均正确

### TC-20: IA-7 RealTrashDaysBadge 颜色 token 替换
- 对应: IA-7
- 端: Android
- 前置条件: 列表中有 days > 25 的条目
- 步骤:
  1. 检查 days > 25 的 badge 视觉
  2. 检查 days ≤ 25 的 badge 视觉
- 预期结果:
  - days > 25: `destructive` 红 (原 `error`) — 视觉一致但来源是 token
  - days ≤ 25: 其他 token 色
- 通过标准: 颜色与原 `error` 视觉一致 (因 `destructive = 0xFFBA1A1A = MaterialTheme error`)

### TC-21: IA-8 死代码删除编译
- 对应: IA-8
- 端: Android
- 前置条件: 无
- 步骤:
  1. Grep 验证 `RealTrashViewerOverlayButton` 在源代码中无引用
  2. Grep 验证 `RealTrashPostMediaGridTile` 在源代码中无引用
  3. Grep 验证 `RealTrashPostDetailTopBar` 在源代码中无引用
  4. Grep 验证 `RealTrashPostTopIconButton` 在源代码中无引用
  5. Grep 验证 `RealTrashPostTopIcon` 在源代码中无引用
- 预期结果: 5 个废弃符号在 `app/src/main/java/` 下均无引用
- 通过标准: 编译通过 + 5 个符号无源代码引用

---

## 五、跨模块回归测试

### TC-22: 回收站单元测试全部 PASS (RealTrashNavigationTest)
- 对应: 回归 (Round 2 FR-6 导航修复)
- 端: Android
- 前置条件: TC-2 通过
- 步骤/命令: `.\gradlew.bat :app:testDebugUnitTest --tests "com.example.yingshi.feature.photos.RealTrashNavigationTest" --no-daemon`
- 预期结果: 全部测试 PASS (Round 2 的 11 个导航测试不破坏)
- 通过标准: 退出码 0 + 测试报告中 0 FAIL

### TC-23: 回收站架构测试 PASS (RealTrashArchitectureTest)
- 对应: 回归 (Round 1 FR-2 架构清理)
- 端: Android
- 前置条件: TC-2 通过
- 步骤/命令: `.\gradlew.bat :app:testDebugUnitTest --tests "com.example.yingshi.feature.photos.RealTrashArchitectureTest" --no-daemon`
- 预期结果: 全部测试 PASS (UiState 默认值 + activeListKey 计算不破坏)
- 通过标准: 退出码 0 + 测试报告中 0 FAIL

### TC-24: 06 小相册复用组件不受影响
- 对应: 跨模块回归
- 端: Android
- 前置条件: TC-2 通过
- 步骤:
  1. Grep 验证 Round 3 未修改 `PostDetailBodyLayout`/`PostMediaArea`/`PostMediaCard` 等 06 模块文件
  2. 编译已通过证明无签名破坏
- 预期结果: 06 模块相关文件无修改
- 通过标准: Grep 无 Round 3 变更 + 编译已通过

### TC-25: 07 照片查看器视觉标准对齐
- 对应: 跨模块视觉对齐
- 端: Android
- 前置条件: TC-2 通过
- 步骤:
  1. 比较 RealTrashViewer.kt 与 07 模块 Viewer 的视觉 token 使用
  2. 检查 destructive 替换后视觉是否与 07 Viewer 一致
- 预期结果:
  - RealTrashViewer 使用 `viewerSurface`/`viewerAccent`/`viewerBackground` 与 07 一致
  - destructive 替换不破坏 Viewer 深色风格
- 通过标准: 视觉风格与 07 一致 + 编译通过

---

## 六、测试执行顺序

1. **编译层**: TC-1 → TC-2 (必须先通过)
2. **自动测试**: TC-22 → TC-23 (单元测试回归)
3. **功能测试**: TC-3 ~ TC-13 (按 FR 顺序)
4. **影响测试**: TC-14 ~ TC-21 (按 IA 顺序)
5. **跨模块回归**: TC-24 → TC-25

## 七、通过标准

- 全部 25 个 TC 均 PASS
- TC-1/TC-2 (编译) 0 FAIL (硬性要求)
- TC-22/TC-23 (单元测试) 0 FAIL (硬性要求)
- TC-21 (死代码验证) 100% 通过
- 其他 TC 允许 PARTIAL，但所有 FR AC 必须有对应测试通过

## 八、FAIL 处理

- TC-1/TC-2 FAIL: 立即停止，回到阶段3 修复
- TC-22/TC-23 FAIL: 根因分析 → 修复 → 重测 (优先级 P0)
- TC-3~TC-20 FAIL: 根因分析 → 修复 → 重测 (优先级 P1)
- TC-21 FAIL: 标记为遗漏，需补充清理后再测
- TC-24/TC-25 FAIL: 跨模块回归失败，优先级 P0，立即修复
