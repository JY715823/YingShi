# 实施计划 — Round 1: 首页核心视觉与数据预热

> 模块: 02-首页 | 轮次: Round 1 | 日期: 2026-07-08
> 包含需求: FR-1 (照片卡片视觉升级) + FR-2 (账本卡片视觉层级强化) + FR-3 (通知铃铛精修) + FR-5 (首页数据预热)
> 实施顺序: FR-5 → FR-1 → FR-2 → FR-3 → 编译验证

---

## 一、变更文件清单

| # | 文件路径 | 变更类型 | 涉及 FR | 变更概要 |
|---|---------|---------|--------|---------|
| 1 | `app/.../feature/home/HomeScreen.kt` | 修改 | FR-1/2/3/5 | 新增 FR-5 预热 LaunchedEffect；FR-1 照片卡片视觉升级；FR-2 账本卡片视觉强化；FR-3 铃铛呼吸光晕 |
| 2 | `app/.../ui/components/YingShiMotion.kt` | 修改 | FR-2 | 新增 `Modifier.yingShiShimmerSweep()` 可复用 shimmer 动画 Modifier |

**不变更文件**: HomeSummary.kt（数据层无改动）、AppReadCacheStore.kt（缓存层无改动）、RepositoryProvider.kt（仅读取使用）、YingShiAuroraBackdrop.kt（Round 2 处理）。

---

## 二、FR-5: 首页数据预热（首先实施）

### 2.1 落点
- 文件: `HomeScreen.kt`
- 位置: `HomeScreen()` Composable，L74（`rememberHomeUiState` 调用）之后

### 2.2 实施方案
新增一个 `LaunchedEffect(uiState.currentUser?.userId)`，在首页首次显示时并行拉取照片 feed 和通知数据，写入 `AppReadCacheStore`，触发 `changeVersion` 自增，`HomeSummary` 自动重组。

### 2.3 函数级计划

```kotlin
// 在 HomeScreen.kt L74 之后插入
LaunchedEffect(uiState.currentUser?.userId) {
    val userId = uiState.currentUser?.userId ?: return@LaunchedEffect
    runCatching {
        coroutineScope {
            launch {
                if (AppReadCacheStore.readPhotoFeed(userId) == null) {
                    val result = RepositoryProvider.mediaRepository.getMediaFeedPage(pageSize = 60)
                    if (result is ApiResult.Success) {
                        withContext(Dispatchers.IO) {
                            AppReadCacheStore.writePhotoFeed(
                                userId = userId,
                                payload = CachedPhotoFeed(
                                    items = result.data.items,
                                    nextCursor = result.data.nextCursor,
                                    hasMore = result.data.hasMore,
                                ),
                            )
                        }
                    }
                }
            }
            launch {
                if (AppReadCacheStore.readNotifications(userId) == null) {
                    val result = RepositoryProvider.notificationRepository.getNotifications(limit = 100)
                    if (result is ApiResult.Success) {
                        withContext(Dispatchers.IO) {
                            AppReadCacheStore.writeNotifications(
                                userId = userId,
                                notifications = result.data,
                            )
                        }
                    }
                }
            }
        }
    }.onFailure { /* 静默失败，不阻塞首页 */ }
}
```

### 2.4 验收标准对照
| AC | 要求 | 实现方式 |
|----|------|---------|
| AC-1 | 3秒内显示照片 | LaunchedEffect 启动即并行拉取，写入缓存后 changeVersion 触发重组 |
| AC-2 | 角标正确显示 | 通知拉取后写入缓存，unreadNotificationCount 重算 |
| AC-3 | 不阻塞渲染 | LaunchedEffect 异步，首页先渲染空态/骨架 |
| AC-4 | 不重复拉取 | `readPhotoFeed(userId) == null` / `readNotifications(userId) == null` 守卫 |
| AC-5 | 失败不影响使用 | `runCatching {}.onFailure {}` 静默吞掉异常 |

### 2.5 新增 import
- `androidx.compose.runtime.LaunchedEffect`
- `androidx.compose.runtime.launch`（coroutineScope 内）
- `kotlinx.coroutines.coroutineScope`
- `kotlinx.coroutines.Dispatchers`
- `kotlinx.coroutines.withContext`
- `com.example.yingshi.data.cache.AppReadCacheStore`
- `com.example.yingshi.data.cache.CachedPhotoFeed`
- `com.example.yingshi.data.repository.RepositoryProvider`
- `com.example.yingshi.data.repository.ApiResult`

---

## 三、FR-1: 照片卡片视觉升级

### 3.1 落点
- 文件: `HomeScreen.kt`
- 组件: `HomePhotoCard` (L186-295), `HomePhotoCollage` (L297-359), `HomePhotoPane` (L361-404)

### 3.2 组件级计划

#### 3.2.1 HomePhotoCard (L186-295) — 磨砂玻璃质感 + 遮罩柔和化 + 边框多层化

**改动 1: 磨砂玻璃质感增强 (AC-1)**
- 当前 L204: `color = colors.raisedSurface.copy(alpha = 0.72f)`
- 改为: 增加 `shadowElevation` 从 4.dp → 6.dp，并在背景渐变中增加一层 `glowWash` 提升通透感
- 当前 L211-219 背景渐变 3 色改为 4 色叠加，增加 `colors.glowWash.copy(alpha = 0.24f)` 作为中间层

**改动 2: 遮罩柔和化 (AC-3)**
- 当前 L229-235: verticalGradient 3 色（白0.16 → 透明 → 蓝灰0.42），硬切明显
- 改为 5 色渐变，增加中间过渡色，消除色带：
  ```kotlin
  colors = listOf(
      Color.White.copy(alpha = 0.16f),
      Color.White.copy(alpha = 0.04f),
      Color.Transparent,
      colors.titleAccent.copy(alpha = 0.18f),
      colors.titleAccent.copy(alpha = 0.42f),
  )
  ```
- **关键修复 (AC-4)**: L233 硬编码 `Color(0xCC5E708A).copy(alpha=0.42f)` → `colors.titleAccent.copy(alpha = 0.42f)`

**改动 3: 边框多层化 (AC-2)**
- 当前 L205: 单一 `border = BorderStroke(1.dp, colors.glassStroke.copy(alpha = 0.56f))`
- 在 Box 内层增加一道极细装饰边框（`colors.glassStroke.copy(alpha = 0.18f)`），形成双层边线效果
- 在 Box 的 background 渐变之后增加一个 `Modifier.border` 内层边框

#### 3.2.2 HomePhotoCollage (L297-359) — 拼贴精致化

- 保持现有 `spacedBy(10.dp)` 和圆角 24.dp（AC-2 已满足）
- pane 间距和圆角已一致，无需改动

#### 3.2.3 HomePhotoPane (L361-404) — 边框精致化

- 当前 L372: `border = BorderStroke(1.dp, colors.glassStroke.copy(alpha = 0.32f))`
- 保持现有边框，fallback 渐变色保留硬编码（属于装饰细节，FR-9 范围）

### 3.3 验收标准对照
| AC | 要求 | 实现方式 |
|----|------|---------|
| AC-1 | 磨砂玻璃质感 | shadowElevation 提升 + 背景多层渐变 |
| AC-2 | 拼贴间距圆角一致 | 现有 10dp/24dp 已满足，保持不变 |
| AC-3 | 遮罩柔和过渡 | verticalGradient 3色→5色，增加过渡色 |
| AC-4 | 颜色使用 token | L233 硬编码替换为 titleAccent.copy(alpha) |

---

## 四、FR-2: 账本卡片视觉层级强化

### 4.1 落点
- 文件: `YingShiMotion.kt` (新增 shimmer Modifier)
- 文件: `HomeScreen.kt`
- 组件: `HomeLedgerCard` (L459-536), `HomeLedgerHeroSignal` (L579-657), `HomeLedgerRecentSignal` (L659-750)

### 4.2 新建 Modifier.yingShiShimmerSweep() (AC-3)

#### 4.2.1 落点: YingShiMotion.kt 末尾追加

**参照**: `YingShiSurfaces.kt` L425-484 `YingShiPrimaryMistButton` 的 shimmer 实现

**设计**:
- 周期: 3500ms（3-5秒范围内，与登录页按钮一致）
- 缓动: LinearEasing + RepeatMode.Restart
- 颜色: `Transparent → White@0.18 → Transparent`（与登录页一致）
- motionEnabled 守卫: 关闭时返回 this 不施加动画
- 可配置: `enabled: Boolean = true` 控制是否启用

```kotlin
@Composable
fun Modifier.yingShiShimmerSweep(
    enabled: Boolean = true,
    motionEnabled: Boolean = rememberYingShiMotionEnabled(),
): Modifier {
    if (!enabled || !motionEnabled) return this
    val transition = rememberInfiniteTransition(label = "yingShiShimmer")
    val offset by transition.animateFloat(
        initialValue = 0f,
        targetValue = 1f,
        animationSpec = infiniteRepeatable(
            animation = tween(durationMillis = 3500, easing = LinearEasing),
            repeatMode = RepeatMode.Restart,
        ),
        label = "yingShiShimmerOffset",
    )
    return this.background(
        Brush.linearGradient(
            colors = listOf(
                Color.Transparent,
                Color.White.copy(alpha = 0.18f),
                Color.Transparent,
            ),
            start = Offset(offset * 3f - 1f, 0f),
            end = Offset(offset * 3f, 0f),
        ),
    )
}
```

#### 4.2.2 新增 import (YingShiMotion.kt)
- `androidx.compose.animation.core.LinearEasing`
- `androidx.compose.animation.core.RepeatMode`
- `androidx.compose.animation.core.animateFloat`
- `androidx.compose.animation.core.infiniteRepeatable`
- `androidx.compose.animation.core.rememberInfiniteTransition`
- `androidx.compose.foundation.background`（已有）

### 4.3 HomeLedgerCard (L459-536) — 卡片 shimmer

- 在 L489 Column 的 `.background(...)` 之后追加 `.yingShiShimmerSweep()`
- 使整个账本卡片有微妙光泽扫过

### 4.4 HomeLedgerHeroSignal (L579-657) — 丝绸光泽 (AC-1)

**改动 1: 替换硬编码暖色 (token 化)**
- L599: `Color(0xFFFFF5ED).copy(alpha = 0.64f)` → `colors.memoryWash.copy(alpha = 0.64f)`

**改动 2: 强化丝绸光泽**
- 当前 L594-602: linearGradient 3 色
- 改为 4 色渐变，增加 `colors.glowWash.copy(alpha = 0.36f)` 作为高光层：
  ```kotlin
  colors = listOf(
      accentColor.copy(alpha = 0.16f),
      colors.glowWash.copy(alpha = 0.36f),
      Color.White.copy(alpha = 0.70f),
      colors.memoryWash.copy(alpha = 0.64f),
  )
  ```

### 4.5 HomeLedgerRecentSignal (L659-750) — 轻量化 (AC-2)

- 当前 L671: `color = colors.raisedSurface.copy(alpha = 0.70f)`
- 改为: `colors.raisedSurface.copy(alpha = 0.56f)`（降低 alpha，视觉轻于 Hero 区）
- 当前 L672: `border = BorderStroke(1.dp, colors.dividerSoft.copy(alpha = 0.58f))`
- 改为: `colors.dividerSoft.copy(alpha = 0.42f)`（边框也减弱）

### 4.6 验收标准对照
| AC | 要求 | 实现方式 |
|----|------|---------|
| AC-1 | Hero 丝绸光泽 | 4色渐变 + memoryWash 暖色 |
| AC-2 | 最近一笔轻量化 | alpha 0.70→0.56，边框 0.58→0.42 |
| AC-3 | 卡片 shimmer 3-5秒 | yingShiShimmerSweep 3500ms |
| AC-4 | 圆角边框一致 | 保持 30dp，与照片卡片设计语言一致 |

---

## 五、FR-3: 通知铃铛精修

### 5.1 落点
- 文件: `HomeScreen.kt`
- 组件: `HomeNotificationBellButton` (L752-827)

### 5.2 组件级计划

#### 5.2.1 呼吸光晕动画 (AC-3)

**当前状态**: L777-798 静态 `radialGradient`，无动画
**改动**: 用 `rememberInfiniteTransition` + `animateFloat` 实现呼吸效果

```kotlin
val motionEnabled = rememberYingShiMotionEnabled()
val hasUnread = unreadCount > 0
val breathTransition = if (hasUnread && motionEnabled) {
    rememberInfiniteTransition(label = "bellBreath")
} else null
val breathAlpha by (breathTransition?.animateFloat(
    initialValue = 0.20f,
    targetValue = 0.44f,
    animationSpec = infiniteRepeatable(
        animation = tween(durationMillis = 2400, easing = FastOutSlowInEasing),
        repeatMode = RepeatMode.Reverse,
    ),
    label = "bellBreathAlpha",
) ?: remember { mutableFloatStateOf(0.36f) })
```

将 L784 `colors.memoryWash.copy(alpha = 0.36f)` 替换为 `colors.memoryWash.copy(alpha = breathAlpha)`，实现 2.4 秒周期的呼吸光晕（在 2-3 秒范围内）。

#### 5.2.2 角标比例优化 (AC-2)

- 当前 L819: `Modifier.padding(horizontal = 5.dp, vertical = 1.dp)`
- 调整为: `padding(horizontal = 6.dp, vertical = 2.dp)`，文字更居中、比例更协调
- 当前 L820: `labelSmall` 字体保持，但增加 `fontSize` 微调确保数字居中

#### 5.2.3 图标优化 (AC-1)

- 当前已使用 `Icons.Rounded.NotificationsActive` / `Icons.Rounded.Notifications`（有/无未读区分）
- 保持现状，图标已精致（历史 Issue 06-09-01 已修复）

### 5.3 验收标准对照
| AC | 要求 | 实现方式 |
|----|------|---------|
| AC-1 | 图标有/无未读区分 | 已使用 NotificationsActive/Notifications |
| AC-2 | 角标比例协调 | padding 微调 |
| AC-3 | 呼吸光晕 2-3秒 | breathTransition 2400ms Reverse |
| AC-4 | 99+ 封顶 memoryAccent | 现有实现已满足 |

### 5.4 新增 import (HomeScreen.kt)
- `androidx.compose.animation.core.FastOutSlowInEasing`
- `androidx.compose.animation.core.RepeatMode`
- `androidx.compose.animation.core.animateFloat`
- `androidx.compose.animation.core.infiniteRepeatable`
- `androidx.compose.animation.core.rememberInfiniteTransition`
- `androidx.compose.animation.core.tween`
- `androidx.compose.runtime.mutableFloatStateOf`

---

## 六、实施顺序与里程碑

| 步骤 | 内容 | 涉及文件 | 验证点 |
|------|------|---------|--------|
| 1 | FR-5 数据预热 LaunchedEffect | HomeScreen.kt | 缓存存在性守卫 + 静默失败 |
| 2 | FR-2 新建 yingShiShimmerSweep | YingShiMotion.kt | motionEnabled 守卫 + 3500ms 周期 |
| 3 | FR-1 照片卡片视觉升级 | HomeScreen.kt | L233 硬编码替换 + 遮罩5色 + 双层边框 |
| 4 | FR-2 账本卡片视觉强化 | HomeScreen.kt | shimmer 应用 + Hero 4色 + Recent 轻量化 |
| 5 | FR-3 铃铛呼吸光晕 | HomeScreen.kt | breathTransition + 角标 padding |
| 6 | 编译验证 | — | `gradlew.bat assembleDebug --no-daemon` |

---

## 七、风险与缓解

| # | 风险 | 严重度 | 缓解措施 |
|---|------|--------|---------|
| 1 | FR-5 预热与缓存刷新冲突 | 中 | `readPhotoFeed == null` 守卫，已有缓存跳过 |
| 2 | FR-1 遮罩 5 色渐变性能 | 低 | Compose 渐变缓存机制，无额外开销 |
| 3 | FR-2 shimmer 动画性能 | 低 | motionEnabled 守卫 + 仅可见时运行 |
| 4 | FR-3 呼吸动画内存 | 低 | hasUnread=false 时不创建 transition |
| 5 | 视觉改动影响小屏密度 | 低 | 保持现有间距尺寸，只改颜色/材质/阴影 |
| 6 | 预热网络不可用无限重试 | 低 | LaunchedEffect 仅执行一次，runCatching 静默 |

---

## 八、NFR 合规检查

| NFR | 要求 | 合规设计 |
|-----|------|---------|
| NFR-1 | 动画可访问性 | yingShiShimmerSweep + breathTransition 均检查 motionEnabled |
| NFR-2 | 视觉不退步 | 保持现有间距/尺寸/圆角，只增强材质和颜色 |
| NFR-3 | 数据预热不阻塞 | LaunchedEffect 异步，首页先渲染 |
| NFR-4 | 编译通过 | 步骤6 编译验证 |
