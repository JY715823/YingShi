# 测试报告 — Round 3: 颜色治理 + 空态精修

> 模块: 02-首页 | 轮次: Round 3 | 日期: 2026-07-08
> 测试范围: FR-9 + FR-10 + NFR-4

---

## 一、测试执行摘要

| 指标 | 数值 |
|------|------|
| 总 AC 数 | 10 |
| 通过 | 10 |
| 失败 | 0 |
| 阻塞 | 0 |
| 通过率 | 100% |
| 编译 | BUILD SUCCESSFUL (2m14s) |
| 编译错误 | 0 |

---

## 二、FR-9: 首页硬编码颜色 token 化

### AC-1: 照片卡片遮罩色改为 token 派生 ✅

| 位置 | 审查结果 |
|------|---------|
| L315 | `colors.raisedSurface.copy(alpha = 0.16f)` ✅ |
| L316 | `colors.raisedSurface.copy(alpha = 0.04f)` ✅ |
| L555 | `colors.raisedSurface.copy(alpha = 0.18f)` (HomeMetaPill 背景) ✅ |
| L556 | `colors.raisedSurface.copy(alpha = 0.24f)` (HomeMetaPill 边框) ✅ |
| L562 | `colors.raisedSurface` (HomeMetaPill 文字) ✅ |
| L912 | `colors.raisedSurface.copy(alpha = 0.68f)` (铃铛背景) ✅ |
| L933 | `colors.raisedSurface.copy(alpha = 0.28f)` (铃铛光晕) ✅ |

**判定**: 7 处 `Color.White` → `colors.raisedSurface`，全部通过。✅

### AC-2: 账本卡片渐变改为 token 派生 ✅

| 位置 | 修改前 | 修改后 | 结果 |
|------|--------|--------|------|
| L601 | `Color(0xFFEAF7F3).copy(alpha=0.94f)` | `colors.glowWash.copy(alpha=0.94f)` | ✅ |
| L602 | `Color(0xFFFFF2E8).copy(alpha=0.88f)` | `colors.memoryWash.copy(alpha=0.88f)` | ✅ |

**判定**: 2 处硬编码 `Color(0x...)` → token 派生，全部通过。✅

### AC-3: Hero 暖色改为 token 派生 ✅

| 位置 | 修改前 | 修改后 | 结果 |
|------|--------|--------|------|
| L723 | `Color.White.copy(alpha=0.70f)` | `colors.raisedSurface.copy(alpha=0.70f)` | ✅ |

**判定**: 1 处 `Color.White` → `colors.raisedSurface`，通过。✅

### AC-4: 拼贴 fallback 保留硬编码 ✅

| 位置 | 颜色 | 状态 |
|------|------|------|
| L402 | `Color(0xFFB9D6F5)` | 保留 ✅ |
| L403 | `Color(0xFFEAF5FF)` | 保留 ✅ |
| L412 | `Color(0xFFFFE8D9)` | 保留 ✅ |
| L427 | `Color(0xFFE9F6D8)` | 保留 ✅ |
| L437 | `Color(0xFFF1E4FF)` | 保留 ✅ |
| L438 | `Color(0xFFD8E8FF)` | 保留 ✅ |

**判定**: 6 个拼贴 fallback 装饰色全部保留，未修改。✅

---

## 三、FR-10: 空态视觉精修

### AC-1: 照片空态有质感 ✅

**代码**: `HomeScreen.kt L460-485`
- 新增 `contentAlignment = Alignment.Center`
- 新增 `Icons.Rounded.Landscape` Icon，32dp，tint = `colors.raisedSurface.copy(alpha = 0.14f)`
- 保留 pastel 背景 + radial 光晕

**判定**: 空态增加图标轮廓，视觉有质感。✅

### AC-2: 账本空态轻文案 ✅

**代码**: `HomeScreen.kt L866-895`
- 保留 "暂无记录" / "进入记账" 文案
- 无报错
- 新增 Wallet Icon（20dp, alpha 0.48）+ "最近一笔" 标签的 Row 布局

**判定**: 空态文案保持轻量，无报错，图标点缀和谐。✅

### AC-3: 空态颜色使用 token ✅

| 空态位置 | 颜色 | 来源 |
|---------|------|------|
| 照片空态 Landscape Icon | `colors.raisedSurface.copy(alpha=0.14f)` | token ✅ |
| 照片空态 fallbackBrush | 硬编码 (AC-4 保留) | 允许 ✅ |
| 照片空态 radialGradient | `colors.raisedSurface.copy(alpha=0.24f)` | token ✅ |
| 账本空态 Wallet Icon | `colors.textSecondary.copy(alpha=0.48f)` | token ✅ |
| 账本空态 "最近一笔" | `colors.textSecondary` | token ✅ |
| 账本空态 "暂无记录" | `colors.titleAccent` | token ✅ |

**判定**: 空态颜色全部使用 token 或 AC-4 允许的保留硬编码。✅

---

## 四、NFR 测试

| NFR | 要求 | 验证结果 |
|-----|------|---------|
| NFR-4 | 编译通过 | ✅ BUILD SUCCESSFUL in 2m14s, 0 errors |

---

## 五、编译验证

| 项目 | 详情 |
|------|------|
| 命令 | `gradlew.bat assembleDebug --no-daemon` |
| 结果 | **BUILD SUCCESSFUL** |
| 耗时 | 2m14s |
| 总任务 | 40 actionable (5 executed, 35 up-to-date) |
| 编译错误 | 0 |
| 编译警告 | 0 新增 |

---

## 六、结论

Round 3 全部 2 个 FR（FR-9/10）的 10 条 AC **全部通过**，编译 **BUILD SUCCESSFUL**。无遗留问题，可进入复检阶段。