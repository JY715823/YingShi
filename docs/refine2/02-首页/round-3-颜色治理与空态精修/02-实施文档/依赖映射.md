# 依赖映射 — Round 3: 颜色治理 + 空态精修

> 模块: 02-首页 | 轮次: Round 3 | 日期: 2026-07-08

---

## 一、本模块依赖的外部模块

| 依赖模块 | 依赖内容 | 本轮是否改动接口 | 影响说明 |
|---------|---------|----------------|---------|
| ui/theme | YingShiThemeTokens.colors (raisedSurface, glowWash, memoryWash) | 否 | 仅读取已有 token，不修改定义 |
| ui/components | YingShiAuroraBackdrop, yingShiSoftReveal, yingShiShimmerSweep 等 | 否 | 本轮不改动 |
| photos | AppContentMediaThumbnail | 否 | 仅非空态使用 |
| ledger | 账本数据 | 否 | 仅读取数据，不修改 |
| material-icons | Icons.Rounded.Landscape, Icons.Rounded.AccountBalanceWallet | 新增引用 | 新增 import，来自 Material Icons 库 |

---

## 二、依赖本模块的外部调用方

| 调用方 | 调用内容 | 本轮是否影响 | 影响说明 |
|--------|---------|------------|---------|
| AppShell (底部导航) | HomeScreen 作为 Tab 页面 | 否 | HomeScreen 对外接口未变 |
| 路由 | 导航到首页 | 否 | 路由不变 |

本轮变更仅涉及 HomeScreen.kt 内部子组件的颜色值和空态 UI，不改变任何函数签名、状态管理或对外接口。

---

## 三、前后端共享组件依赖

无 Server 端变更，不涉及共享组件。

---

## 四、数据流图

```
本轮变更范围 (仅 Android 端)
┌─────────────────────────────────────────────────┐
│ HomeScreen.kt                                   │
│                                                 │
│ ┌─ HomePhotoCard ────────────────────────────┐  │
│ │ 遮罩渐变: Color.White → raisedSurface      │  │
│ │ └─ HomeMetaPill: Color.White → raisedSurface│ │
│ │    └─ HomePhotoCollage                     │  │
│ │       └─ HomePhotoPane (x4)                │  │
│ │          空态: + Landscape Icon            │  │
│ │          光晕: Color.White → raisedSurface  │  │
│ └────────────────────────────────────────────┘  │
│                                                 │
│ ┌─ HomeLedgerCard ──────────────────────────┐  │
│ │ 渐变: Color(0xFF...) → glowWash/memoryWash│  │
│ │ └─ HomeLedgerHeroSignal                   │  │
│ │    Hero渐变: Color.White → raisedSurface   │  │
│ │ └─ HomeLedgerRecentSignal                  │  │
│ │    空态: + Wallet Icon, Row 重构           │  │
│ └────────────────────────────────────────────┘  │
│                                                 │
│ ┌─ HomeNotificationBellButton ───────────────┐  │
│ │ 背景: Color.White → raisedSurface          │  │
│ │ 光晕: Color.White → raisedSurface          │  │
│ └────────────────────────────────────────────┘  │
│                                                 │
│ 读取: YingShiThemeTokens.colors                  │
│   ├── raisedSurface (0xFFFFFFFC)                 │
│   ├── glowWash (0xFFE7FAFF)                      │
│   └── memoryWash (0xFFFFF1EE)                    │
└─────────────────────────────────────────────────┘
```

---

## 五、回归测试范围

| 范围 | 内容 | 测试方法 |
|------|------|---------|
| 照片卡片 | 有照片时显示正常 | 代码审查 L460-476 else 分支未变 |
| 账本卡片 | 有交易时显示正常 | 代码审查 L855-873 else 分支未变 |
| 铃铛按钮 | 有/无未读时显示正常 | 代码审查 L911/L932 仅替换颜色值 |
| Token 颜色 | raisedSurface 已定义 | 代码审查 Color.kt L7 |
| 编译 | 整体编译通过 | gradlew assembleDebug |