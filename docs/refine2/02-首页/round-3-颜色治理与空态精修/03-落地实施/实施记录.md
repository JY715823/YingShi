# 实施记录 — Round 3: 颜色治理 + 空态精修

> 模块: 02-首页 | 轮次: Round 3 | 日期: 2026-07-08
> 实施顺序: FR-9 → FR-10

---

## 一、变更文件总览

| # | 文件 | 变更行数 | 涉及 FR |
|---|------|---------|--------|
| 1 | `app/.../feature/home/HomeScreen.kt` | +2 import, ~10 行颜色替换, ~15 行空态增强 | FR-9, FR-10 |

---

## 二、FR-9: 首页硬编码颜色 token 化 (9 处)

### 2.1 照片遮罩渐变 (HomePhotoCard)

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L315 | `Color.White.copy(alpha = 0.16f)` | `colors.raisedSurface.copy(alpha = 0.16f)` |
| L316 | `Color.White.copy(alpha = 0.04f)` | `colors.raisedSurface.copy(alpha = 0.04f)` |

### 2.2 HomeMetaPill

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L551 | (无) | `val colors = YingShiThemeTokens.colors` (新增) |
| L555 | `Color.White.copy(alpha = 0.18f)` | `colors.raisedSurface.copy(alpha = 0.18f)` |
| L556 | `Color.White.copy(alpha = 0.24f)` | `colors.raisedSurface.copy(alpha = 0.24f)` |
| L562 | `Color.White` | `colors.raisedSurface` |

### 2.3 账本卡片渐变 (HomeLedgerCard)

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L601 | `Color(0xFFEAF7F3).copy(alpha = 0.94f)` | `colors.glowWash.copy(alpha = 0.94f)` |
| L602 | `Color(0xFFFFF2E8).copy(alpha = 0.88f)` | `colors.memoryWash.copy(alpha = 0.88f)` |

### 2.4 Hero 渐变 (HomeLedgerHeroSignal)

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L723 | `Color.White.copy(alpha = 0.70f)` | `colors.raisedSurface.copy(alpha = 0.70f)` |

### 2.5 铃铛按钮 (HomeNotificationBellButton)

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L912 | `Color.White.copy(alpha = 0.68f)` | `colors.raisedSurface.copy(alpha = 0.68f)` |
| L933 | `Color.White.copy(alpha = 0.28f)` | `colors.raisedSurface.copy(alpha = 0.28f)` |

### 2.6 拼贴空态光晕 (HomePhotoPane)

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| L468 | `Color.White.copy(alpha = 0.24f)` | `colors.raisedSurface.copy(alpha = 0.24f)` |

### 2.7 保留硬编码 (AC-4)

| 行号 | 颜色 | 保留原因 |
|------|------|---------|
| L402 | `Color(0xFFB9D6F5)` | 拼贴 fallback 装饰色 |
| L403 | `Color(0xFFEAF5FF)` | 拼贴 fallback 装饰色 |
| L412 | `Color(0xFFFFE8D9)` | 拼贴 fallback 装饰色 |
| L427 | `Color(0xFFE9F6D8)` | 拼贴 fallback 装饰色 |
| L437 | `Color(0xFFF1E4FF)` | 拼贴 fallback 装饰色 |
| L438 | `Color(0xFFD8E8FF)` | 拼贴 fallback 装饰色 |

---

## 三、FR-10: 空态视觉精修 (2 处)

### 3.1 HomePhotoPane 空态 (L460-485)

**修改**: 空态 Box 增加 `contentAlignment = Alignment.Center` + 居中 Landscape Icon

```kotlin
contentAlignment = Alignment.Center,
) {
    Icon(
        imageVector = Icons.Rounded.Landscape,
        contentDescription = null,
        modifier = Modifier.size(32.dp),
        tint = colors.raisedSurface.copy(alpha = 0.14f),
    )
}
```

### 3.2 HomeLedgerRecentSignal 空态 (L866-895)

**修改**: "最近一笔" 标签改为 Row 包裹，左侧增加 Wallet Icon

```kotlin
Row(
    verticalAlignment = Alignment.CenterVertically,
    horizontalArrangement = Arrangement.spacedBy(8.dp),
) {
    Icon(
        imageVector = Icons.Rounded.AccountBalanceWallet,
        contentDescription = null,
        modifier = Modifier.size(20.dp),
        tint = colors.textSecondary.copy(alpha = 0.48f),
    )
    Text(text = "最近一笔", ...)
}
```

---

## 四、新增 import

| # | import | 用途 |
|---|--------|------|
| 1 | `androidx.compose.material.icons.rounded.AccountBalanceWallet` | FR-10 账本空态图标 |
| 2 | `androidx.compose.material.icons.rounded.Landscape` | FR-10 照片空态图标 |

---

## 五、编译验证

| 项目 | 详情 |
|------|------|
| 命令 | `gradlew.bat assembleDebug --no-daemon` |
| 结果 | **BUILD SUCCESSFUL** |
| 耗时 | 2m14s |
| 总任务 | 40 actionable (5 executed, 35 up-to-date) |
| 编译错误 | 0 |
| 编译警告 | 0 新增