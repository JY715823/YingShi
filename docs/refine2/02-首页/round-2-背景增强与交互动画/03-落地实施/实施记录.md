# 实施记录 — Round 2: 背景增强与交互动画

> 模块: 02-首页 | 轮次: Round 2 | 日期: 2026-07-08
> 实施顺序: FR-4 → FR-6 → FR-7 → FR-8

---

## 一、变更文件总览

| # | 文件 | 变更行数 | 涉及 FR |
|---|------|---------|--------|
| 1 | `app/.../ui/components/YingShiAuroraBackdrop.kt` | ~10 行修改 | FR-4 |
| 2 | `app/.../feature/home/HomeScreen.kt` | +3 import, +18 FR-6, ~20 FR-7, ~15 FR-8 | FR-6/7/8 |

---

## 二、FR-4: 首页背景 HOME variant 增强 (YingShiAuroraBackdrop.kt)

### 2.1 参数修改清单

| 参数 | 行号 | 修改前 | 修改后 | 目的 |
|------|------|--------|--------|------|
| gradient memoryWash | L437 | `alpha = 0.62f` | `alpha = 0.82f` | 底色暖色调增强 |
| upperAurora 暖色 | L445 | `Color(0x6AFFF5E8)` | `colors.memoryWash.copy(alpha=0.42f)` | 硬编码→token 派生 |
| lowerAurora 暖色 | L446 | `Color(0x4CFFE1CF)` | `colors.memoryWash.copy(alpha=0.30f)` | 硬编码→token 派生 |
| goldTrace | L448 | `Color(0xC8F7D39A)` | `Color(0xE0F7C88A)` | 金色轨迹加深 |
| lowerBloom | L450 | `Color(0x30FFE0C7)` | `Color(0x3CFFE0C7)` | 下方光晕增强 |
| warmSparkleAlpha | L452 | `0.38f` | `0.52f` | 暖色粒子强度对齐 AUTH |

### 2.2 不变参数
- topLeftGlow, topRightGlow, bottomRightGlow, bottomLeftGlow, centerGlow, glassSweep, petalVeil
- coolSparkle, coolSparkleAlphaStrong, coolSparkleAlphaSoft
- 粒子呼吸动画 breathPhase (4000ms, L51-60, 所有 variant 共享)

---

## 三、FR-6: 卡片 staggered reveal 入场 (HomeScreen.kt)

### 3.1 新增状态变量 (L139-141)
```kotlin
var photoVisible by remember { mutableStateOf(false) }
var ledgerVisible by remember { mutableStateOf(false) }
var bellVisible by remember { mutableStateOf(false) }
```

### 3.2 新增 LaunchedEffect (L142-154)
```kotlin
LaunchedEffect(Unit) {
    if (motionEnabled) {
        photoVisible = true       // 0ms
        delay(120)                // 120ms
        ledgerVisible = true      // 120ms
        delay(80)                 // 80ms
        bellVisible = true        // 200ms
    } else {
        // 减少动画: 全部瞬间可见
        photoVisible = true
        ledgerVisible = true
        bellVisible = true
    }
}
```

### 3.3 yingShiSoftReveal visible 参数

| 组件 | 行号 | 修改前 | 修改后 |
|------|------|--------|--------|
| 标题「映世」 | L179 | `visible = true` (默认) | 不变 |
| 状态药丸 | L194 | `visible = true` (默认) | 不变 |
| 照片卡片 | L202 | `visible = true` (默认) | `visible = photoVisible` |
| 账本卡片 | L216 | `visible = true` (默认) | `visible = ledgerVisible` |
| 铃铛按钮 | L183 | **无 softReveal** | `visible = bellVisible` (新增 Box 包裹) |

---

## 四、FR-7: 协作人圆点交互优化 (HomeScreen.kt L486-537)

### 4.1 新增 animateColorAsState 颜色过渡
```kotlin
val motionEnabled = rememberYingShiMotionEnabled()
val bgColor by animateColorAsState(
    targetValue = if (selected) primaryContainer@0.96 else raisedSurface@0.78,
    animationSpec = tween(if (motionEnabled) 200 else 0),
    label = "dotBgColor",
)
val textColor by animateColorAsState(
    targetValue = if (selected) titleAccent else textSecondary,
    animationSpec = tween(if (motionEnabled) 200 else 0),
    label = "dotTextColor",
)
```

### 4.2 外圈光晕
- 外层 Box 尺寸: selected 时 34.dp，否则 30.dp
- 选中态: 新增 34.dp glassStroke@0.24 外圈背景
- 未选中态: 无外圈

### 4.3 shadowElevation 调整
- 增加 `motionEnabled` 守卫: `if (selected && motionEnabled) 1.dp else 0.dp`

---

## 五、FR-8: 账本筛选 chip 视觉精修 (HomeScreen.kt L640-696)

### 5.1 下拉标记 "▾" → Icons.Rounded.ExpandMore
- 删除: `text = if (enabled) "$label ▾" else label`
- 新增: `text = label` + `if (enabled) { Icon(ExpandMore, 16.dp) }`

### 5.2 选中态背景色
- 修改前: `colors.sectionBackground.copy(alpha = if (enabled) 0.90f else 0.70f)`
- 修改后: `if (enabled) accentColor.copy(alpha = 0.12f) else sectionBackground@0.70`

### 5.3 选中态边框
- 修改前: `BorderStroke(1.dp, colors.dividerSoft.copy(alpha = 0.72f))` (始终)
- 修改后: enabled 时 `accentColor.copy(alpha = 0.32f)`, 否则 `dividerSoft@0.72`

---

## 六、新增 import 汇总

### HomeScreen.kt
```kotlin
import androidx.compose.animation.core.animateColorAsState  // FR-7
import androidx.compose.material.icons.rounded.ExpandMore    // FR-8
import androidx.compose.material3.Icon                        // FR-8
import kotlinx.coroutines.delay                              // FR-6
```

### YingShiAuroraBackdrop.kt
- 无新增 import（colors 已在作用域内）

---

## 七、编译验证

**命令**: `gradlew.bat assembleDebug --no-daemon`
**工作目录**: `E:/Study/App/YingShi`
**结果**: BUILD SUCCESSFUL in 6m31s

### 编译错误与修复

| # | 错误 | 位置 | 修复 |
|---|------|------|------|
| 1 | `animateColorAsState` 包路径错误: `animation.core.animateColorAsState` 不存在 | HomeScreen.kt L3 | 改为 `androidx.compose.animation.animateColorAsState` |
| 2 | `background(CircleShape, glassStroke)` 参数顺序错误 | HomeScreen.kt L520 | 改为 `background(glassStroke.copy(alpha=0.24f), CircleShape)` |
| 3 | `infiniteRepeatable` 缺少 import | HomeScreen.kt L888 | 新增 `import androidx.compose.animation.core.infiniteRepeatable` |

### 编译统计
- 总任务: 40 actionable
- 执行: 5 tasks (compileDebugKotlin, dexBuilderDebug, mergeProjectDexDebug, packageDebug, assembleDebug)
- 复用: 35 tasks UP-TO-DATE
- 警告: 0 新增（仅预存 deprecation 警告）
- 错误: 0