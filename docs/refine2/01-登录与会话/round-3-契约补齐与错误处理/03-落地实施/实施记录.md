# Round 3 落地实施记录：契约补齐与错误处理

> 实施日期: 2026-07-08
> 对应需求: FR-5
> 实施者: Trae

## 一、变更文件清单（完整）

| # | 文件路径 | 端 | 变更类型 | 状态 |
|---|---------|-----|---------|------|
| 1 | `YingShi/app/src/main/java/com/example/yingshi/feature/auth/LoginScreen.kt` | Android | 修改 | ✅ 完成 |
| 2 | `YingShi/docs/contracts/auth-api.md` | Android 文档 | 修改 | ✅ 完成 |
| 3 | `YingShi-Server/docs/contracts/auth-api.md` | Server 文档 | 修改 | ✅ 完成 |

## 二、逐文件实施记录

### 文件1: LoginScreen.kt

#### 变更1.1: 新增 isAccountLocked 状态
**位置**: L160（紧邻 nowMillis 声明后）
**变更**:
```kotlin
var isAccountLocked by rememberSaveable { mutableStateOf(false) }
```
**理由**: 使用 rememberSaveable 确保配置变更后保留锁定状态；独立状态避开 errorMessage 的 16 处清除点。

#### 变更1.2: handleActionError 增加锁定分支
**位置**: L209-227（handleActionError 函数）
**变更**:
```kotlin
fun handleActionError(result: ApiResult.Error) {
    val effectiveCode = result.code
    if (
        effectiveCode == "AUTH_LOGIN_CODE_EXPIRED" ||
        effectiveCode == "AUTH_LOGIN_CHALLENGE_INVALID"
    ) {
        clearChallenge()
    }
    if (effectiveCode == "AUTH_ACCOUNT_LOCKED") {
        isAccountLocked = true
        errorMessage = "账号已临时锁定，请 15 分钟后再试"
    } else {
        errorMessage = result.message
    }
    statusMessage = null
    pendingAction = null
    waitingForNetwork = false
    isLoading = false
}
```
**理由**: AUTH_ACCOUNT_LOCKED 分支优先设置中文文案 + isAccountLocked 标志，其余错误码走原逻辑透传 message。

#### 变更1.3: queueAction 重置锁定状态
**位置**: L447-455（queueAction 函数）
**变更**:
```kotlin
fun queueAction(action: PendingLoginAction) {
    actionNonce += 1
    pendingAction = action
    waitingForNetwork = false
    errorMessage = null
    statusMessage = null
    isAccountLocked = false
    launchPendingAction(actionNonce, action)
}
```
**理由**: 每次发起新登录动作时重置锁定状态，允许用户在锁定解除后重试。

#### 变更1.4: 按钮禁用逻辑增加 !isAccountLocked
**位置**: L666-670（YingShiPrimaryMistButton 的 enabled 参数）
**变更**:
```kotlin
enabled = if (challengeState == null) {
    account.isNotBlank() && password.isNotBlank() && !isLoading && !isAccountLocked
} else {
    verificationCode.length >= 6 && !isLoading && !isAccountLocked
},
```
**理由**: 两个阶段都增加锁定禁用，实现 AC-2。

### 文件2: Android auth-api.md
**位置**: L211-213（Error Codes 章节）
**变更**: 在 AUTH_INVALID_CREDENTIALS 后插入 AUTH_ACCOUNT_LOCKED
```
- `AUTH_INVALID_CREDENTIALS`
- `AUTH_ACCOUNT_LOCKED`
- `AUTH_LOGIN_CHALLENGE_INVALID`
```

### 文件3: Server auth-api.md
**位置**: L242-244（Error Codes 章节）+ L260-268（新增 Account Lockout 段）
**变更1**: 在 AUTH_INVALID_CREDENTIALS 后插入 AUTH_ACCOUNT_LOCKED
**变更2**: 在 Error Codes 列表末尾新增 Account Lockout 说明段
```markdown
## Account Lockout

- Triggered by `POST /api/auth/login/challenge` after `app.auth.account-lockout.max-attempts` (default 5) consecutive password failures
- HTTP status: `429 Too Many Requests`
- Lock duration: `app.auth.account-lockout.lock-duration` (default 15 minutes)
- Failed counter resets to 0 on successful password validation or after lock expiry
- Verification code failures do NOT trigger account lockout (only invalidate the challenge)
- Response body does not include `lockedUntil` / remaining time — clients should show a generic "try again later" message
- Lock check happens before password validation in `AuthService.requireAccountNotLocked`
```

## 三、完整性自检

对照实施计划逐条验证：

| # | 实施计划项 | 状态 | 验证方式 |
|---|-----------|------|---------|
| 1 | isAccountLocked 状态声明（rememberSaveable） | ✅ | L160 确认 |
| 2 | handleActionError 增加 AUTH_ACCOUNT_LOCKED 分支 + 中文文案 | ✅ | L217-219 确认 |
| 3 | queueAction 重置 isAccountLocked | ✅ | L453 确认 |
| 4 | 按钮禁用逻辑增加 !isAccountLocked（密码阶段 + 验证码阶段） | ✅ | L667/669 确认 |
| 5 | Android auth-api.md 补充 AUTH_ACCOUNT_LOCKED | ✅ | L212 确认 |
| 6 | Server auth-api.md 补充 AUTH_ACCOUNT_LOCKED + Account Lockout 说明段 | ✅ | L243/L260-268 确认 |
| 7 | Android 编译通过 | ✅ | BUILD SUCCESSFUL in 8m 27s |
| 8 | Server 编译通过 | ✅ | BUILD SUCCESS in 1m 54s |

**完整性评分: 8/8 (100%)**

## 四、计划外变更

无。所有变更均按实施计划执行，无新增文件，无计划外修改。

## 五、编译验证结果

### Android 编译
- 命令: `.\gradlew.bat assembleDebug --no-daemon`
- 结果: BUILD SUCCESSFUL in 8m 27s
- 任务: 40 actionable tasks: 5 executed, 35 up-to-date
- 警告: 仅有既有 deprecation warning（statusBarColor / Icons.Rounded.Login / ExperimentalCoilApi），与本次改动无关
- 备注: 编译后 TRAE Sandbox 拦截了 Kotlin daemon 临时文件清理，不影响编译产物

### Server 编译
- 命令: `.\mvnw.cmd compile`
- 结果: BUILD SUCCESS in 1m 54s
- 任务: Compiling 273 source files
- 备注: 本轮无 Server 代码改动，编译验证按规范执行

## 六、实施总结

本轮改动集中在 3 个文件，共 6 处变更：
- LoginScreen.kt: 4 处（状态声明 + 错误处理 + 重置逻辑 + 按钮禁用）
- Android auth-api.md: 1 处（错误码列表）
- Server auth-api.md: 2 处（错误码列表 + Account Lockout 说明段）

所有变更均通过编译验证，无新增文件，无计划外改动。FR-5 的 4 个 AC 已在代码层面全部落实：
- AC-1: handleActionError 显示中文"账号已临时锁定，请 15 分钟后再试"
- AC-2: 按钮禁用逻辑增加 !isAccountLocked（两阶段）
- AC-3: 两端 auth-api.md 补充 AUTH_ACCOUNT_LOCKED
- AC-4: queueAction 重置 isAccountLocked 允许重试
