# Round 1 实施记录：登录页视觉重构

> 实施日期: 2026-07-08
> 实施顺序: FR-4 → FR-1 → FR-6
> 编译状态: 进行中

---

## 实施步骤

### 1. FR-4: 品牌区域丰富 ✅

**文件**: `app/src/main/java/com/example/yingshi/feature/auth/LoginScreen.kt`
**函数**: `BrandHeader()` (L706-723)

**变更**:
- 修改 Column 参数: 增加 `horizontalAlignment = Alignment.CenterHorizontally`
- 将 `spacedBy(2.dp)` 改为 `spacedBy(6.dp)`
- 新增副标题 Text: `"我们的共同空间"`
- 副标题样式: `MaterialTheme.typography.titleSmall`, `colors.goldAccent.copy(alpha = 0.78f)`

**提交**: 已完成，代码修改正确。

---

### 2. FR-1: 表单容器卡片 ✅

**文件**: `app/src/main/java/com/example/yingshi/feature/auth/LoginScreen.kt`
**范围**: L482-654，原 Column 包装

**变更**:
- 将原 Column（包含 BrandHeader、PresetAccountRow、输入框、按钮等）移入 `Surface`
- 新增: `Surface` + `Box` + 内层 `Column`
- `Surface` 参数:
  - shape: `RoundedCornerShape(YingShiThemeTokens.radius.xl)` (28dp)
  - color: `colors.raisedSurface.copy(alpha = 0.88f)`
  - border: `BorderStroke(1.dp, colors.glassStroke.copy(alpha = 0.50f))`
  - shadowElevation: `6.dp`
- `Box` 背景: 水平渐变 `colors.glowWash.copy(alpha = 0.10f)` + `colors.raisedSurface.copy(alpha = 0.20f)` + `Color.Transparent`
- 内层 Column: `padding(YingShiThemeTokens.spacing.xl)` (24dp), 保持 `spacedBy(18.dp)`
- `widthIn(max = 560.dp)` 从原 Column 移到 Surface

**新增 import**:
- `androidx.compose.foundation.BorderStroke` ✅
- `androidx.compose.foundation.shape.RoundedCornerShape` ✅
- `androidx.compose.material3.Surface` ✅

**提交**: 已完成，结构正确。

---

### 3. FR-6: 连接设置入口视觉弱化 ✅

**文件**: `app/src/main/java/com/example/yingshi/feature/auth/LoginScreen.kt`
**函数**: `ConnectionEntryLink()` (L826-880)

**变更**:
- Icon tint: `colors.titleAccent.copy(alpha = 0.88f)` → `colors.textSecondary.copy(alpha = 0.72f)`
- Text style: `MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.SemiBold)` → `MaterialTheme.typography.labelMedium` (去掉 SemiBold)
- Text color: `colors.titleAccent` → `colors.textSecondary`

**提交**: 已完成，改动正确。

---

## 变更统计

| 项 | 数量 |
|---|------|
| 新增文件 | 0 |
| 修改文件 | 1 (`LoginScreen.kt`) |
| 删除文件 | 0 |
| 新增行数 | ~15 |
| 修改行数 | ~6 |
| 删除行数 | 0 |

---

## 编译验证

当前状态: Gradle 编译中（Kotlin daemon 连接问题，回退到无 daemon 编译，速度较慢）

预期结果: `assembleDebug` 成功。

---

## 完整性自检（按实施计划逐条对照）

| # | 实施计划项 | 状态 | 备注 |
|---|---------|------|------|
| 1 | FR-4 BrandHeader 新增副标题 | ✅ 完成 | 文案"我们的共同空间"，颜色 goldAccent alpha 0.78，字号 titleSmall，间距 6dp，居中对齐 |
| 2 | FR-1 表单区域外层包裹 Surface 卡片 | ✅ 完成 | raisedSurface alpha 0.88 + glassStroke alpha 0.50 + radius.xl 28dp + shadowElevation 6dp + glowWash 渐变 + padding 24dp |
| 3 | FR-1 新增 import BorderStroke/RoundedCornerShape/Surface | ✅ 完成 | 三个 import 都已添加 |
| 4 | FR-6 连接设置 Icon 颜色改为 textSecondary | ✅ 完成 | alpha 0.72 |
| 5 | FR-6 连接设置 Text 颜色改为 textSecondary，字号改为 labelMedium | ✅ 完成 | 去掉 SemiBold |
| 6 | 所有颜色使用 YingShiThemeTokens | ✅ 完成 | 无硬编码 |

**完整性**: 计划内 6 项全部完成，无遗漏。

**计划外变更**: 无。