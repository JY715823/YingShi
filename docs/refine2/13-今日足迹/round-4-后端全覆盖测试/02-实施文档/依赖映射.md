# Round 4 依赖映射

> 基于 FR-12: 后端全覆盖测试

---

## 本模块依赖的外部模块/服务

| 依赖 | 本轮是否改动接口 | 说明 |
|------|:--:|------|
| LifeConsoleService | 否 | 被测对象，不修改 |
| LifeConsoleController | 否 | 被测对象，不修改 |
| AlbumRepository | 否 | 测试数据准备使用 |
| PostRepository | 否 | 测试数据准备使用 |
| PostMediaRepository | 否 | 测试数据准备使用 |
| MediaRepository | 否 | 测试数据准备使用 |
| BowelEventRepository | 否 | 测试数据准备使用 |
| UserRepository | 否 | 测试数据准备使用 |
| SharedLibraryMemberRepository | 否 | 测试数据准备使用 |
| TrashService | 否 | deleteMedia 依赖 |
| PushNotificationService | 否 | 通过 CapturingPushMessageSender 捕获 |
| ContentMapper | 否 | MediaEntity→MediaDto 转换 |
| AbstractPostgresIntegrationTest | 否 | 测试基类 |

---

## 依赖本模块的外部调用方

| 调用方 | 本轮是否影响该接口 | 说明 |
|--------|:--:|------|
| Android 客户端 | 否 | 不改变 API 契约 |
| FCM 推送 | 否 | 不改变推送逻辑 |
| PhotoFeed 照片流 | 否 | 不改变 photo 域查询 |
| Sync 同步 | 否 | 不改变 lifeConsoleVersion |

---

## 前后端共享组件依赖

| 组件 | 本轮是否受影响 | 说明 |
|------|:--:|------|
| API 响应结构 | 否 | 不改变 DTO 结构 |
| 认证机制 | 否 | 测试验证 @AuthRequired 行为 |
| Testcontainers 配置 | 否 | 共享单例容器 |

---

## 数据流图

```
┌─────────────────────────────────────────────────────────┐
│                    Round 4 测试架构                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  LifeConsoleServiceTest                    ControllerTest│
│  ┌───────────────────┐          ┌──────────────────────┐│
│  │ @Autowired        │          │ MockMvc              ││
│  │ LifeConsoleService│          │  │                   ││
│  │ MediaRepository   │          │  ▼                   ││
│  │ AlbumRepository   │          │ LifeConsoleController││
│  │ PostRepository    │          │  │                   ││
│  │ PostMediaRepo     │          │  ▼                   ││
│  │ BowelEventRepo    │          │ LifeConsoleService   ││
│  │ UserRepository    │          └──────────────────────┘│
│  │ SharedLibMemberR  │                                  │
│  └───────┬───────────┘                                  │
│          │                                              │
│          ▼                                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │         AbstractPostgresIntegrationTest           │   │
│  │  ┌─────────────────────────────────────────────┐ │   │
│  │  │  PostgreSQLContainer (postgres:16-alpine)    │ │   │
│  │  │  ├── Flyway V1~V27 迁移                      │ │   │
│  │  │  ├── TestSeedDataInitializer                 │ │   │
│  │  │  │   ├── user_demo_a (OWNER)                 │ │   │
│  │  │  │   ├── user_demo_b (MEMBER)                │ │   │
│  │  │  │   └── library_shared                      │ │   │
│  │  │  ├── CapturingPushMessageSender              │ │   │
│  │  │  └── CapturingAuthLoginCodeSender            │ │   │
│  │  └─────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 回归测试范围

| 测试范围 | 说明 | 本轮是否执行 |
|---------|------|:--:|
| 现有 Server 测试 | `mvnw.cmd test` 全部通过，不引入回归 | 是 |
| API 契约 | 通过 ControllerTest 验证 6 个端点 | 是 |
| 数据库迁移 | Flyway V1~V27 全部执行 | 是 |
| 测试容器 | Testcontainers PostgreSQL 正常启动 | 是 |