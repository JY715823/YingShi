# Round 3 健壮性增强 — 实施记录

> 实施时间: 2026-07-18
> 编译结果: BUILD SUCCESSFUL

---

## 实施文件清单

| # | 文件 | 变更类型 | 状态 |
|---|------|---------|:--:|
| 1 | `data/remote/api/SyncApi.kt` | 修改 | OK |
| 2 | `data/cache/AppReadCacheStore.kt` | 修改 | OK |
| 3 | `data/remote/result/ApiResult.kt` | 修改 | OK |
| 4 | `data/repository/RealRepositories.kt` | 修改 | OK |
| 5 | `feature/life/LifeConsoleViewModel.kt` | 修改 | OK |
| 6 | `feature/life/LifeConsoleScreen.kt` | 修改 | OK |

---

## 变更详情

### 1. SyncApi.kt — FR-7
- `SyncVersionsDto` 新增 `val chatVersion: Long = 0L`

### 2. AppReadCacheStore.kt — FR-5
- 新增 scope: `LIFE_CONSOLE_TODAY_SCOPE`, `LIFE_CONSOLE_HISTORY_SCOPE`
- 新增 4 个 public 方法: `readLifeConsoleToday()`, `writeLifeConsoleToday()`, `readLifeConsoleHistory()`, `writeLifeConsoleHistory()`
- 新增 import: `RemoteLifeConsoleToday`, `RemoteLifeConsoleHistory`

### 3. ApiResult.kt — FR-5
- `ApiResult.Success` 新增 `isFromCache: Boolean = false`

### 4. RealRepositories.kt — FR-5 + FR-6
- `RealLifeConsoleRepository` 新增 `CACHE_MAX_AGE_MILLIS = 7天`
- `getToday()`: 成功时写缓存，失败时读缓存（7天过期检查），带 `isFromCache` 标记
- `getHistory()`: 同上
- 新增 `isCacheExpired()` 私有方法
- 新增 import: `AppReadCacheStore`

### 5. LifeConsoleViewModel.kt — FR-5 + FR-6
- `LifeConsoleUiState` 新增: `offlineMode`, `errorType`, `errorMessage`
- 新增 `LifeConsoleErrorType` 枚举: NETWORK, TIMEOUT, SERVER, UNKNOWN
- 新增 `Throwable.toLifeConsoleErrorType()` 扩展函数
- `loadToday()`: 处理 `isFromCache` → 设 `offlineMode`；`apiResult.Error` → 设 `errorType`
- `loadHistory()`: 同上
- 缓存数据时显示 INFO notice "正在使用离线数据"
- 新增 import: `HttpException`, `ConnectException`, `SocketTimeoutException`, `UnknownHostException`

### 6. LifeConsoleScreen.kt — FR-5 + FR-6
- 今日页新增离线 Banner: `AnimatedVisibility(visible = offlineMode)` 显示 "离线数据 · 点击刷新"
- 新增 `LaunchedEffect` 监听 `errorType`/`errorMessage`，通过 `showNotice` 展示分级错误
- 离线 Banner 点击触发 `loadToday()` + `loadHistory()` 刷新