# Round 6 实施计划

> 基于 FR-18 / FR-19 / FR-20: 位置追踪功能（前后端同步）
> 实施日期: 2026-07-18
> 数据流方案: 方案 B（Android 获取 GPS，服务端调高德反向地理编码）
> 高德 Web Key: 通过环境变量 `AMAP_GEOCODING_KEY` 注入

---

## 一、变更文件清单（完整）

| # | 文件路径 | 端 | 变更类型 | 变更内容 | FR |
|---|---------|-----|---------|---------|-----|
| 1 | `YingShi-Server/src/main/resources/db/migration/postgresql/V29__life_console_location_tracking.sql` | Server | 新建 | 三表加 lat/lng/location_label | FR-18 AC-1/2/6 |
| 2 | `YingShi-Server/src/main/java/com/yingshi/server/domain/MediaEntity.java` | Server | 修改 | 加 latitude/longitude/locationLabel | FR-18 AC-1 |
| 3 | `YingShi-Server/src/main/java/com/yingshi/server/domain/BowelEventEntity.java` | Server | 修改 | 加 latitude/longitude/locationLabel | FR-18 AC-2 |
| 4 | `YingShi-Server/src/main/java/com/yingshi/server/domain/UploadTaskEntity.java` | Server | 修改 | 加 latitude/longitude/locationLabel（透传） | FR-18 AC-3 |
| 5 | `YingShi-Server/src/main/java/com/yingshi/server/dto/upload/UploadTokenRequest.java` | Server | 修改 | 加可选 latitude/longitude/locationLabel | FR-18 AC-3 |
| 6 | `YingShi-Server/src/main/java/com/yingshi/server/service/upload/UploadService.java` | Server | 修改 | createUploadToken + buildMediaFromTask 透传位置 | FR-18 AC-3 |
| 7 | `YingShi-Server/src/main/java/com/yingshi/server/dto/life/AddBowelEventRequest.java` | Server | 新建 | 携带可选 latitude/longitude/locationLabel | FR-18 AC-4 |
| 8 | `YingShi-Server/src/main/java/com/yingshi/server/dto/life/LifeConsoleBowelEventDto.java` | Server | 修改 | 加 latitude/longitude/locationLabel | FR-18 AC-5 |
| 9 | `YingShi-Server/src/main/java/com/yingshi/server/dto/content/MediaDto.java` | Server | 修改 | 加 latitude/longitude/locationLabel（共享 DTO，NON_NULL） | FR-18 AC-5 |
| 10 | `YingShi-Server/src/main/java/com/yingshi/server/mapper/ContentMapper.java` | Server | 修改 | toMediaDto 传递位置 | FR-18 AC-5 |
| 11 | `YingShi-Server/src/main/java/com/yingshi/server/service/life/LifeConsoleService.java` | Server | 修改 | addBowelEvent 改签名 + withDisplayTime 同步位置 + toBowelEventDto 扩展 + 调 GeocodingService | FR-18 AC-4/5/7 |
| 12 | `YingShi-Server/src/main/java/com/yingshi/server/controller/LifeConsoleController.java` | Server | 修改 | addBowelEvent 加 @RequestBody AddBowelEventRequest | FR-18 AC-4 |
| 13 | `YingShi-Server/src/main/java/com/yingshi/server/service/geocoding/GeocodingService.java` | Server | 新建 | 接口: reverseGeocode(lat, lng) → String? | FR-18 AC-7 |
| 14 | `YingShi-Server/src/main/java/com/yingshi/server/service/geocoding/AmapGeocodingService.java` | Server | 新建 | 用 HttpClient 调高德 API + 缓存 + 重试 | FR-18 AC-7 |
| 15 | `YingShi-Server/src/main/java/com/yingshi/server/service/geocoding/NoopGeocodingService.java` | Server | 新建 | amap.enabled=false 时返回 null | FR-18 AC-7 |
| 16 | `YingShi-Server/src/main/java/com/yingshi/server/config/GeocodingProperties.java` | Server | 新建 | @ConfigurationProperties(prefix="app.geocoding.amap") | FR-18 AC-7 |
| 17 | `YingShi-Server/src/main/java/com/yingshi/server/config/AuthConfig.java` | Server | 修改 | @EnableConfigurationProperties 加 GeocodingProperties | FR-18 AC-7 |
| 18 | `YingShi-Server/src/main/resources/application.yml` | Server | 修改 | 加 app.geocoding.amap 段 | FR-18 AC-7 |
| 19 | `YingShi-Server/docker-compose.yml` | Server | 修改 | 加 AMAP_* 环境变量 | FR-18 AC-7 |
| 20 | `YingShi/gradle/libs.versions.toml` | Android | 修改 | 加 playServicesLocation 版本 | FR-19 AC-6 |
| 21 | `YingShi/app/build.gradle.kts` | Android | 修改 | implementation(libs.play.services.location) | FR-19 AC-6 |
| 22 | `YingShi/app/src/main/AndroidManifest.xml` | Android | 修改 | 加 ACCESS_FINE/COARSE_LOCATION 权限 + uses-feature | FR-19 AC-5 |
| 23 | `YingShi/app/src/main/java/com/example/yingshi/feature/life/LocationHelper.kt` | Android | 新建 | FusedLocationProviderClient 封装 + 静默 fallback | FR-19 AC-1/3/4/6 |
| 24 | `YingShi/app/src/main/java/com/example/yingshi/feature/life/LifeConsoleUploadBridge.kt` | Android | 修改 | 上传前 await LocationHelper + 扩展 UploadMetadata | FR-19 AC-1 |
| 25 | `YingShi/app/src/main/java/com/example/yingshi/feature/life/LifeConsoleViewModel.kt` | Android | 修改 | addBowelEvent 前 await LocationHelper | FR-19 AC-2 |
| 26 | `YingShi/app/src/main/java/com/example/yingshi/data/remote/dto/UploadDtos.kt` | Android | 修改 | UploadTokenRequestDto 加 latitude/longitude | FR-18/19 |
| 27 | `YingShi/app/src/main/java/com/example/yingshi/data/remote/dto/LifeConsoleDtos.kt` | Android | 修改 | 新增 LifeConsoleBowelEventRequestDto + 多个 DTO 加位置字段 | FR-18/19/20 |
| 28 | `YingShi/app/src/main/java/com/example/yingshi/data/remote/api/LifeConsoleApi.kt` | Android | 修改 | addBowelEvent 改 @Body | FR-19 AC-2 |
| 29 | `YingShi/app/src/main/java/com/example/yingshi/data/model/LifeConsoleModels.kt` | Android | 修改 | RemoteLifeConsole* 加 locationLabel | FR-20 |
| 30 | `YingShi/app/src/main/java/com/example/yingshi/data/model/RemoteModels.kt` | Android | 修改 | RemoteMedia 加 lat/lng/locationLabel | FR-20 |
| 31 | `YingShi/app/src/main/java/com/example/yingshi/data/repository/RealRepositories.kt` | Android | 修改 | addBowelEvent 签名扩展 + DTO mapper | FR-19 |
| 32 | `YingShi/app/src/main/java/com/example/yingshi/feature/life/LifeConsoleScreen.kt` | Android | 修改 | 4 处加 locationLabel 展示 | FR-20 AC-1/2/3/4 |
| 33 | `YingShi-Server/src/test/java/com/yingshi/server/service/life/LifeConsoleServiceTest.java` | Server | 修改 | addBowelEvent 测试发 Body + 位置断言 | FR-18 测试 |
| 34 | `YingShi-Server/src/test/java/com/yingshi/server/controller/LifeConsoleControllerTest.java` | Server | 修改 | addBowelEvent 测试发 Body + 位置断言 | FR-18 测试 |

**统计**: 新建 6 个文件 + 修改 28 个文件 = 34 个变更点

---

## 二、FR-18: 位置追踪后端

### FR-18-1: Flyway V29 迁移

**文件**: `V29__life_console_location_tracking.sql`（新建）

**SQL**（幂等，参考 V26/V27 风格）:
```sql
-- V29: Life Console Location Tracking
-- Add latitude / longitude / location_label to media, bowel_events, upload_tasks
ALTER TABLE media ADD COLUMN IF NOT EXISTS latitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE media ADD COLUMN IF NOT EXISTS longitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE media ADD COLUMN IF NOT EXISTS location_label VARCHAR(255) DEFAULT NULL;

ALTER TABLE bowel_events ADD COLUMN IF NOT EXISTS latitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE bowel_events ADD COLUMN IF NOT EXISTS longitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE bowel_events ADD COLUMN IF NOT EXISTS location_label VARCHAR(255) DEFAULT NULL;

ALTER TABLE upload_tasks ADD COLUMN IF NOT EXISTS latitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE upload_tasks ADD COLUMN IF NOT EXISTS longitude DOUBLE PRECISION DEFAULT NULL;
ALTER TABLE upload_tasks ADD COLUMN IF NOT EXISTS location_label VARCHAR(255) DEFAULT NULL;
```

**回滚方案**: Flyway 不支持回滚，如需回滚手动执行 `ALTER TABLE ... DROP COLUMN ...`。

### FR-18-2: Entity 加字段

**MediaEntity.java** — 加 3 个字段（Lombok @Getter/@Setter 自动生成访问器）:
```java
@Column(name = "latitude")
private Double latitude;

@Column(name = "longitude")
private Double longitude;

@Column(name = "location_label", length = 255)
private String locationLabel;
```

**BowelEventEntity.java** — 加同样的 3 个字段。

**UploadTaskEntity.java** — 加同样的 3 个字段（用于透传到 MediaEntity）。

### FR-18-3: UploadTokenRequest 加字段

**UploadTokenRequest.java** record 加可选字段:
```java
public record UploadTokenRequest(
    // ... 现有 17 个字段 ...
    Double latitude,        // 新增，可选
    Double longitude,       // 新增，可选
    String locationLabel    // 新增，可选（客户端不传，服务端反向地理编码填）
) {}
```

### FR-18-4: UploadService 透传位置

**UploadService.java** 改造点:

1. `createUploadToken` 方法（约 L150-200）: 从 request 复制 lat/lng 到 UploadTaskEntity
   ```java
   task.setLatitude(request.latitude());
   task.setLongitude(request.longitude());
   // locationLabel 暂不设置，等 GeocodingService 回填
   ```

2. `buildMediaFromTask` 私有方法（L584-617）: 从 task 复制到 MediaEntity
   ```java
   media.setLatitude(task.getLatitude());
   media.setLongitude(task.getLongitude());
   media.setLocationLabel(task.getLocationLabel());
   ```

3. **GeocodingService 调用时机**: 在 `buildMediaFromTask` 之后，如果 `media.getLatitude() != null && media.getLocationLabel() == null`，**同步调用** `geocodingService.reverseGeocode(lat, lng)`，如果返回非 null 则 `media.setLocationLabel(label)` 并 `mediaRepository.save(media)`。失败不阻断（GeocodingService 内部已捕获异常返回 null）。

### FR-18-5: AddBowelEventRequest 新建

**文件**: `dto/life/AddBowelEventRequest.java`（新建）
```java
package com.yingshi.server.dto.life;

import jakarta.validation.constraints.Nullable;

public record AddBowelEventRequest(
    @Nullable Double latitude,
    @Nullable Double longitude,
    @Nullable String locationLabel
) {
    public AddBowelEventRequest {
        // 默认构造，允许全 null
    }
}
```

### FR-18-6: LifeConsoleBowelEventDto 加字段

**LifeConsoleBowelEventDto.java** record 加字段:
```java
public record LifeConsoleBowelEventDto(
    String bowelEventId,
    String userId,
    Long occurredAtMillis,
    Double latitude,        // 新增
    Double longitude,       // 新增
    String locationLabel    // 新增
) {}
```

### FR-18-7: MediaDto 加字段（共享 DTO）

**MediaDto.java** record 加字段（已有 `@JsonInclude(NON_NULL)`，向后兼容）:
```java
public record MediaDto(
    // ... 现有 21 个字段 ...
    Double latitude,        // 新增
    Double longitude,       // 新增
    String locationLabel    // 新增
) {}
```

### FR-18-8: ContentMapper.toMediaDto 传递位置

**ContentMapper.java** 改造: 在 `toMediaDto(MediaEntity media, List<String> smallAlbumIds)` 方法中，构造 MediaDto 时传入位置字段:
```java
new MediaDto(
    // ... 现有 21 个字段 ...
    media.getLatitude(),
    media.getLongitude(),
    media.getLocationLabel()
)
```

**LifeConsoleService.withDisplayTime 同步**（L479-503，重要）: 重建 MediaDto 时必须同步传递位置字段，否则历史 API 丢失位置:
```java
private MediaDto withDisplayTime(MediaDto dto, Long displayTimeMillis) {
    return new MediaDto(
        // ... 现有 21 个字段（从 dto 透传）...
        dto.displayTimeMillis() != null ? displayTimeMillis : dto.displayTimeMillis(),  // 覆盖
        // ... 其他字段 ...
        dto.latitude(),       // 同步位置
        dto.longitude(),
        dto.locationLabel()
    );
}
```

### FR-18-9: LifeConsoleService 改造

**addBowelEvent 方法签名变更**:
```java
// 旧: public LifeConsoleBowelMutationResponse addBowelEvent(String zoneId, AuthenticatedUser currentUser)
// 新:
public LifeConsoleBowelMutationResponse addBowelEvent(
    AddBowelEventRequest request,  // 新增
    String zoneId,
    AuthenticatedUser currentUser
) {
    BowelEventEntity event = new BowelEventEntity();
    event.setId(IdGenerator.newId("bowel"));
    event.setLibraryId(currentUser.libraryId());
    event.setUserId(currentUser.userId());
    event.setOccurredAtMillis(Instant.now().toEpochMilli());
    // 新增: 设置位置
    if (request != null) {
        event.setLatitude(request.latitude());
        event.setLongitude(request.longitude());
        event.setLocationLabel(request.locationLabel());
    }
    bowelEventRepository.save(event);
    
    // 新增: 反向地理编码回填 locationLabel
    if (event.getLatitude() != null && event.getLocationLabel() == null) {
        String label = geocodingService.reverseGeocode(event.getLatitude(), event.getLongitude());
        if (label != null) {
            event.setLocationLabel(label);
            bowelEventRepository.save(event);
        }
    }
    
    // ... 返回组装 ...
}
```

**toBowelEventDto 扩展**:
```java
private LifeConsoleBowelEventDto toBowelEventDto(BowelEventEntity event) {
    return new LifeConsoleBowelEventDto(
        event.getId(),
        event.getUserId(),
        event.getOccurredAtMillis(),
        event.getLatitude(),       // 新增
        event.getLongitude(),      // 新增
        event.getLocationLabel()   // 新增
    );
}
```

**注入 GeocodingService**:
```java
private final GeocodingService geocodingService;
// 在构造器中注入
```

### FR-18-10: LifeConsoleController 改造

**addBowelEvent 端点**:
```java
@PostMapping("/bowel-events")
public ApiResponse<LifeConsoleBowelMutationResponse> addBowelEvent(
    @RequestBody(required = false) AddBowelEventRequest request,  // 新增
    @RequestParam(required = false) String zoneId,
    AuthenticatedUser currentUser,
    HttpServletRequest httpRequest
) {
    return ApiResponse.success(requestId, lifeConsoleService.addBowelEvent(request, zoneId, currentUser));
}
```

`required = false` 保证旧客户端不发 body 也能工作（向后兼容）。

### FR-18-11: GeocodingService 接口与实现

**接口** `GeocodingService.java`:
```java
package com.yingshi.server.service.geocoding;

public interface GeocodingService {
    /**
     * 反向地理编码: 经纬度 → 地址文字
     * @return 地址文字，失败返回 null（不抛异常）
     */
    String reverseGeocode(double latitude, double longitude);
}
```

**AmapGeocodingService.java**（用 JDK 21 HttpClient + Caffeine 缓存）:
```java
package com.yingshi.server.service.geocoding;

import com.github.benmanes.caffeine.cache.Cache;
import com.github.benmanes.caffeine.cache.Caffeine;
import com.yingshi.server.config.GeocodingProperties;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;

import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.time.Duration;

@ConditionalOnProperty(prefix = "app.geocoding.amap", name = "enabled", havingValue = "true")
public class AmapGeocodingService implements GeocodingService {
    private static final Logger log = LoggerFactory.getLogger(AmapGeocodingService.class);
    
    private final GeocodingProperties props;
    private final HttpClient httpClient;
    private final ObjectMapper mapper = new ObjectMapper();
    
    // 缓存: 经纬度(精度3位小数=约110米) → 地址，最多 10000 条，7 天过期
    private final Cache<String, String> cache = Caffeine.newBuilder()
        .maximumSize(10_000)
        .expireAfterWrite(Duration.ofDays(7))
        .build();
    
    public AmapGeocodingService(GeocodingProperties props) {
        this.props = props;
        this.httpClient = HttpClient.newBuilder()
            .connectTimeout(Duration.ofMillis(props.timeoutMillis()))
            .build();
    }
    
    @Override
    public String reverseGeocode(double latitude, double longitude) {
        // 缓存键: 3 位小数约 110 米精度
        String cacheKey = String.format("%.3f,%.3f", latitude, longitude);
        String cached = cache.getIfPresent(cacheKey);
        if (cached != null) return cached;
        
        try {
            // 高德 location 参数: 经度,纬度（lng 在前！）
            String location = longitude + "," + latitude;
            String url = String.format("%s?key=%s&location=%s&output=json",
                props.endpoint(),
                URLEncoder.encode(props.key(), StandardCharsets.UTF_8),
                URLEncoder.encode(location, StandardCharsets.UTF_8)
            );
            
            HttpRequest req = HttpRequest.newBuilder()
                .uri(URI.create(url))
                .timeout(Duration.ofMillis(props.timeoutMillis()))
                .GET()
                .build();
            
            HttpResponse<String> resp = httpClient.send(req, HttpResponse.BodyHandlers.ofString());
            if (resp.statusCode() != 200) {
                log.warn("Amap geocoding HTTP {} for lat={},lng={}", resp.statusCode(), latitude, longitude);
                return null;
            }
            
            JsonNode root = mapper.readTree(resp.body());
            String status = root.path("status").asText();
            if (!"1".equals(status)) {
                log.warn("Amap geocoding failed: infocode={}, info={}", 
                    root.path("infocode").asText(), root.path("info").asText());
                return null;
            }
            
            String formatted = root.path("regeocode").path("formatted_address").asText();
            if (formatted == null || formatted.isEmpty()) return null;
            
            cache.put(cacheKey, formatted);
            return formatted;
        } catch (Exception e) {
            log.warn("Amap geocoding error for lat={},lng={}: {}", latitude, longitude, e.getMessage());
            return null;
        }
    }
}
```

**NoopGeocodingService.java**:
```java
package com.yingshi.server.service.geocoding;

import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;

@ConditionalOnMissingBean(GeocodingService.class)
public class NoopGeocodingService implements GeocodingService {
    @Override
    public String reverseGeocode(double latitude, double longitude) {
        return null;  // 静默跳过
    }
}
```

### FR-18-12: GeocodingProperties 配置类

**GeocodingProperties.java**（新建）:
```java
package com.yingshi.server.config;

import org.springframework.boot.context.properties.ConfigurationProperties;

@ConfigurationProperties(prefix = "app.geocoding.amap")
public record GeocodingProperties(
    boolean enabled,
    String key,
    String endpoint,
    long timeoutMillis
) {
    public GeocodingProperties {
        if (endpoint == null || endpoint.isBlank()) {
            endpoint = "https://restapi.amap.com/v3/geocode/regeo";
        }
        if (timeoutMillis <= 0) {
            timeoutMillis = 3000;
        }
    }
}
```

**AuthConfig.java** 修改: 在 `@EnableConfigurationProperties({...})` 中加 `GeocodingProperties.class`。

### FR-18-13: application.yml 配置

**application.yml** 在 `app:` 段下加:
```yaml
app:
  # ... 现有配置 ...
  geocoding:
    amap:
      enabled: ${AMAP_GEOCODING_ENABLED:false}
      key: ${AMAP_GEOCODING_KEY:}
      endpoint: ${AMAP_GEOCODING_ENDPOINT:https://restapi.amap.com/v3/geocode/regeo}
      timeout-millis: ${AMAP_GEOCODING_TIMEOUT_MILLIS:3000}
```

**注意**: 默认 `enabled=false`，生产环境通过环境变量 `AMAP_GEOCODING_ENABLED=true` + `AMAP_GEOCODING_KEY=a658afca...` 启用。

### FR-18-14: docker-compose.yml 环境变量

**docker-compose.yml** server 服务下加:
```yaml
AMAP_GEOCODING_ENABLED: ${AMAP_GEOCODING_ENABLED:-true}
AMAP_GEOCODING_KEY: ${AMAP_GEOCODING_KEY:-a658afca501f9337e964af99b3f2670f}
AMAP_GEOCODING_TIMEOUT_MILLIS: ${AMAP_GEOCODING_TIMEOUT_MILLIS:-3000}
```

**注意**: Key 默认值用用户提供的 Web Key，生产部署可覆盖。

### FR-18-15: Caffeine 依赖检查

**pom.xml**: 项目已有 `spring-boot-starter-cache`？需检查。如果没有，新增:
```xml
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
</dependency>
```
（Spring Boot 4.0.6 管理版本，无需显式指定）

---

## 三、FR-19: 位置追踪 Android

### FR-19-1: Gradle 依赖

**libs.versions.toml** 新增:
```toml
[versions]
playServicesLocation = "21.3.0"

[libraries]
play-services-location = { group = "com.google.android.gms", name = "play-services-location", version.ref = "playServicesLocation" }
```

**app/build.gradle.kts** dependencies 块新增:
```kotlin
implementation(libs.play.services.location)
```

### FR-19-2: AndroidManifest 权限

**AndroidManifest.xml** 在 `<manifest>` 内 `<application>` 之前加:
```xml
<!-- FR-19: 位置追踪 -->
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-feature android:name="android.hardware.location.gps" android:required="false" />
<uses-feature android:name="android.hardware.location" android:required="false" />
```

### FR-19-3: LocationHelper 新建

**文件**: `feature/life/LocationHelper.kt`（新建）

**设计**: 静默 fallback，不抛异常，不做交互式权限请求
```kotlin
package com.example.yingshi.feature.life

import android.Manifest
import android.content.Context
import android.content.pm.PackageManager
import android.os.Looper
import androidx.core.content.ContextCompat
import com.google.android.gms.location.LocationServices
import com.google.android.gms.location.Priority
import com.google.android.gms.tasks.CancellationTokenSource
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.suspendCancellableCoroutine
import kotlinx.coroutines.withContext
import kotlinx.coroutines.withTimeoutOrNull
import kotlin.coroutines.resume

data class LocationSnapshot(
    val latitude: Double,
    val longitude: Double,
)

object LocationHelper {
    /**
     * 获取当前 GPS 位置。
     * - 无定位权限 → 返回 null
     * - 无 GMS / FusedLocationProvider 失败 → 返回 null
     * - 超时（默认 3s）→ 返回 null
     * - 永不抛异常（FR-19 AC-3/AC-4 静默跳过）
     */
    suspend fun getCurrentLocation(
        context: Context,
        timeoutMillis: Long = 3000L,
    ): LocationSnapshot? = withContext(Dispatchers.IO) {
        if (!hasLocationPermission(context)) return@withContext null
        
        try {
            val client = LocationServices.getFusedLocationProviderClient(context)
            val cts = CancellationTokenSource()
            
            withTimeoutOrNull(timeoutMillis) {
                suspendCancellableCoroutine<LocationSnapshot> { cont ->
                    val task = client.getCurrentLocation(
                        Priority.PRIORITY_BALANCED_POWER_ACCURACY,
                        cts.token
                    )
                    task.addOnSuccessListener { loc ->
                        if (loc != null && cont.isActive) {
                            cont.resume(LocationSnapshot(loc.latitude, loc.longitude))
                        } else if (cont.isActive) {
                            cont.resume(null)
                        }
                    }
                    task.addOnFailureListener { e ->
                        if (cont.isActive) cont.resume(null)
                    }
                    cont.invokeOnCancellation { cts.cancel() }
                }
            }
        } catch (e: Exception) {
            null  // 包括 GooglePlayServicesNotAvailableException, SecurityException 等
        }
    }
    
    private fun hasLocationPermission(context: Context): Boolean {
        val fine = ContextCompat.checkSelfPermission(
            context, Manifest.permission.ACCESS_FINE_LOCATION
        ) == PackageManager.PERMISSION_GRANTED
        val coarse = ContextCompat.checkSelfPermission(
            context, Manifest.permission.ACCESS_COARSE_LOCATION
        ) == PackageManager.PERMISSION_GRANTED
        return fine || coarse
    }
}
```

### FR-19-4: LifeConsoleUploadBridge 改造

**改造点**: 在 `uploadMedia` 入口处 await 一次定位，扩展 `UploadMetadata` data class 加 lat/lng 字段，传给 `toTokenPayload()`。

```kotlin
suspend fun uploadMedia(context: Context, category: String, uris: List<Uri>) {
    if (uris.isEmpty()) return
    
    // 新增: 获取 GPS（静默 fallback）
    val location = LocationHelper.getCurrentLocation(context)
    
    // ... 现有逻辑 ...
    val metadataList = uris.map { resolveUploadMetadata(context, it) }
    // 把 location 传给每个 metadata
    val metadataWithLocation = metadataList.map { it.copy(location = location) }
    
    val token = RepositoryProvider.uploadRepository.createUploadToken(
        metadataWithLocation.first().toTokenPayload()
    )
    // ...
}

private data class UploadMetadata(
    // ... 现有字段 ...
    val location: LocationSnapshot? = null,  // 新增
) {
    fun toTokenPayload(): CreateUploadTokenPayload = CreateUploadTokenPayload(
        // ... 现有字段 ...
        latitude = location?.latitude,     // 新增
        longitude = location?.longitude,   // 新增
    )
}
```

### FR-19-5: LifeConsoleViewModel 改造

**改造点**: `addBowelEvent()` 调 repository 前获取定位:

```kotlin
fun addBowelEvent() {
    viewModelScope.launch {
        val appContext = getApplication<Application>().applicationContext
        // 新增: 获取 GPS（静默 fallback）
        val location = LocationHelper.getCurrentLocation(appContext)
        
        // 乐观本地更新（现有逻辑）
        val restored = /* ... */
        
        // 调 repository（签名扩展）
        val result = RepositoryProvider.lifeConsoleRepository.addBowelEvent(
            zoneId = LIFE_CONSOLE_ZONE_ID,
            latitude = location?.latitude,
            longitude = location?.longitude,
        )
        // ... 现有结果处理 ...
    }
}
```

### FR-19-6: Android DTO 改造

**UploadDtos.kt** — `UploadTokenRequestDto` 加字段:
```kotlin
data class UploadTokenRequestDto(
    // ... 现有字段 ...
    val latitude: Double? = null,     // 新增
    val longitude: Double? = null,    // 新增
)
```

**LifeConsoleDtos.kt** 改造:
```kotlin
// 新增: 排便事件请求体
data class LifeConsoleBowelEventRequestDto(
    val latitude: Double? = null,
    val longitude: Double? = null,
)

// 现有 DTO 加位置字段
data class LifeConsoleBowelEventDto(
    val bowelEventId: String,
    val userId: String,
    val occurredAtMillis: Long,
    val latitude: Double? = null,      // 新增
    val longitude: Double? = null,     // 新增
    val locationLabel: String? = null, // 新增
)

data class LifeConsoleBowelUserSummaryDto(
    val userId: String,
    val count: Int,
    val latestOccurredAtMillis: Long?,
    val eventTimesMillis: List<Long>,
    val latestLocationLabel: String? = null,  // 新增（最新事件的位置）
)

data class LifeConsoleHistoryDayDto(
    val date: String,
    val displayLabel: String,
    val selfMedia: List<RemoteMedia>,
    val partnerMedia: List<RemoteMedia>,
    val locationLabel: String? = null,  // 新增（代表性位置，取最新一张媒体）
)

data class LifeConsoleBowelHistoryDayDto(
    val date: String,
    val displayLabel: String,
    val users: List<LifeConsoleBowelUserSummaryDto>,
    val locationLabel: String? = null,  // 新增（代表性位置）
)
```

### FR-19-7: LifeConsoleApi 改造

```kotlin
interface LifeConsoleApi {
    // ... 其他端点不变 ...
    
    // 旧: @POST("api/life-console/bowel-events") suspend fun addBowelEvent(@Query("zoneId") zoneId: String): ApiEnvelopeDto<...>
    // 新:
    @POST("api/life-console/bowel-events")
    suspend fun addBowelEvent(
        @Query("zoneId") zoneId: String,
        @Body request: LifeConsoleBowelEventRequestDto,
    ): ApiEnvelopeDto<LifeConsoleBowelMutationDto>
}
```

### FR-19-8: RealLifeConsoleRepository 改造

```kotlin
class RealLifeConsoleRepository(...) {
    // 旧: suspend fun addBowelEvent(zoneId: String): ApiResult<RemoteLifeConsoleBowelMutation>
    // 新:
    suspend fun addBowelEvent(
        zoneId: String,
        latitude: Double? = null,
        longitude: Double? = null,
    ): ApiResult<RemoteLifeConsoleBowelMutation> {
        val request = LifeConsoleBowelEventRequestDto(latitude, longitude)
        val result = api.addBowelEvent(zoneId, request)
        // ... mapper 传递位置字段 ...
    }
}
```

### FR-19-9: Domain Model 改造

**LifeConsoleModels.kt** — `RemoteLifeConsoleBowelEvent / RemoteLifeConsoleBowelUserSummary / RemoteLifeConsoleHistoryDay / RemoteLifeConsoleBowelHistoryDay` 加 `locationLabel: String? = null`。

**RemoteModels.kt** — `RemoteMedia` 加 `latitude: Double? = null, longitude: Double? = null, locationLabel: String? = null`。

### FR-19-10: DTO Mapper 改造

在 `LifeConsoleDtos.kt` 的 mapper 扩展函数（`toRemoteModel()`）中传递位置字段。具体文件位置在阶段3 实施时定位。

---

## 四、FR-20: 历史页位置标签展示

### FR-20-1: 今日页 LifeMediaFrame 加位置

**LifeConsoleScreen.kt** L1085 起，在标题行附近加 locationLabel 展示。位置来源: `slot.mediaItems` 中最新一张媒体的 locationLabel（或第一张）。

```kotlin
// 在 LifeMediaFrame 标题行下方加
val locationLabel = slot.mediaItems
    .maxByOrNull { it.displayTimeMillis ?: 0L }
    ?.locationLabel

if (!locationLabel.isNullOrBlank()) {
    Text(
        text = locationLabel,
        style = YingShiThemeTokens.typography.caption,
        color = colors.textSecondary,
        maxLines = 1,
        overflow = TextOverflow.Ellipsis,
    )
}
```

### FR-20-2: 今日页 BowelCard 加位置

**LifeConsoleScreen.kt** L1343 起，在每位用户的时间戳旁加位置:
```kotlin
// 在 user.latestOccurredAtMillis 文本旁
val locationLabel = /* 从 snapshot.bowel.users 中取最新事件的 locationLabel */
if (!locationLabel.isNullOrBlank()) {
    Text(
        text = locationLabel,
        style = YingShiThemeTokens.typography.caption,
        color = colors.textSecondary,
        maxLines = 1,
        overflow = TextOverflow.Ellipsis,
    )
}
```

**注意**: `RemoteLifeConsoleBowelUserSummary` 已加 `latestLocationLabel` 字段，直接使用。

### FR-20-3: 历史页 LifeConsoleHistoryDaySection 加位置

**LifeConsoleScreen.kt** L794 起，在"共 X 张"统计行旁加 `day.locationLabel`:
```kotlin
Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween,
    verticalAlignment = Alignment.CenterVertically,
) {
    if (!day.locationLabel.isNullOrBlank()) {
        Text(
            text = day.locationLabel,
            style = YingShiThemeTokens.typography.caption,
            color = colors.textSecondary,
            maxLines = 1,
            overflow = TextOverflow.Ellipsis,
            modifier = Modifier.weight(1f, fill = false),
        )
    }
    Text(
        text = "共 ${day.selfMedia.size + day.partnerMedia.size} 张",
        style = YingShiThemeTokens.typography.statLabel,
        color = colors.goldAccent,
        textAlign = TextAlign.End,
    )
}
```

### FR-20-4: 历史页 LifeConsoleHistoryBowelDayCard 加位置

**LifeConsoleScreen.kt** L725 起，在 emoji 行旁加 `day.locationLabel`:
```kotlin
Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween,
    verticalAlignment = Alignment.CenterVertically,
) {
    Row(
        horizontalArrangement = Arrangement.spacedBy(spacing.xs),
        verticalAlignment = Alignment.CenterVertically,
    ) {
        Text("💩", ...)
        Text("${day.users.sumOf { it.count }} 次", ...)
    }
    if (!day.locationLabel.isNullOrBlank()) {
        Text(
            text = day.locationLabel,
            style = YingShiThemeTokens.typography.caption,
            color = colors.textSecondary,
            maxLines = 1,
            overflow = TextOverflow.Ellipsis,
        )
    }
}
```

### FR-20-5: 服务端历史页代表性位置策略

**LifeConsoleService.getHistory** 中，对每天的 MediaDto 列表聚合时取代表性位置:
```java
// 在构造 LifeConsoleHistoryDayDto 时
String dayLocationLabel = dayMediaList.stream()
    .filter(m -> m.locationLabel() != null)
    .max(Comparator.comparingLong(m -> m.displayTimeMillis() != null ? m.displayTimeMillis() : 0L))
    .map(MediaDto::locationLabel)
    .orElse(null);
```

对每天的排便事件聚合时同理取最新事件的位置。

---

## 五、实施顺序

1. **服务端 Entity + V29 迁移**（FR-18-1/2）— 基础
2. **服务端 UploadService 透传位置**（FR-18-3/4）
3. **服务端 GeocodingService 三件套**（FR-18-11/12/13/14）— 独立模块
4. **服务端 DTO 加字段**（FR-18-5/6/7/8）— Entity 完成后
5. **服务端 LifeConsoleService 改造**（FR-18-9）— 依赖 GeocodingService + DTO
6. **服务端 LifeConsoleController 改造**（FR-18-10）— 依赖 Service
7. **服务端编译验证** `mvnw.cmd compile`
8. **服务端测试扩展**（FR-18 测试）— 编译通过后
9. **服务端测试验证** `mvnw.cmd test`
10. **Android Gradle + Manifest**（FR-19-1/2）— 独立
11. **Android LocationHelper 新建**（FR-19-3）— 独立
12. **Android DTO/Model/Api/Repository 改造**（FR-19-6/7/8/9/10）— 独立
13. **Android UploadBridge 改造**（FR-19-4）— 依赖 LocationHelper + DTO
14. **Android ViewModel 改造**（FR-19-5）— 依赖 LocationHelper + Repository
15. **Android UI 加位置标签**（FR-20）— 依赖 Model
16. **Android 编译验证** `gradlew.bat assembleDebug`
17. **Docker 重建 + 健康检查** `docker compose up -d --build server`
18. **端到端联调验证**

## 六、完整性自检

实施完成后逐条对照本计划"变更文件清单"34 项，确认每项已落实。
