# Round 6 测试计划

> 模块: 13-今日足迹
> 轮次主题: 位置追踪功能 (FR-18 / FR-19 / FR-20)
> 测试设计日期: 2026-07-18
> 配套文档: 实施计划.md (34 项变更)、影响分析.md (12 个 IA 路径)、依赖映射.md (10 个回归模块)、实施记录.md (48/48 完整性)

---

## 一、测试范围与策略

### 1.1 测试范围

| 范围 | 数量 | 类型 |
|------|------|------|
| 编译/基础设施 | 4 | TC-INFRA-01~04 |
| FR-18 后端功能 | 12 | TC-F18-01~12 |
| FR-19 Android 前端功能 | 6 | TC-F19-01~06 |
| FR-20 历史页 UI 展示 | 4 | TC-F20-01~04 |
| 影响范围测试 | 12 | TC-IA-01~12 (对应 IA-1~12) |
| 跨模块回归测试 | 10 | TC-REGRESS-01~10 |
| **合计** | **48** | |

### 1.2 测试策略

- **后端**: 优先扩展现有 `LifeConsoleServiceTest` + `LifeConsoleControllerTest`，新增位置相关断言; GeocodingService 单独单测; 配置开关通过 Spring Profile 切换验证
- **前端**: LocationHelper 涉及 Android Framework + GMS，不做 JVM 单测，通过编译验证 + 真机手测; UI 通过 Compose Preview + 真机手测
- **集成**: Docker 容器启动后通过 curl + 数据库直查验证端到端数据流
- **回归**: 优先跑现有 JUnit 测试套件（`mvnw.cmd test`），新增字段对老测试断言无破坏

### 1.3 测试环境矩阵

| 环境 | 用途 | 关键配置 |
|------|------|---------|
| 本地 Maven 测试 | 后端 JUnit (Testcontainers PG) | `AMAP_GEOCODING_ENABLED` 未设置 → NoopGeocodingService 生效 |
| Docker Compose | 端到端集成 | `AMAP_GEOCODING_ENABLED=true` + Key 注入 → AmapGeocodingService 生效 |
| Android Emulator (API 33+) | 前端验证 | 授权 + 拒绝定位两种场景 |
| Compose Preview | UI 视觉验证 | 4 个 Composable 各 2 个状态 (有/无位置) |

---

## 二、编译 / 基础设施测试 (T-1~T-3)

### TC-INFRA-01: 后端编译通过

- **对应**: NFR-1
- **端**: Server
- **前置条件**: `YingShi-Server/` 目录可访问，JDK 21 已安装
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; .\mvnw.cmd compile -q
  ```
- **预期结果**: exit code 0，无编译错误
- **通过标准**: `BUILD SUCCESS`

### TC-INFRA-02: 后端测试编译通过

- **对应**: NFR-1
- **端**: Server
- **前置条件**: TC-INFRA-01 通过
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; .\mvnw.cmd test-compile -q
  ```
- **预期结果**: exit code 0，测试代码编译通过
- **通过标准**: `BUILD SUCCESS`

### TC-INFRA-03: Android 编译通过

- **对应**: NFR-1 + FR-19 AC-6
- **端**: Android
- **前置条件**: `YingShi/` 目录可访问，Android SDK 已配置，`play-services-location` 依赖已加入
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi; .\gradlew.bat assembleDebug
  ```
- **预期结果**: exit code 0，APK 生成成功
- **通过标准**: `BUILD SUCCESSFUL`

### TC-INFRA-04: Docker 重建 + 健康检查

- **对应**: NFR-1
- **端**: Server
- **前置条件**: Docker Desktop 运行中
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; docker compose up -d --build server
  # 等待 ~30s
  curl http://localhost:8080/actuator/health
  ```
- **预期结果**: 容器 healthy，健康检查返回 `{"status":"UP"}`
- **通过标准**: HTTP 200 + status UP

---

## 三、FR-18 后端功能测试

### TC-F18-01: media 表新增位置字段 (AC-1)

- **对应**: FR-18 AC-1
- **端**: Server
- **前置条件**: TC-INFRA-04 通过，Postgres 容器运行
- **步骤/命令**:
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "\d media"
  ```
- **预期结果**:
  ```
  latitude      | double precision
  longitude     | double precision
  location_label| character varying(255)
  ```
- **通过标准**: 3 个字段均存在，类型为 `double precision` / `character varying(255)`

### TC-F18-02: bowel_events 表新增位置字段 (AC-2)

- **对应**: FR-18 AC-2
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "\d bowel_events"
  ```
- **预期结果**: 3 个字段存在 (latitude / longitude / location_label)
- **通过标准**: 字段类型与 TC-F18-01 一致

### TC-F18-03: upload_tasks 表新增位置字段 (透传支撑)

- **对应**: FR-18 AC-3 (后端透传)
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "\d upload_tasks"
  ```
- **预期结果**: 3 个字段存在
- **通过标准**: 字段类型与 TC-F18-01 一致

### TC-F18-04: UploadTokenRequest 接受可选位置 (AC-3)

- **对应**: FR-18 AC-3
- **端**: Server
- **前置条件**: TC-INFRA-04 通过，已登录拿到 access token
- **步骤/命令**:
  ```powershell
  curl -X POST http://localhost:8080/api/uploads/token `
    -H "Authorization: Bearer $TOKEN" `
    -H "Content-Type: application/json" `
    -d '{"fileName":"loc-test.jpg","fileSizeBytes":512,"mimeType":"image/jpeg","mediaType":"IMAGE","width":4,"height":4,"displayTimeMillis":1721300000000,"sourceFingerprint":"loc-test-fp-001","operationType":"IMPORT_TO_APP","domain":"life","latitude":39.9075,"longitude":116.39723}'
  ```
- **预期结果**: HTTP 200，返回 `data.uploadId`
- **通过标准**: 不报 400/422，uploadId 非空

### TC-F18-05: addBowelEvent 接受位置参数 (AC-4)

- **对应**: FR-18 AC-4
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  curl -X POST "http://localhost:8080/api/life-console/bowel-events?zoneId=Asia/Shanghai" `
    -H "Authorization: Bearer $TOKEN" `
    -H "Content-Type: application/json" `
    -d '{"latitude":39.9075,"longitude":116.39723}'
  ```
- **预期结果**: HTTP 200，响应 `data.bowelEvent.latitude=39.9075`, `data.bowelEvent.longitude=116.39723`
- **通过标准**: `data.bowelEvent.bowelEventId` 非空 + lat/lng 字段透传成功

### TC-F18-06: addBowelEvent 向后兼容 (无 body)

- **对应**: FR-18 AC-4 (向后兼容，对应 IA-4)
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  curl -X POST "http://localhost:8080/api/life-console/bowel-events?zoneId=Asia/Shanghai" `
    -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: HTTP 200，响应正常 (`data.bowelEvent.bowelEventId` 非空)
- **通过标准**: 不报 400 (旧客户端无 body 仍可调用)

### TC-F18-07: 今日 API 返回位置字段 (AC-5)

- **对应**: FR-18 AC-5
- **端**: Server
- **前置条件**: TC-F18-05 已创建一条带位置的排便事件
- **步骤/命令**:
  ```powershell
  curl http://localhost:8080/api/life-console/today `
    -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: 响应 `data.bowel.users[*].latestLocationLabel` 字段存在（值依赖 GeocodingService 是否启用）
- **通过标准**: JSON 中 `latestLocationLabel` 字段 key 出现（启用高德时为非空字符串，未启用时可能为 null 或不存在）

### TC-F18-08: 历史 API 返回位置字段 (AC-5 + IA-3)

- **对应**: FR-18 AC-5 + IA-3 (withDisplayTime 同步)
- **端**: Server
- **前置条件**: TC-F18-04 已上传带位置的媒体 + TC-F18-05 已记录带位置的排便
- **步骤/命令**:
  ```powershell
  curl "http://localhost:8080/api/life-console/history?days=7&zoneId=Asia/Shanghai" `
    -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: 
  - `data.mediaDays[*].locationLabel` 字段存在
  - `data.mediaDays[*].selfMedia[*].latitude/longitude/locationLabel` 透传正确
  - `data.bowelDays[*].locationLabel` 字段存在
- **通过标准**: 历史 API 不丢位置（withDisplayTime 同步验证通过）

### TC-F18-09: V29 迁移幂等 (AC-6 + IA-1)

- **对应**: FR-18 AC-6 + IA-1
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  # 查 Flyway 状态
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "SELECT version, description, success FROM flyway_schema_history WHERE version='29';"
  # 重启容器验证不重复执行
  docker compose restart server
  curl http://localhost:8080/actuator/health
  ```
- **预期结果**: V29 一条记录 + success=true; 重启后仍 healthy，无重复迁移错误
- **通过标准**: Flyway 历史表仅一条 V29 记录

### TC-F18-10: NoopGeocodingService 静默 fallback (AC-7 + IA-5)

- **对应**: FR-18 AC-7 + IA-5
- **端**: Server
- **前置条件**: 测试环境 `AMAP_GEOCODING_ENABLED` 未设置 (默认 false)
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; .\mvnw.cmd test -Dtest=LifeConsoleServiceTest
  ```
  + 查看 `LifeConsoleServiceTest` 中 addBowelEvent 测试断言
- **预期结果**: 测试全 PASS，NoopGeocodingService 注入，`event.locationLabel` 保持 null，无异常
- **通过标准**: 30/30 PASS

### TC-F18-11: AmapGeocodingService location 参数顺序 (AC-7 + IA-6)

- **对应**: FR-18 AC-7 + IA-6 (高风险)
- **端**: Server
- **前置条件**: 已注入有效 AMAP_GEOCODING_KEY
- **步骤/命令**: 新增单元测试 `AmapGeocodingServiceTest.java`，验证 URL 构造:
  ```java
  // 期望 URL: location=116.39723,39.9075 (lng,lat 顺序)
  // 用北京坐标 (39.9075, 116.39723) 反查，期望返回 "北京市" 开头
  ```
  或者通过端到端验证:
  ```powershell
  # 启用 amap 后创建带位置排便事件
  curl -X POST "http://localhost:8080/api/life-console/bowel-events?zoneId=Asia/Shanghai" `
    -H "Authorization: Bearer $TOKEN" `
    -H "Content-Type: application/json" `
    -d '{"latitude":39.9075,"longitude":116.39723}'
  # 期望响应 data.bowelEvent.locationLabel 包含 "北京市"
  ```
- **预期结果**: locationLabel 为中文地址，且与北京天安门对应（不是上海或其他城市）
- **通过标准**: 返回的地址与传入坐标匹配，证明 location 参数顺序正确

### TC-F18-12: 配置开关切换 (AC-7 + IA-5)

- **对应**: FR-18 AC-7 + IA-5
- **端**: Server
- **前置条件**: docker-compose.yml 可修改环境变量
- **步骤/命令**:
  ```powershell
  # 场景1: amap.enabled=false (默认)
  $env:AMAP_GEOCODING_ENABLED="false"
  docker compose up -d --build server
  curl -X POST "http://localhost:8080/api/life-console/bowel-events?zoneId=Asia/Shanghai" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"latitude":39.9075,"longitude":116.39723}'
  # 验证 data.bowelEvent.locationLabel 为 null (或不存在)

  # 场景2: amap.enabled=true + 有效 Key
  $env:AMAP_GEOCODING_ENABLED="true"
  $env:AMAP_GEOCODING_KEY="a658afca501f9337e964af99b3f2670f"
  docker compose up -d --build server
  curl -X POST "http://localhost:8080/api/life-console/bowel-events?zoneId=Asia/Shanghai" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"latitude":39.9075,"longitude":116.39723}'
  # 验证 data.bowelEvent.locationLabel 非空
  ```
- **预期结果**: 场景1 locationLabel 为 null; 场景2 locationLabel 为中文地址
- **通过标准**: 配置开关切换生效，且 enabled=false 时不调高德 API

### TC-F18-13: 媒体上传位置回填 (AC-3 + AC-7)

- **对应**: FR-18 AC-3 + AC-7
- **端**: Server
- **前置条件**: amap.enabled=true
- **步骤/命令**:
  ```powershell
  # 1. 创建 upload token (带位置)
  curl -X POST http://localhost:8080/api/uploads/token -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{...,"latitude":39.9075,"longitude":116.39723}'
  # 2. 上传文件分块
  # 3. confirm
  # 4. 查询 media 表
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "SELECT latitude, longitude, location_label FROM media WHERE id='<mediaId>'"
  ```
- **预期结果**: media 行的 lat=39.9075, lng=116.39723, location_label 非空 (中文地址)
- **通过标准**: 位置从 UploadTaskEntity → MediaEntity 透传成功 + GeocodingService 回填成功

---

## 四、FR-19 Android 前端功能测试

### TC-F19-01: 上传时自动获取 GPS (AC-1)

- **对应**: FR-19 AC-1
- **端**: Android
- **前置条件**: Android 模拟器或真机，已授权 ACCESS_FINE_LOCATION，位置设置为北京 (39.9075, 116.39723)
- **步骤/命令**:
  1. 打开 App → 今日痕迹页面
  2. 点击"人物"或"吃饭"上传按钮
  3. 从相册选 1 张照片
  4. 等待上传完成
  5. 服务端查 media 表
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "SELECT latitude, longitude FROM media ORDER BY created_at DESC LIMIT 1"
  ```
- **预期结果**: media 表最新一行 lat ≈ 39.9075, lng ≈ 116.39723
- **通过标准**: 服务端收到位置数据 (Android 端 LocationHelper → UploadBridge → API → DB 链路通)

### TC-F19-02: 排便时自动获取 GPS (AC-2)

- **对应**: FR-19 AC-2
- **端**: Android
- **前置条件**: 同 TC-F19-01
- **步骤/命令**:
  1. 打开今日痕迹页面
  2. 点击"+1"排便按钮
  3. 服务端查 bowel_events 表
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "SELECT latitude, longitude FROM bowel_events ORDER BY occurred_at_millis DESC LIMIT 1"
  ```
- **预期结果**: bowel_events 表最新一行 lat ≈ 39.9075, lng ≈ 116.39723
- **通过标准**: ViewModel → Repository → API → DB 链路通

### TC-F19-03: 无 GPS 信号时静默跳过 (AC-3 + IA-8)

- **对应**: FR-19 AC-3 + IA-8
- **端**: Android
- **前置条件**: 模拟器开启飞行模式 + 关闭 GPS 模拟
- **步骤/命令**:
  1. 飞行模式 ON
  2. 关闭模拟器位置模拟
  3. 打开今日痕迹 → 点击上传 → 选照片
  4. 观察是否成功上传
  5. 服务端查 media 表
- **预期结果**: 上传成功完成 (无错误弹窗); media 行 lat/lng 为 NULL
- **通过标准**: 上传流程不阻断; `LocationHelper.getCurrentLocation` 返回 null 静默处理

### TC-F19-04: 用户未授权定位时静默跳过 (AC-4 + IA-7)

- **对应**: FR-19 AC-4 + IA-7
- **端**: Android
- **前置条件**: App 设置中拒绝定位权限
- **步骤/命令**:
  1. 系统设置 → 应用 → YingShi → 权限 → 位置 → 拒绝
  2. 打开今日痕迹 → 点击上传 → 选照片
  3. 点击 +1 排便
  4. 观察无权限弹窗、无错误提示
  5. 服务端查表
- **预期结果**: 上传/排便正常完成; DB 中 lat/lng 为 NULL; 全程无崩溃、无权限请求弹窗
- **通过标准**: `LocationHelper.hasLocationPermission` 返回 false → 静默返回 null → 业务流正常

### TC-F19-05: AndroidManifest 声明位置权限 (AC-5)

- **对应**: FR-19 AC-5
- **端**: Android
- **前置条件**: 源码可读
- **步骤/命令**:
  ```powershell
  # 查找权限声明
  Select-String -Path "E:\Study\App\YingShi\app\src\main\AndroidManifest.xml" -Pattern "ACCESS_FINE_LOCATION|ACCESS_COARSE_LOCATION"
  ```
- **预期结果**: 输出 2 行匹配，分别对应 ACCESS_FINE_LOCATION 和 ACCESS_COARSE_LOCATION
- **通过标准**: 两条权限均声明在 `<manifest>` 内 `<application>` 之前

### TC-F19-06: 使用 FusedLocationProvider (AC-6)

- **对应**: FR-19 AC-6
- **端**: Android
- **前置条件**: 源码可读
- **步骤/命令**:
  ```powershell
  # 查找 FusedLocationProviderClient 引用
  Select-String -Path "E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\life\LocationHelper.kt" -Pattern "FusedLocationProviderClient|LocationServices"
  # 查找依赖
  Select-String -Path "E:\Study\App\YingShi\app\build.gradle.kts" -Pattern "play.services.location"
  ```
- **预期结果**: LocationHelper.kt 中有 `LocationServices.getFusedLocationProviderClient` 调用; build.gradle.kts 中有 `implementation(libs.play.services.location)`
- **通过标准**: 使用 Google Play Services 的 FusedLocationProvider，不是旧版 LocationManager

---

## 五、FR-20 历史页 UI 展示测试

### TC-F20-01: 历史卡片展示 locationLabel (AC-1 + IA-10)

- **对应**: FR-20 AC-1 + IA-10
- **端**: Android
- **前置条件**: 至少有一条带 locationLabel 的媒体/排便事件; amap.enabled=true
- **步骤/命令**:
  1. 打开今日痕迹 → 点击"历史记录"
  2. 查看每个日期卡片
  3. 截图保存
- **预期结果**: 日期标题旁出现 `📍 北京市...` 胶囊样式; 位置胶囊与"共 X 张"右对齐
- **通过标准**: locationLabel 非空时胶囊可见

### TC-F20-02: 位置标签样式合规 (AC-2)

- **对应**: FR-20 AC-2
- **端**: Android
- **前置条件**: TC-F20-01 通过
- **步骤/命令**:
  ```powershell
  # 查看样式定义
  Select-String -Path "E:\Study\App\YingShi\app\src\main\java\com\example\yingshi\feature\life\LifeConsoleScreen.kt" -Pattern "labelSmall|textSecondary|locationLabel"
  ```
- **预期结果**: 位置标签使用 `YingShiThemeTokens.typography.labelSmall` + `YingShiThemeTokens.colors.textSecondary` (带 alpha 调整)
- **通过标准**: 样式 token 来自 YingShiThemeTokens，不抢视觉焦点 (字号小、颜色弱)

### TC-F20-03: 无位置时不显示 (AC-3)

- **对应**: FR-20 AC-3
- **端**: Android
- **前置条件**: 至少有一条不带 locationLabel 的媒体 (例如旧数据)
- **步骤/命令**:
  1. 打开历史记录
  2. 查看无位置的日期卡片
- **预期结果**: 不显示"未知位置"或"null"文字; 日期标题独占一行
- **通过标准**: `if (!locationLabel.isNullOrBlank())` 守卫生效

### TC-F20-04: 今日页卡片展示位置 (AC-4)

- **对应**: FR-20 AC-4
- **端**: Android
- **前置条件**: 今日有带位置的排便事件
- **步骤/命令**:
  1. 打开今日痕迹 (非历史页)
  2. 查看 BowelCard 区域
  3. 截图保存
- **预期结果**: BowelCard 中用户的最新事件旁展示 `latestLocationLabel`
- **通过标准**: locationLabel 非空时今日页可见

---

## 六、影响范围测试 (IA-1 ~ IA-12)

### TC-IA-01: V29 迁移幂等性 (对应 IA-1)

- **对应**: IA-1
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**: 见 TC-F18-09
- **预期结果**: V29 仅执行一次，重启不重复执行
- **通过标准**: Flyway 历史 V29 success=true 且无重复记录

### TC-IA-02: MediaDto 共享 DTO 向后兼容 (对应 IA-2)

- **对应**: IA-2
- **端**: Server
- **前置条件**: TC-INFRA-04 通过
- **步骤/命令**:
  ```powershell
  # 查询旧媒体 (无位置)
  curl "http://localhost:8080/api/media-feed?cursor=&pageSize=5" -H "Authorization: Bearer $TOKEN" | ConvertFrom-Json
  ```
- **预期结果**: 响应中媒体 JSON 不含 `latitude`/`longitude`/`locationLabel` key (因 `@JsonInclude(NON_NULL)`)
- **通过标准**: 旧数据序列化不出现新字段 key

### TC-IA-03: withDisplayTime 位置同步 (对应 IA-3，高风险)

- **对应**: IA-3 + TC-F18-08
- **端**: Server
- **前置条件**: 上传过带位置的媒体
- **步骤/命令**:
  ```powershell
  # 调用历史 API
  curl "http://localhost:8080/api/life-console/history?days=30&zoneId=Asia/Shanghai" -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: `data.mediaDays[*].selfMedia[*].latitude` 和 `locationLabel` 与上传时一致
- **通过标准**: withDisplayTime 重建 MediaDto 时位置字段未丢失

### TC-IA-04: addBowelEvent 破坏性变更向后兼容 (对应 IA-4)

- **对应**: IA-4 + TC-F18-06
- **端**: Server
- **步骤/命令**: 见 TC-F18-06 (curl 不发 body 调用)
- **预期结果**: HTTP 200 + 正常响应
- **通过标准**: 旧客户端兼容

### TC-IA-05: GeocodingService 配置开关 (对应 IA-5)

- **对应**: IA-5 + TC-F18-12
- **端**: Server
- **步骤/命令**: 见 TC-F18-12
- **预期结果**: enabled=false → NoopGeocodingService; enabled=true → AmapGeocodingService
- **通过标准**: 两种模式均启动成功且行为正确

### TC-IA-06: 高德 location 参数顺序 (对应 IA-6，高风险)

- **对应**: IA-6 + TC-F18-11
- **端**: Server
- **步骤/命令**: 见 TC-F18-11
- **预期结果**: 北京坐标返回北京地址 (而非上海)
- **通过标准**: location 参数顺序为 `lng,lat`

### TC-IA-07: Android 静默 fallback (对应 IA-7)

- **对应**: IA-7 + TC-F19-04
- **端**: Android
- **步骤/命令**: 见 TC-F19-04
- **预期结果**: 未授权时不抛 SecurityException
- **通过标准**: `LocationHelper.hasLocationPermission` 检查 + 业务流不阻断

### TC-IA-08: GPS 超时静默 (对应 IA-8)

- **对应**: IA-8 + TC-F19-03
- **端**: Android
- **步骤/命令**: 见 TC-F19-03
- **预期结果**: 3s 超时返回 null，上传流程继续
- **通过标准**: `withTimeoutOrNull` 生效

### TC-IA-09: 历史代表性位置策略 (对应 IA-9)

- **对应**: IA-9
- **端**: Server
- **前置条件**: 同一天上传 2 张不同位置的媒体 (位置 A 较早、位置 B 较新)
- **步骤/命令**:
  ```powershell
  # 1. 上传媒体 A: lat=31.2304 (上海), displayTimeMillis=10:00
  # 2. 上传媒体 B: lat=39.9075 (北京), displayTimeMillis=15:00
  # 3. 查历史
  curl "http://localhost:8080/api/life-console/history?days=1&zoneId=Asia/Shanghai" -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: `data.mediaDays[0].locationLabel` 为北京地址 (B 的位置)
- **通过标准**: `pickDayLocationLabel` 取 `effectiveTimeMillis` 最大的 entry

### TC-IA-10: Android UI 4 处展示 (对应 IA-10)

- **对应**: IA-10 + TC-F20-01~04
- **端**: Android
- **步骤/命令**: 见 TC-F20-01~04
- **预期结果**: 4 个 Composable (LifeConsoleHistoryDaySection / LifeConsoleHistoryBowelDayCard / LifeMediaFrame / BowelCard) 在有位置时展示
- **通过标准**: 4 处 UI 均按设计展示位置

### TC-IA-11: 照片模块回归 (对应 IA-11)

- **对应**: IA-11
- **端**: Server
- **前置条件**: 现有照片模块测试存在
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; .\mvnw.cmd test -Dtest=PhotoFeed*Test,Album*Test
  ```
- **预期结果**: 所有测试 PASS
- **通过标准**: 现有照片模块测试不因 MediaDto 加字段而失败

### TC-IA-12: 上传模块回归 (对应 IA-12)

- **对应**: IA-12
- **端**: Server / Android
- **前置条件**: 现有上传测试存在
- **步骤/命令**:
  ```powershell
  cd E:\Study\App\YingShi-Server; .\mvnw.cmd test -Dtest=Upload*Test
  ```
- **预期结果**: 上传相关测试 PASS
- **通过标准**: 照片上传不传位置 → UploadService 接受 null → media 行 lat/lng 为 NULL

---

## 七、跨模块回归测试 (REGRESS-01 ~ 10)

### TC-REGRESS-01: 照片流列表加载 (模块 03)

- **对应**: 依赖映射 4.1 #2
- **端**: Both
- **前置条件**: 至少有 5 张媒体
- **步骤/命令**:
  ```powershell
  curl "http://localhost:8080/api/media-feed?cursor=&pageSize=10" -H "Authorization: Bearer $TOKEN"
  # Android 端打开照片流 Tab
  ```
- **预期结果**: HTTP 200 + 列表正常返回; Android 端缩略图正常显示
- **通过标准**: MediaDto 加字段不破坏照片流

### TC-REGRESS-02: 大相册列表加载 (模块 05)

- **对应**: 依赖映射 4.1 #3
- **端**: Both
- **步骤/命令**:
  ```powershell
  curl "http://localhost:8080/api/albums" -H "Authorization: Bearer $TOKEN"
  # Android 端打开大相册
  ```
- **预期结果**: 列表正常返回; Android 端正常展示
- **通过标准**: 大相册模块不受影响

### TC-REGRESS-03: 小相册详情加载 (模块 06)

- **对应**: 依赖映射 4.1 #4
- **端**: Both
- **步骤/命令**:
  ```powershell
  curl "http://localhost:8080/api/albums/<albumId>/posts" -H "Authorization: Bearer $TOKEN"
  curl "http://localhost:8080/api/posts/<postId>" -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: 详情正常返回，媒体字段完整
- **通过标准**: 小相册详情含媒体列表，不破坏

### TC-REGRESS-04: 照片上传不带位置 (模块 20)

- **对应**: 依赖映射 4.1 #1 + IA-12
- **端**: Both
- **步骤/命令**:
  1. Android 端打开照片 Tab → 上传一张照片 (不经 LifeConsoleUploadBridge)
  2. 服务端查 media 表
  ```powershell
  docker exec yingshi-postgres psql -U yingshi -d yingshi -c "SELECT latitude, longitude, location_label FROM media ORDER BY created_at DESC LIMIT 1"
  ```
- **预期结果**: lat/lng/location_label 均为 NULL
- **通过标准**: 非足迹上传不传位置 (PhotoUploadBridge 不调 LocationHelper)

### TC-REGRESS-05: 聊天上传不带位置 (模块 18)

- **对应**: 依赖映射 4.1 #8
- **端**: Both
- **步骤/命令**: 通过聊天模块导入聊天记录 → 查 chat 相关 media 行
- **预期结果**: lat/lng/location_label 均为 NULL
- **通过标准**: 聊天上传不调 LocationHelper

### TC-REGRESS-06: 回收站恢复媒体 (模块 09)

- **对应**: 依赖映射 4.1 #6
- **端**: Both
- **步骤/命令**:
  1. 上传一张带位置的足迹媒体
  2. 软删除该媒体
  3. 从回收站恢复
  4. 查 media 表位置字段
- **预期结果**: 恢复后 lat/lng/location_label 与删除前一致
- **通过标准**: 软删除/恢复不丢位置

### TC-REGRESS-07: 增量同步含新字段 (Sync 模块)

- **对应**: 依赖映射 4.1 #7
- **端**: Both
- **步骤/命令**:
  ```powershell
  curl "http://localhost:8080/api/sync/media?sinceVersion=0" -H "Authorization: Bearer $TOKEN"
  ```
- **预期结果**: 同步响应包含 lat/lng/location_label 字段
- **通过标准**: 增量同步不丢新字段

### TC-REGRESS-08: Widget 显示今日数据 (模块 14)

- **对应**: 依赖映射 4.1 #9
- **端**: Android
- **前置条件**: 桌面已添加 YingShi Widget
- **步骤/命令**:
  1. 在桌面长按 → 添加 YingShi 今日痕迹 Widget
  2. 触发 Widget 刷新
  3. 点 Widget 上的 +1 排便按钮
- **预期结果**: Widget 正常显示今日数据; 点 +1 调用 `addBowelEvent()` (无参) 仍成功
- **通过标准**: Widget 调用 addBowelEvent (默认参数 null) 不报错 (向后兼容)

### TC-REGRESS-09: 照片查看器不显示位置 (模块 07)

- **对应**: 依赖映射 4.2 #9 (向后兼容)
- **端**: Android
- **步骤/命令**:
  1. 打开照片流 → 点击单张照片进入照片查看器
  2. 观察是否显示位置标签
- **预期结果**: 不显示位置标签 (本轮未在照片查看器加 UI)
- **通过标准**: RemoteMedia 加字段但不影响照片查看器 UI

### TC-REGRESS-10: 大相册不显示位置 (模块 05)

- **对应**: 依赖映射 4.2 #10 (向后兼容)
- **端**: Android
- **步骤/命令**:
  1. 打开大相册 → 进入相册详情
  2. 观察媒体缩略图
- **预期结果**: 不显示位置标签
- **通过标准**: 大相册模块未消费新字段，UI 不变

---

## 八、后端新增测试用例规划

本轮在后端新增以下 JUnit 测试用例（写入 `LifeConsoleServiceTest.java` 和 `LifeConsoleControllerTest.java`）：

### 8.1 LifeConsoleServiceTest 新增

| 测试方法名 | 验证点 | 对应 |
|----------|--------|------|
| `addBowelEventWithLocationStoresLatLng` | 调用 `addBowelEvent(zoneId, body, user)` 传入位置 → DB 中 event.lat/lng 与传入一致 | FR-18 AC-4 |
| `addBowelEventWithoutBodyStillWorks` | 调用 `addBowelEvent(zoneId, null, user)` → 正常返回 | FR-18 AC-4 / IA-4 |
| `addBowelEventWithLocationTriggersNoopGeocoding` | 测试环境 (NoopGeocodingService) → label 为 null | FR-18 AC-7 / IA-5 |
| `bowelHistoryIncludesLocationLabel` | 上传带位置排便事件 → `getHistory` 返回的 bowelDays[*].locationLabel 字段存在 | FR-18 AC-5 / IA-3 |
| `mediaHistoryPreservesLocationThroughWithDisplayTime` | 上传带位置媒体 → `getHistory` 返回的 selfMedia[*].latitude 非空 | IA-3 高风险 |

### 8.2 LifeConsoleControllerTest 新增

| 测试方法名 | 验证点 | 对应 |
|----------|--------|------|
| `addBowelEventWithLocationReturns200` | POST `/api/life-console/bowel-events` 带 body → 200 + 响应含 lat/lng | FR-18 AC-4 |
| `addBowelEventWithoutBodyReturns200` | POST 无 body → 200 (向后兼容) | FR-18 AC-4 / IA-4 |
| `createUploadTokenWithLocationReturns200` | POST `/api/uploads/token` 带 lat/lng → 200 + uploadId 非空 | FR-18 AC-3 |
| `getHistoryReturnsLocationFields` | GET `/api/life-console/history` 响应含 locationLabel 字段 | FR-18 AC-5 / IA-3 |

### 8.3 新增 AmapGeocodingServiceTest（单元测试）

| 测试方法名 | 验证点 | 对应 |
|----------|--------|------|
| `reverseGeocodeReturnsNullWhenDisabled` | NoopGeocodingService.reverseGeocode → null | FR-18 AC-7 / IA-5 |
| `reverseGeocodeCachesBy3DecimalKey` | 同一 3 位小数坐标 → 缓存命中 | FR-18 AC-7 |
| `reverseGeocodeHandlesHttpFailure` | HTTP 500 → 返回 null (不抛) | IA-5 |
| `reverseGeocodeHandlesTimeout` | 超时 → 返回 null | IA-5 |

> **注**: AmapGeocodingService 涉及外部 HTTP 调用，使用 Mockito 模拟 HttpClient，或通过 `@MockBean` 替换 GeocodingProperties (enabled=false) 走 NoopGeocodingService。

---

## 九、Android 测试策略

### 9.1 不新增 JVM 单元测试的理由

- `LocationHelper` 涉及 `FusedLocationProviderClient` (依赖 Google Play Services + Android Framework)，无法在 JVM 中模拟
- `LifeConsoleUploadBridge` / `LifeConsoleViewModel` 中 LocationHelper 调用已通过编译期验证 + 真机手测覆盖
- DTO/Model 字段透传通过编译验证 (Kotlin data class 自动生成 equals/copy)

### 9.2 真机手测脚本 (人工执行)

| 用例 | 步骤 | 预期 |
|------|------|------|
| 授权定位 + 上传媒体 | 设置 → 授权定位 → 上传一张照片 | DB 中 media.lat 非空 |
| 拒绝定位 + 上传媒体 | 设置 → 拒绝定位 → 上传一张照片 | DB 中 media.lat 为 NULL，无崩溃 |
| 授权定位 + 排便 | 授权 → 点 +1 排便 | DB 中 bowel_events.lat 非空 |
| 历史页展示位置 | 上传带位置媒体 → 打开历史页 | 日期卡片有 📍 胶囊 |
| 今日页展示位置 | 上传带位置排便 → 看今日页 | BowelCard 有位置文字 |
| Widget +1 排便 | 桌面 Widget 点 +1 | DB 中 bowel_events 新增一行 (lat=NULL) |

### 9.3 Compose Preview 覆盖

新增 4 个 Preview 函数 (在 LifeConsoleScreen.kt 末尾或单独文件)：

```kotlin
@Preview
fun LifeConsoleHistoryTimelineItemWithLocationPreview() { ... }

@Preview
fun LifeConsoleHistoryTimelineItemWithoutLocationPreview() { ... }

@Preview
fun LifeConsoleHistoryBowelDayCardWithLocationPreview() { ... }

@Preview
fun BowelCardWithLocationPreview() { ... }
```

> **注**: 本轮在阶段5 时视情况补充 Preview，如果原代码已有 Preview 框架则复用。

---

## 十、测试执行顺序与依赖

```
1. TC-INFRA-01~04 (编译 + Docker) ─── 必须先通过
      ↓
2. TC-F18-01~03 (DB 字段) ─── 验证 V29 迁移成功
      ↓
3. TC-F18-04 (UploadTokenRequest) ─── 验证 API 接受位置
      ↓
4. TC-F18-05/06 (addBowelEvent) ─── 验证排便 API
      ↓
5. TC-F18-07/08 (今日/历史 API) ─── 验证响应含位置
      ↓
6. TC-F18-09 (V29 幂等) ─── 重启验证
      ↓
7. TC-F18-10~13 (GeocodingService) ─── 配置开关 + 真实调用
      ↓
8. TC-IA-09 (历史代表性位置) ─── 同日多位置策略
      ↓
9. TC-F19-01~06 (Android 真机) ─── 端到端验证
      ↓
10. TC-F20-01~04 (UI 展示) ─── 视觉验证
      ↓
11. TC-REGRESS-01~10 (跨模块回归) ─── 影响范围确认
```

---

## 十一、测试通过标准

### 11.1 整体通过标准

| 类别 | 通过标准 |
|------|---------|
| 编译/基础设施 | 4/4 PASS |
| FR-18 后端功能 | 13/13 PASS (含 AC-1~7 全覆盖) |
| FR-19 Android 功能 | 6/6 PASS (含 AC-1~6 全覆盖) |
| FR-20 UI 展示 | 4/4 PASS (含 AC-1~4 全覆盖) |
| 影响范围测试 | 12/12 PASS |
| 跨模块回归测试 | 10/10 PASS |
| **总计** | **48/48 PASS (100%)** |

### 11.2 失败处理

- **FAIL → 根因分析 → 修复 → 重测** (不允许表面补丁)
- **同一测试连续 FAIL 2 次** → 停下做彻底 root-cause 分析
- **跨模块回归 FAIL** → P0 立即修复 (优先级最高)
- **后端编译 FAIL** → 不进入后续测试
- **Docker 容器 unhealthy** → 不进入功能测试

### 11.3 已知风险点 (重点观察)

| 风险 ID | 测试用例 | 风险描述 | 缓解 |
|---------|---------|---------|------|
| R-1 | TC-IA-03 / TC-F18-08 | withDisplayTime 漏传位置 → 历史 API 丢位置 | 已在阶段3 实施时同步; 阶段5 重点验证 |
| R-2 | TC-F18-11 / TC-IA-06 | 高德 location 参数顺序写反 → 返回错误地址 | URL 构造严格 `lng,lat` 顺序; 真实调用北京坐标验证 |
| R-3 | TC-F18-06 / TC-IA-04 | addBowelEvent 向后兼容破坏 → 旧客户端 400 | `@RequestBody(required=false)` + null 守护 |
| R-4 | TC-F19-04 / TC-IA-07 | 未授权定位崩溃 | LocationHelper 内部权限检查 + try-catch |
| R-5 | TC-INFRA-04 | V29 字段类型不匹配 → ddl-auto=validate 失败 | V29 用 `DOUBLE PRECISION`，Entity 用 `Double`，类型一致 |
| R-6 | TC-IA-09 | 历史代表性位置策略错误 | `pickDayLocationLabel` 取 max effectiveTimeMillis |

---

## 十二、测试文档维护

本测试计划在阶段5 (落地测试) 执行过程中如发现新问题或需要补充测试，将更新本文档并标注 `更新: YYYY-MM-DD`。

测试执行结果将汇总至 `05-落地测试/测试报告.md`，每个测试用例标记 PASS / PARTIAL / FAIL。
