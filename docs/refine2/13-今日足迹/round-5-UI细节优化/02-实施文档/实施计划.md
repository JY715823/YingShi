# Round 5 实施计划

> 基于 FR-13~FR-17: UI 细节优化
> 实施日期: 2026-07-18

---

## 变更文件清单

| # | 文件路径 | 端 | 变更类型 | 变更内容 |
|---|---------|-----|---------|---------|
| 1 | `app/src/main/java/com/example/yingshi/ui/components/YingShiSurfaces.kt` | Android | 修改 | FR-13: YingShiMistCard 增加 `borderless` 参数 |
| 2 | `app/src/main/java/com/example/yingshi/ui/theme/Tokens.kt` | Android | 修改 | FR-14: 新增 `YingShiTypography` data class + `typography` 字段 |
| 3 | `app/src/main/java/com/example/yingshi/ui/components/AppShell.kt` | Android | 修改 | FR-15: TitleTabs Tab Box 加 `weight(1f)` + 统一 padding |
| 4 | `app/src/main/java/com/example/yingshi/feature/life/LifeConsoleScreen.kt` | Android | 修改 | FR-13/14/16/17: 去框+字体+删文案+删日期 |

---

## FR-13: 今日页卡片去框优化

### 目标
去掉四张媒体卡片和健康日志卡片的完整边框，保留左侧彩色竖条+微阴影，整体更透气。

### 变更1: YingShiSurfaces.kt — YingShiMistCard 签名扩展

**当前签名** (L355-373):
```kotlin
fun YingShiMistCard(
    modifier: Modifier = Modifier,
    shape: Shape? = null,
    color: Color? = null,
    borderColor: Color? = null,
    content: @Composable () -> Unit,
)
```

**新签名**:
```kotlin
fun YingShiMistCard(
    modifier: Modifier = Modifier,
    shape: Shape? = null,
    color: Color? = null,
    borderColor: Color? = null,
    borderless: Boolean = false,
    content: @Composable () -> Unit,
)
```

**Surface border 逻辑**:
```kotlin
border = if (borderless) null
         else BorderStroke(1.dp, borderColor ?: YingShiThemeTokens.colors.dividerSoft.copy(alpha = 0.70f)),
```

shadowElevation 保持 2.dp 不变（需求要求保留阴影）。

### 变更2: LifeConsoleScreen.kt — 4 处 MistCard 调用

| # | Composable | 行号 | 修改 |
|---|-----------|------|------|
| 1 | LifeMediaFrame | L1136-1139 | 加 `borderless = true` |
| 2 | BowelCard | L1367-1371 | 加 `borderless = true` |
| 3 | LifeConsoleHistoryBowelDayCard | L739-742 | 加 `borderless = true` |
| 4 | LifeConsoleHistoryDaySection | L809-812 | 加 `borderless = true` |

### 保留项
- 左侧彩色竖条 (L1143-1149): 宽度 4.dp, alpha 0.72 — 保留
- 内部媒体区阴影 (L1199): `.shadow(1.dp, ...)` — 保留
- shadowElevation 2.dp — 保留

---

## FR-14: 字体层次优化

### 目标
拉开标题/正文/辅助文字字号层次，统一字体 token 使用。

### 变更1: Tokens.kt — 新增 YingShiTypography

在 `YingShiThemeTokens` 对象中新增 `typography` 字段：

```kotlin
data class YingShiTypography(
    val cardTitle: TextStyle,       // 卡片大标题: titleMedium + Bold
    val sectionTitle: TextStyle,    // 区块标题: titleLarge + SemiBold
    val body: TextStyle,            // 正文: bodyMedium + Normal
    val caption: TextStyle,         // 辅助说明: labelSmall + textSecondary
    val statNumber: TextStyle,      // 大号数字: displaySmall + Bold
    val statLabel: TextStyle,       // 数字标签: labelMedium + Normal
)

object YingShiThemeTokens {
    ...
    @Composable
    val typography: YingShiTypography
        @Composable get() = YingShiTypography(
            cardTitle = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold),
            sectionTitle = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.SemiBold),
            body = MaterialTheme.typography.bodyMedium,
            caption = MaterialTheme.typography.labelSmall,
            statNumber = MaterialTheme.typography.displaySmall.copy(fontWeight = FontWeight.Bold),
            statLabel = MaterialTheme.typography.labelMedium,
        )
}
```

### 变更2: LifeConsoleScreen.kt — Text style 替换

| 行号 | 当前 | 替换为 | 角色 |
|------|------|--------|------|
| L1180 | titleMedium + Bold | `typography.cardTitle` | LifeMediaFrame 标题 |
| L1379 | titleMedium + Bold | `typography.cardTitle` | BowelCard 标题 |
| L478 | titleLarge + SemiBold | `typography.sectionTitle` | 历史页大标题 |
| L1411-1416 | displaySmall + Bold | `typography.statNumber` | BowelCard 大号数字 |
| L592 | titleLarge + Bold | `typography.statNumber` | 历史统计数字 |
| L757 | titleLarge + Bold | `typography.statNumber` | 历史大便"X 次" |
| L384 | bodyMedium | `typography.body` | AlertDialog 正文 |
| L957 | bodyMedium | `typography.body` | 历史空态 |
| L1419 | bodyMedium | `typography.body` | BowelCard"次"后缀 |
| L436 | bodySmall | `typography.caption` | 今日空态副标题 |
| L496 | bodySmall | `typography.caption` | 历史页 actionMessage |
| L962 | bodySmall | `typography.caption` | 历史空态副标题 |
| L1245 | labelSmall | `typography.caption` | LifeMediaFrame"今天还没有" |
| L1350 | bodySmall | `typography.caption` | EmptyFrame 副标题 |
| L831 | labelSmall | `typography.statLabel` | 历史日卡"共 X 张" |
| L1399 | labelLarge + Medium | `typography.statLabel` | BowelCard 用户名 |

**注意**: 替换时需移除原有的 `fontWeight = FontWeight.xxx` 显式设置（已封装在 token 中），但保留 `color` 设置。

---

## FR-15: 大便 Tab 截断修复

### 目标
"大便" Tab 完整显示，不截断。三 Tab 等宽。

### 变更: AppShell.kt — TitleTabs 实现 (L190-234)

**修改1**: Tab Box 加 `weight(1f)` (L203)
```kotlin
Box(
    modifier = Modifier
        .weight(1f)   // 新增: 等分宽度
        .yingShiHapticClickable(...)
        ...
)
```

**修改2**: 统一 selected/unselected 的 horizontal padding (L217-220)
```kotlin
.padding(
    horizontal = spacing.sm,  // 统一为 sm，消除宽度抖动
    vertical = spacing.xxs,
)
```

---

## FR-16: 去掉"今日记录在上方展示"文案

### 目标
删除历史页冗余提示文案。

### 变更: LifeConsoleScreen.kt L603-608

**删除**:
```kotlin
// FR-4: 今日记录提示
Text(
    text = "今日记录在上方展示",
    style = MaterialTheme.typography.bodySmall,
    color = colors.textSecondary.copy(alpha = 0.60f),
)
```

---

## FR-17: 去掉卡片冗余日期显示

### 目标
日期仅在时间轴左侧显示一次，卡片内不重复。

### 变更: LifeConsoleScreen.kt L817-833

**当前** (L817-833):
```kotlin
Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween,
    verticalAlignment = Alignment.CenterVertically,
) {
    Text(  // 冗余日期 — 删除
        text = day.displayLabel,
        style = MaterialTheme.typography.titleMedium,
        fontWeight = FontWeight.SemiBold,
        color = colors.titleAccent,
    )
    Text(  // "共 X 张" — 保留
        text = "共 ${day.selfMedia.size + day.partnerMedia.size} 张",
        style = MaterialTheme.typography.labelSmall,
        color = colors.goldAccent,
    )
}
```

**修改后**: 删除日期 Text，保留"共 X 张" Text，改为右对齐：
```kotlin
Text(
    text = "共 ${day.selfMedia.size + day.partnerMedia.size} 张",
    style = YingShiThemeTokens.typography.statLabel,
    color = colors.goldAccent,
    modifier = Modifier.fillMaxWidth(),
    textAlign = TextAlign.End,
)
```

---

## 实施顺序

1. `Tokens.kt` — 新增 YingShiTypography（FR-14 基础）
2. `YingShiSurfaces.kt` — YingShiMistCard 加 borderless 参数（FR-13）
3. `AppShell.kt` — TitleTabs 修复（FR-15）
4. `LifeConsoleScreen.kt` — 去框 + 字体替换 + 删文案 + 删日期（FR-13/14/16/17）
5. 编译验证: `gradlew.bat assembleDebug`