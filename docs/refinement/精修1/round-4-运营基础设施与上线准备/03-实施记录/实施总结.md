# Round 4 实施总结

> **实施日期**: 2026-07-04
> **涉及模块**: R4-M1 ~ R4-M5

---

## 一、实际修改的文件清单

### R4-M1: CI/CD 流水线

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `.github/workflows/android-ci.yml` | **新增** | GitHub Actions CI 工作流：JDK 21 + Android SDK + Gradle 缓存 → compileDebugKotlin → assembleDebug → 上传 APK 产物 + Lint 检查 |

### R4-M2: 崩溃上报集成

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `gradle/libs.versions.toml` | 修改 | 添加 crashlytics 版本 (3.0.4) + firebase-crashlytics 库 + firebase-crashlytics 插件 |
| `build.gradle.kts` (root) | 修改 | 添加 `alias(libs.plugins.firebase.crashlytics) apply false` |
| `app/build.gradle.kts` | 修改 | 条件化 apply crashlytics 插件（google-services.json 存在时）+ 添加 firebase-crashlytics 依赖 |
| `app/.../util/AppLogger.kt` | **新增** | 统一日志工具：封装 Log.e/w/i/d/v + FirebaseCrashlytics.log/recordException + setCustomKey/setUserId。Crashlytics 不可用时静默降级 |

### R4-M3: 真机回归测试

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `03-实施记录/R4-M3-真机回归清单.md` | **新增** | 15 个模块 × 主力机 + 窄屏机的完整回归检查清单 + 5 个跨模块场景 + 设备适配表 |

### R4-M4: 隐私权限说明

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `app/src/main/res/values/strings.xml` | 修改 | 添加 5 个权限用途说明字符串：CAMERA、READ_MEDIA_IMAGES、READ_MEDIA_VIDEO、READ_EXTERNAL_STORAGE、POST_NOTIFICATIONS |

### R4-M5: 上线前终审

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `06-复检报告/R4-M5-上线前终审报告.md` | **新增** | 全部检查清单逐项核对结果 |
