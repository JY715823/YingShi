# Round 3 实施总结

> **实施日期**: 2026-07-04
> **涉及模块**: R3-M1 ~ R3-M4

---

## 一、实际修改的文件清单

### R3-M1: YingShiApp.kt 拆分

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `app/.../app/YingShiAppHelpers.kt` | **新增** | 提取 9 个私有辅助函数/Composable/常量/扩展（AuthCheckingScreen、transferToastMessage、toNotificationPostRoute、pushSmallAlbumFallbackRoute、toTrashEntryTypeOrNull、NotificationPlaceholderPalette、findActivity、successfulResultMediaIdsInOperation、YingShiAppPreview） |
| `app/.../app/YingShiBackHandlers.kt` | **新增** | 提取 18 个 BackHandler 块 + root 双击退出 + 照片选择清除的完整模板（~200 行），供后续集成 |
| `app/.../app/YingShiDeepLinks.kt` | **新增** | 提取 5 个深链 LaunchedEffect（OPEN_LIFE_CONSOLE/LEDGER/LEDGER_ADD/PHOTO_FEED/SMALL_ALBUM）完整模板，供后续集成 |
| `app/.../app/YingShiApp.kt` | 修改 | 移除已提取的私有辅助函数（2064→1935 行，减少 129 行） |

### R3-M2: AGENTS.md 更新

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `AGENTS.md` | **重写** | 从 Stage 0 描述全面更新为 Stage 12+：当前阶段、构建命令、技术栈、代码结构、架构模式、协作规则、已知技术债务、完成检查清单 |

### R3-M3: 服务端并发与性能修复

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `UploadNotificationService.java` | 修改 | `notifiedUploadOperationKeys` 从 `ConcurrentHashMap.newKeySet()` 改为 `ConcurrentHashMap<String, Long>`（带时间戳），添加 24h TTL 过期清理（每 100 次通知触发一次清理） |
| `PushDispatchSupport.java` | 修改 | 从 `CompletableFuture.runAsync()`（默认 ForkJoinPool.commonPool）改为 `Executors.newVirtualThreadPerTaskExecutor()`（Java 21 虚拟线程），消除 sleep 导致的线程饥饿风险 |

### R3-M4: Deprecated API 更新

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `LedgerUiSupport.kt` | 修改 | `Icons.Default.ArrowBack` → `Icons.AutoMirrored.Filled.ArrowBack`（import + 使用） |

## 二、与实施文档的偏差

| 偏差 | 说明 |
|------|------|
| YingShiApp.kt 拆分范围缩小 | 原计划拆分到 ~200 行，实际仅提取 helpers（129 行减少）。BackHandlers 和 DeepLinks 模板已创建但未集成（30+ 参数的传递需要状态持有者类重构，风险较高） |
| RateLimitFilter 多实例 | 未修改代码——两人应用场景下 ConcurrentHashMap 单机限流可接受，已在 AGENTS.md 技术债务表中标注 |
| X-Forwarded-For 防伪 | 已在 Round 1 的 nginx-https.conf 中配置 `$remote_addr`（非 `$proxy_add_x_forwarded_for`），服务端未修改 |
| FLAG_FULLSCREEN/statusBarColor | 已在 PhotoViewerScreen.kt 中有 @Suppress("DEPRECATION") 注解，替换为 WindowInsetsController 需要 Viewer 重构，归入后续版本 |
| LocalClipboardManager | Compose BOM 2024.09.00 中 `LocalClipboardManager` 未正式弃用，无需替换 |

## 三、遇到的问题

1. **YingShiApp.kt 大括号不平衡**：提取 helpers 后文件缺少一个闭合大括号，通过 Python 脚本检测并修复（opens: 419, closes: 418 → 419/419）
2. **DeepLinks 文件 import 丢失**：`toPostDetailPlaceholderRoute` import 在 Write 时被截断，编译报错后手动添加修复
