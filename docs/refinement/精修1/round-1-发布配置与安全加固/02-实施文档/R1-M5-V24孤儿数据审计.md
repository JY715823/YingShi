# R1-M5: V24 外键孤儿数据审计 实施文档

## 一、目标

为 V24 外键迁移编写孤儿数据审计脚本，在生产库执行迁移前识别并处理孤儿行，避免 FK 约束失败导致服务启动崩溃。

## 二、涉及文件清单

| 文件 | 端 | 改动类型 | 说明 |
|------|-----|---------|------|
| `scripts/audit-orphans.sql` | Server/DB | 新增 | 孤儿数据审计 SQL |
| `scripts/fix-orphans.sql` | Server/DB | 新增 | 孤儿数据修复 SQL 模板 |

**不修改** V24 迁移脚本本身（不加 NOT VALID），原因：
- 项目使用 Flyway，修改已执行的迁移脚本会 checksum 不匹配
- V24 在 dev 环境已成功执行，说明 schema 定义正确
- 问题仅在于生产库可能存在孤儿数据，应在迁移前清理

## 三、配置级实施方案

### 3.1 audit-orphans.sql

覆盖 V24 中所有 FK 关系，对每个关系执行孤儿检查：

```sql
-- V24 Foreign Key Orphan Audit Script
-- Run this BEFORE deploying V24 migration to production.
-- Usage: psql -h localhost -U yingshi -d yingshi -f scripts/audit-orphans.sql

\echo '=== V24 Foreign Key Orphan Audit ==='
\echo ''

-- Library-scoped tables
\echo '--- albums.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM albums a
    LEFT JOIN shared_libraries sl ON a.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- small_albums.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM small_albums sa
    LEFT JOIN shared_libraries sl ON sa.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- media.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM media m
    LEFT JOIN shared_libraries sl ON m.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- comments.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM comments c
    LEFT JOIN shared_libraries sl ON c.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- trash_items.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM trash_items t
    LEFT JOIN shared_libraries sl ON t.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- upload_tasks.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM upload_tasks ut
    LEFT JOIN shared_libraries sl ON ut.library_id = sl.id
    WHERE sl.id IS NULL;

-- Small albums ownership
\echo '--- small_albums.album_id → albums.id ---'
SELECT COUNT(*) AS orphan_count FROM small_albums sa
    LEFT JOIN albums a ON sa.album_id = a.id
    WHERE a.id IS NULL AND sa.album_id IS NOT NULL;

-- Junction table
\echo '--- small_album_media.small_album_id → small_albums.id ---'
SELECT COUNT(*) AS orphan_count FROM small_album_media sam
    LEFT JOIN small_albums sa ON sam.small_album_id = sa.id
    WHERE sa.id IS NULL;

\echo '--- small_album_media.media_id → media.id ---'
SELECT COUNT(*) AS orphan_count FROM small_album_media sam
    LEFT JOIN media m ON sam.media_id = m.id
    WHERE m.id IS NULL;

-- Comments (optional references, SET NULL)
\echo '--- comments.small_album_id → small_albums.id (optional) ---'
SELECT COUNT(*) AS orphan_count FROM comments c
    LEFT JOIN small_albums sa ON c.small_album_id = sa.id
    WHERE sa.id IS NULL AND c.small_album_id IS NOT NULL;

\echo '--- comments.media_id → media.id (optional) ---'
SELECT COUNT(*) AS orphan_count FROM comments c
    LEFT JOIN media m ON c.media_id = m.id
    WHERE m.id IS NULL AND c.media_id IS NOT NULL;

-- Auth tables
\echo '--- auth_sessions.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM auth_sessions s
    LEFT JOIN users u ON s.user_id = u.id
    WHERE u.id IS NULL;

\echo '--- auth_sessions.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM auth_sessions s
    LEFT JOIN shared_libraries sl ON s.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- auth_login_challenges.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM auth_login_challenges lc
    LEFT JOIN users u ON lc.user_id = u.id
    WHERE u.id IS NULL;

\echo '--- auth_remembered_logins.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM auth_remembered_logins rl
    LEFT JOIN users u ON rl.user_id = u.id
    WHERE u.id IS NULL;

-- Push tables
\echo '--- push_device_tokens.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM push_device_tokens pdt
    LEFT JOIN users u ON pdt.user_id = u.id
    WHERE u.id IS NULL;

\echo '--- push_preferences.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM push_preferences pp
    LEFT JOIN users u ON pp.user_id = u.id
    WHERE u.id IS NULL;

-- Shared library members
\echo '--- shared_library_members.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM shared_library_members slm
    LEFT JOIN shared_libraries sl ON slm.library_id = sl.id
    WHERE sl.id IS NULL;

\echo '--- shared_library_members.user_id → users.id ---'
SELECT COUNT(*) AS orphan_count FROM shared_library_members slm
    LEFT JOIN users u ON slm.user_id = u.id
    WHERE u.id IS NULL;

-- Upload tasks optional reference
\echo '--- upload_tasks.media_id → media.id (optional) ---'
SELECT COUNT(*) AS orphan_count FROM upload_tasks ut
    LEFT JOIN media m ON ut.media_id = m.id
    WHERE m.id IS NULL AND ut.media_id IS NOT NULL;

-- Ledger tables (library_id → shared_libraries)
\echo '--- ledger_books.library_id → shared_libraries.id ---'
SELECT COUNT(*) AS orphan_count FROM ledger_books lb
    LEFT JOIN shared_libraries sl ON lb.library_id = sl.id
    WHERE sl.id IS NULL;

-- Ledger internal relationships (sample - cover key ones)
\echo '--- ledger_transactions.category_id → ledger_categories.id (optional) ---'
SELECT COUNT(*) AS orphan_count FROM ledger_transactions lt
    LEFT JOIN ledger_categories lc ON lt.category_id = lc.id
    WHERE lc.id IS NULL AND lt.category_id IS NOT NULL;

\echo '--- ledger_transactions.account_id → ledger_accounts.id (optional) ---'
SELECT COUNT(*) AS orphan_count FROM ledger_transactions lt
    LEFT JOIN ledger_accounts la ON lt.account_id = la.id
    WHERE la.id IS NULL AND lt.account_id IS NOT NULL;

\echo ''
\echo '=== Audit complete. All orphan_count should be 0. ==='
```

### 3.2 fix-orphans.sql

提供软修复（SET NULL）和硬修复（DELETE）两种模板：

```sql
-- V24 Foreign Key Orphan Fix Script
-- WARNING: Review each section before executing. Some fixes use DELETE.
-- Usage: psql -h localhost -U yingshi -d yingshi -f scripts/fix-orphans.sql

BEGIN;

-- =====================================================================
-- SOFT FIX: SET NULL for optional references
-- =====================================================================

-- comments.small_album_id (ON DELETE SET NULL in V24)
UPDATE comments SET small_album_id = NULL
    WHERE small_album_id IS NOT NULL
    AND small_album_id NOT IN (SELECT id FROM small_albums);

-- comments.media_id (ON DELETE SET NULL in V24)
UPDATE comments SET media_id = NULL
    WHERE media_id IS NOT NULL
    AND media_id NOT IN (SELECT id FROM media);

-- upload_tasks.media_id (ON DELETE SET NULL in V24)
UPDATE upload_tasks SET media_id = NULL
    WHERE media_id IS NOT NULL
    AND media_id NOT IN (SELECT id FROM media);

-- ledger_transactions.category_id (ON DELETE SET NULL in V24)
UPDATE ledger_transactions SET category_id = NULL
    WHERE category_id IS NOT NULL
    AND category_id NOT IN (SELECT id FROM ledger_categories);

-- ledger_transactions.account_id (ON DELETE SET NULL in V24)
UPDATE ledger_transactions SET account_id = NULL
    WHERE account_id IS NOT NULL
    AND account_id NOT IN (SELECT id FROM ledger_accounts);

-- =====================================================================
-- HARD FIX: DELETE for required references (CASCADE in V24)
-- Review carefully before uncommenting each section!
-- =====================================================================

-- Albums without library (should not happen in normal operation)
-- DELETE FROM albums WHERE library_id NOT IN (SELECT id FROM shared_libraries);

-- Media without library
-- DELETE FROM media WHERE library_id NOT IN (SELECT id FROM shared_libraries);

-- Auth sessions without user
-- DELETE FROM auth_sessions WHERE user_id NOT IN (SELECT id FROM users);

-- Push tokens without user
-- DELETE FROM push_device_tokens WHERE user_id NOT IN (SELECT id FROM users);

COMMIT;

\echo 'Orphan fix complete. Run audit-orphans.sql again to verify.'
```

## 四、验收标准

- [ ] `audit-orphans.sql` 可在 PG 16 上直接运行，输出所有 FK 关系的孤儿统计
- [ ] 所有 orphan_count 为 0（或已人工确认修复）
- [ ] `fix-orphans.sql` 的软修复部分可直接执行
- [ ] 硬修复部分默认注释，需人工确认后取消注释执行
- [ ] V24 迁移在清理后的生产库上成功执行
